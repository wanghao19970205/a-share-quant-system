# A股量化系统总调研记录：阶段效果、provenance 与严格链审计

> 统一记录入口：后续阶段效果和调研结果均追加到本文件，不再分散到多个记录文档。
> 调研日期：2026-08-12
> 生产纪律：realtime 保持旧模型；未恢复调度；未修改 active manifest；未执行生产发布。

## 0. 已完成阶段效果总表

### 0.1 网格工程修复

已完成并已推送至 GitHub `main`，commit `8fe1f9f`：

- short 网格加入 `hold_rank_buffer={0,1,2}`，`rebalance_stride` 固定为 `1`。
- short 组合数由 8100 扩展到 24300。
- standalone、daily grid、promotion gate 不再无条件把 buffer 整列压回 `0`。
- 显式 CLI stride/buffer 优先于 manifest 回读，避免隔天静默回滚。
- strict label worker 改为依据已准备 target matrix 判断 horizon，不再硬编码 `target_ret_{horizon}d`。
- selection score 的 annual return、drawdown、turnover 按 gross 归一化。
- stability monthly win rate 按 horizon 独立统计。

结论：**工程修复和远端定向测试已完成；生产仍未恢复，不能把“实现完成”写成“生产已启用”。**

### 0.2 严格 short 网格

真实远端结果：

- v3：24300 组合、horizon 1/2/3，作为 buffer 敏感性证据，不作为生产 short 主基线。
- 严格 h1：24300 组合，buffer 0/1/2 各 8100 行，正常完成。
- 严格 h2：24300 组合，buffer 0/1/2 各 8100 行，正常完成。
- h2 严格标签：115165 行，`tradable_ret_2d` 非空 113876。
- h2 同底层参数配对中，buffer=2 相对 buffer=0 的平均差：total return -0.023702、annual return -0.014465、turnover -0.005722、Sharpe -0.081054。

结论：**buffer 确实生效，但当前 h2 样本未显示收益优势；不得因冠军出现 buffer=2 就切换生产参数。**

### 0.3 严格 rolling/fixed 对照

已有结果：rolling_v2 净收益 +23.56bp/day、fixed_33 +18.96bp/day；rolling 相对 fixed 方向性净增益约 +4.60bp/day。

封锁项：配对 CI 跨零、Holm block-5 `p=0.4111588841`、fixed-33 来源不可审计、production strict flags 未启用。

结论：**rolling 方向性较好，但不是 confirmed alpha，也未达到 promotion-ready。**

### 0.4 成本敏感性

远端隔离报告：

```text
/www/A/research_runs/opus5_cost_audit_20260812/fair_daily_topn_evaluation.json
```

60 个验证交易日、191129 行，成本 7/10/20bp。盘中 causal 模型：20bp→10bp 约改善 9.1bp/day，20bp→7bp 约改善 11.8bp/day，但模型仍为负。

结论：**成本修正是必要的记账口径修正，不足以使盘中链翻正，也不等于新增 alpha。**

## 1. 本轮目标：fixed-33 provenance 与 rolling/fixed 配对审计

> 调研日期：2026-08-12
> 调研范围：远端 `/www/A/research_runs/audit_20260810_v1`、远端真实研究 artifact、只读容器环境
> 生产纪律：未恢复调度、未修改 active manifest、未更新 realtime

## 1. 本轮目标

根据 `复核意见_新.md` 的 N1/N2 入口，完成：

1. 追踪 rolling factor selection 的可审计来源。
2. 判断 fixed-33 是否存在可复原的 producer、窗口和标签 provenance。
3. 核对 rolling/fixed paired validation 是否满足同口径配对条件。
4. 判断下一步应重建 fixed-33，还是先继续补 provenance/数据门控。

## 2. Rolling selection provenance

远端存在两个 rolling selection manifest：

```text
quant_data_ridge_strict/ridge_only_strict_rolling_provenance_20260812_9w_factor_selection_manifest.parquet
quant_data_ridge_strict/ridge_only_strict_rolling_provenance_20260812_9w_v2_factor_selection_manifest.parquet
```

两者均为 9 个窗口、15 个字段，字段包含：

```text
window
train_start / train_end
valid_start / valid_end
test_start / test_end
label_col
purge_span
candidate_count / selected_count
candidate_pool_sha256
selected_sha256
generator_code_sha256
manifest_hash
```

v2 manifest 的窗口记录显示：

- `label_col=tradable_ret_1d`
- `purge_span=3`
- `selected_count=30`
- 每窗口有 candidate pool hash、selected hash、generator code hash
- train/valid/test 边界逐窗口记录

示例窗口 20：

```text
train: 2023-10-16 .. 2025-10-10
valid: 2025-10-16 .. 2025-11-11
test:  2025-11-16 .. 2025-12-16
candidate_count: 67
selected_count: 30
purge_span: 3
label_col: tradable_ret_1d
```

结论：**rolling selection 的 artifact 具备窗口、标签、purge、候选池和生成器 hash，来源结构上可审计。**

本轮进一步核对 generator hash：manifest 中的 `generator_code_sha256` 为：

```text
01bbde12fb682ddfcc94f3c341ba3cca5619ae481d1a65bb73c91d73e4079ac5
```

它不对应 `full_train_batched.py`，而对应 rolling provenance producer `quant/pipeline.py` 的历史审计版本。远端当前 `/www/A/quant/pipeline.py` 与审计 source 副本 hash 一致：

```text
77b819dbf668a1becff62905528ec095aa95c67d59e7354d66d4008136add0f5
```

因此 rolling manifest 的生成器来源链可以闭合到 `quant/pipeline.py`；这解决了“manifest 字段齐全但 generator 来源不明”的疑问，但不解除 paired comparison 的显著性和 promotion blocker。

## 3. Fixed-33 provenance

当前远端范围内未找到可用的 fixed-33 prediction/return artifact：

```text
comparison_status=blocked_missing_fixed_prediction_and_return_artifacts
available_fixed_artifacts=false
```

paired validation 明确记录：

```text
requested_fixed_prefix=fixed_33_strict_rolling_protocol
available_fixed_artifacts=false
decision=do_not_substitute_legacy_fixed_selection_or_rerun_without_provenance
```

现有 `factor_selection_lh1000_cont` 仍属于冻结消费者侧 artifact，缺少能够证明以下内容的 producer 证据：

- fixed-33 的候选池来源
- 选择窗口
- label 口径
- train/valid/predict 边界
- 生成代码版本
- candidate pool hash
- selected list hash

结论：**fixed-33 当前不能被重新包装成“有 provenance 的 legacy baseline”；不能用 rolling manifest 或冻结手工清单伪造 fixed provenance。**

## 4. Rolling/fixed paired validation 口径核验

已有 paired validation artifact：

```text
audit_20260812_rolling_fixed_paired_validation.json
```

配对契约已满足：

```text
return_dates=176
return_dates_equal=true
prediction_keys=115165
prediction_keys_equal=true
pit_windows=9
pit_universe_manifests_equal=true
label_col=tradable_ret_1d
cost_roundtrip=0.002
date_min=2025-11-17
date_max=2026-08-06
```

因此，**就日期、预测 key、PIT 窗口、标签和成本字段而言，rolling/fixed paired validation 是同口径配对**。

已有结果：

```text
rolling_net_mean=0.0023559009
fixed_net_mean=0.0018958785
net_gain=4.6002bp/day
gross_gain=5.7934bp/day
extra_cost=1.1932bp/day
extra_turnover=0.05966
```

但统计封锁仍在：

```text
block bootstrap CI 跨零
Holm block-5 p=0.4111588841
reject_0_05=false
```

最终 decision：

```text
rolling_is_directionally_better_but_not_confirmed_alpha_or_promotion_ready
```

## 5. 本轮结论

### 已确认

- rolling selection 有 9 窗口 manifest，字段和 hash 足以支持后续 provenance 验证。
- rolling/fixed 配对的日期、prediction keys、PIT windows、label 和 cost 口径已经对齐。
- rolling 相对 fixed 的净收益方向为正，约 `+4.60bp/day`。

### 未确认

- rolling 相对 fixed 的差异没有通过 Holm 校正显著性门。
- fixed-33 的 baseline 来源不可审计。
- 不存在可直接复用的 fixed-33 prediction/return artifact。

### 不允许的结论

- 不能称 rolling 为 confirmed alpha。
- 不能称 fixed-33 为有 provenance 的 legacy baseline。
- 不能用 rolling 的 manifest 补造 fixed-33 provenance。
- 不能因为 rolling 方向性较好就恢复生产调度或更新 realtime。

## 6. 下一步调研顺序

### N1-a：追 rolling producer

检查 `full_train_batched.py` 的 rolling selection producer 是否能在每个窗口稳定写出：

- candidate pool
- selected list
- label/protocol
- train/valid/predict 边界
- candidate/selected/generator hashes

如果 producer 与现有 manifest 不一致，先修 producer，不打开 provenance gate。

### N1-b：fixed-33 复原判定

只做来源审计，不直接重跑：

- 查找固定清单是否有生成脚本、提交、输入 hash 或窗口 metadata。
- 若仍只有消费者引用，正式标记“不可复原”。
- 不用新造 sidecar 把历史清单伪装成 legacy provenance。

### N2-a：paired comparison 扩展

在 fixed provenance 可审计或明确不可复原后，再决定：

- 重新生成 fixed control；或
- 只保留 rolling 与现有 fixed 结果的方向性对照，不作晋级结论。

### N3：缓存/严格链阻塞项

在任何 strict production batch 之前处理：

- `price_source_signature` 改为窗口稳定签名。
- `DailyICCache` key 加入标签口径字段。
- purge 静默跳窗改为计数和阈值门禁。
- 修复 `realtime-weight-shadow` 对 `tradable_ret` 的回退兼容。

## 7. 当前状态

本轮只读调研已完成，未改源码、未改生产配置、未更新 realtime。

当前最合理的下一步不是直接重跑 fixed-33，而是先完成 rolling producer provenance 审计，并对 fixed-33 做“可复原/不可复原”判定。

## 8. N3：严格链缓存与静默失效审计

### 8.1 全局 price_source_signature

实现位置：`quant/full_train_batched.py:192-201`。

当前签名对 `quant_dir/price/` 下全部 parquet 计算：

```text
(name, size, mtime_ns)
```

而不是按训练窗口或被使用月份计算。严格标签 recipe 在 `:431-434` 注入该 signature；`daily-light` 又会通过 `--force-latest-price` 每日重写最新价格 parquet。

影响：

- 任意一个价格文件的 mtime/size 变化都会改变整个 signature。
- 严格 `tradable-label` 窗口 recipe 全部变化。
- 日更窗口缓存可能每天全部 miss。
- 这是性能和可复现性的施工 blocker，不是收益证据。

建议：将 signature 改为窗口稳定的月份/文件子集签名，或直接复用 `_window_month_signature()` 的边界语义；必须保留旧 signature 的兼容识别和显式 cache invalidation 记录。

状态：**已定位，未修改。**

### 8.2 DailyICCache 口径键不完整

实现位置：`quant/full_train_batched.py:520-535`。

当前 disk key 仅包含：

```text
horizon | target | factor1 | factor2 | ...
```

但 `tradable_ret_1d` 的数值还受以下参数影响：

- `sell_roll_max_days`
- `strict_execution_labels`
- `min_adv20`
- `min_listing_sessions`
- trading status source
- price/source recipe

这些参数不进入 IC cache key；而 IC cache 在 `:1257-1260` 初始化，只要传入 `window_cache_dir` 就启用，不受 `_rolling_cache_allowed()` 是否关闭影响。

影响：

- 即使预测窗口缓存因 strict/provenance 被关闭，旧 factor IC 仍可能被复用。
- 同列名、不同标签数值口径之间会发生陈旧 IC 污染。
- 这可能改变 rolling factor selection，属于结论可信性 blocker。

建议：把 label recipe/source signature 纳入 DailyICCache key；切换标签口径时拒绝旧 key，而不是仅清空目录。

状态：**已定位，未修改。**

### 8.3 purge 静默跳窗

实现位置：`quant/full_train_batched.py:502-517`、`:1431-1444`。

`tradable-label` 下 `purge_span = horizon + sell_roll_max_days - 1`。当未开启 strict execution labels 时，使用 `_purged_end_by_code()`；任意股票在边界前不足 purge 日期就返回 `None`。调用方只打印：

```text
window=... skipped: purge leaves insufficient train/valid dates
```

随后直接进入下一个窗口，不抛异常、不设置最低完成比例。

影响：

- 任务可能以 exit 0 完成，但实际训练窗口少于预期。
- 研究报告可能把少窗口结果当成完整 rolling 结果。
- 现有 `_minimum_validation_months()` 只保护月份数量，不保护 purge 跳窗比例。

建议：累计 requested/skipped/completed 窗口；跳过比例超过阈值时 fail closed；manifest 必须写入跳窗统计，不能只写 completed windows。

状态：**已定位，未修改。**

### 8.4 realtime-weight-shadow 标签兼容

实现位置：`realtime/weight_shadow.py:27-30`、`:235-246`。

当前固定：

```python
TARGET_COLUMN = "target_ret_1d"
```

而严格 `tradable-label` 训练输出可能只保留 `tradable_ret_1d`。`weight_shadow` 的列检查和 realized filter 不使用 realtime reference 中已有的 target fallback 链。

影响：

- 直接缺列时抛 `ValueError`。
- 列并集合并后可能出现 `target_ret_1d` 全 NA，进而样本静默停止增长。
- 当前 `realtime-weight-shadow` 被 scheduler SKIP，所以不是当前立即故障；但恢复调度前属于阻塞级修复。

建议：与 `realtime/reference.py` 共用 target fallback resolver，或在 prediction producer 同时保留 `target_ret_1d` 和 `tradable_ret_1d`；必须增加 strict-label shadow regression test。

状态：**已定位，未修改。**

### 8.5 N3 优先级结论

施工优先级：

1. `DailyICCache` key：直接影响因子选择结果，先修。
2. purge 跳窗统计和 fail-closed：直接影响窗口样本完整性，紧随其后。
3. `price_source_signature`：影响严格链缓存性能和日更可运行性，随后修。
4. weight-shadow target fallback：恢复调度前必须修，当前 realtime 本身仍保持旧模型。

本轮已开始修复 N3：

- `DailyICCache` 增加 `recipe_signature`，严格训练路径使用 label recipe canonical hash；默认 legacy 调用保持兼容。
- purge 训练增加 requested/skipped/completed 统计；`purge_horizon=True` 时默认完成率低于 90% fail closed；summary 写入完成率和阈值。
- 本地 py_compile/diff check 通过。
- 远端缓存和 purge 定向测试通过：`Ran 3 tests / OK`。

当前仍未修改 price signature、weight-shadow fallback、生产调度或 realtime；上述代码修改尚未形成新的 GitHub commit。

## 9. 什么时候可以晋级

没有固定日历日期，必须以门禁完成为准。至少要满足：

- strict/rolling 与 fixed control 的 provenance 可审计；fixed-33 若不可复原，必须重建 provenance-complete control。
- 同口径 paired comparison 的 Holm 校正后 `p<0.05`，CI 不跨零；当前 `p=0.4112`，尚未满足。
- 缓存 key、price signature、purge 跳窗和 weight-shadow 兼容 blocker 修复并通过远端真实数据验证。
- 预测 artifact freshness gate 和生产调度 dry-run 通过。
- 候选优势在成本、尾部记账、敞口和 universe 口径统一后仍成立。
- 本地、远端、GitHub main 源码和逐文件 SHA256 一致。

因此：**当前不能晋级；最早的晋级时点是上述门禁全部通过之后，而不是本轮网格或修复完成之后。**

本轮已完成并验证以下修复：

- `DailyICCache` 增加 label recipe signature，避免不同标签参数复用同一 IC cache。
- purge 窗口增加 requested/skipped/completed 统计；完成率低于 90% 时 fail closed；summary 记录阈值和完成率。
- `weight_shadow` 支持 `target_ret_1d -> tradable_ret_1d -> open_ret_1d` 回退，并拒绝所有候选列均无真实值的输入。
- 远端定向测试：`Ran 5 tests / OK`。

`price_source_signature` 已完成 window-stable 修复：

- `_price_source_signature(start, end)` 按 price parquet 的实际 `date` 列判断是否与窗口 `[start, end)` 相交。
- 窗口外文件的 mtime/size 变化不影响当前窗口签名。
- 窗口内文件变化会改变签名并触发缓存失效。
- 读取失败时保守纳入签名，避免不安全复用。
- rolling window 会把该窗口签名注入 window recipe，IC cache 也切换到对应 recipe namespace。

远端 7 项缓存/signature/purge/shadow 定向测试通过：`Ran 7 tests / OK`。

本轮代码修改已完成验证，待提交推送；仍不清理旧缓存、不恢复调度、不更新 realtime。

## 10. 生产晋级门禁

当前只能在以下门禁全部通过后讨论晋级；单个网格冠军、正收益、成本下降或方向性优于 fixed 都不构成晋级条件：

1. **来源门**：rolling/fixed 的 factor selection 均有可审计 producer、窗口、label、candidate pool hash、selected hash 和 generator hash；fixed-33 若不可复原，必须先生成新的 provenance-complete fixed control，不能使用伪 legacy baseline。
2. **口径门**：候选与 baseline 使用相同 PIT universe、标签、成本、buyability、purge、holdout 和敞口定义。
3. **缓存门**：DailyICCache 标签口径 key、window-stable price signature、严格链缓存策略全部修复并经真实数据验证；不能存在旧口径 cache reuse。
4. **窗口门**：训练窗口 requested/completed/skipped 可审计，purge 跳窗比例在阈值内；超过阈值必须 fail closed。
5. **统计门**：配对差异 CI 不跨零，Holm 校正后 `p<0.05`，并满足最小样本/稳定性门；当前 rolling/fixed 的 Holm block-5 `p=0.4112`，未通过。
6. **收益门**：优势在成本、敞口和尾部记账口径修正后仍存在，不能只来自账面惩罚变化。
7. **生产门**：strict 上游 reference data、scheduler 参数转发、weight-shadow 兼容、预测 freshness gate 全部就绪；先研究环境 dry-run，再小范围生产验证。
8. **交付门**：本地、远端、GitHub main 逐文件源码和 SHA256 一致，测试和真实远端证据已固化。

### 当前判断

**当前没有任何候选达到晋级条件。** 最近的 rolling 结果虽为方向性正收益，但统计门、fixed provenance、strict production chain、realtime freshness 和缓存门均未通过。因此 realtime 继续使用旧模型，生产调度继续关闭。

## 10. 统一当前状态

- 阶段效果、rolling/fixed provenance、成本敏感性和 N3 缓存审计均已记录在本文件。
- 后续调研继续追加本文件，不再创建分散阶段记录。
- 当前仍不允许把 rolling、fixed、buffer 或成本重算结果直接晋级生产。

## 11. 严格链 research-only dry-run（2026-08-12/13）

本轮使用真实远端数据、隔离容器和新 panel 名称执行最小 strict training probe；未恢复生产调度、未修改 active manifest、未更新 realtime。

### 11.1 探针边界

- 首次探针移除不支持的 `--no-window-cache` 后进入训练入口，但远端 `quant/factors/engineering.py` 缺少 `_price_row_eligibility_dates`，在 `build_monthly_panel` 处退出。
- 对比发现本地该文件包含函数，远端文件缺失；同步后两端 SHA256 均为：
  `9dc6d119fa69c1b4655c92294d6f9d80902dd44e6bf9de6659bf8c17f6a37ea1`。
- 第二次探针确认源码入口已推进，但已有 monthly panel 与 strict recipe 不一致，按设计 fail-closed：
  `strict panel recipe does not match cached monthly panel; use --rebuild`。
- 为避免删除共享缓存，使用全新隔离 panel `strict_probe_panel_20260812` 并加 `--rebuild` 重建。

### 11.2 真实重建结果

- strict panel 发现 4645 个代码，按 200 代码批次处理。
- raw price、valuation、financial_asof、events、targets 阶段均正常完成。
- prepared monthly panel 从 `2022-02` 生成到 `2026-08`，每月产物为 72 列，说明严格 panel 构建链已实际运行到 provenance 校验前。
- 最终在 `--require-selection-provenance` 处按预期 fail-closed：
  `factor_selection_lh1000_cont` 缺少 `candidate_count`、`candidate_pool_sha256`、`generator_code_sha256`、`label_col`、`predict_start`、`selected_count`、`selected_sha256`、`selection_name`、`train_end`、`valid_end` 等字段。
- 本次不是模型训练成功，也不是收益证据；因为 provenance 不完整，训练没有进入可审计候选选择和预测发布阶段。

### 11.3 PIT 依赖状态与结论

远端真实数据根目录 `/www/A/quant_data/full_a_2018_wide` 当前仍为：

- 存在：`trading_calendar.parquet`
- 缺失：`security_master.parquet`、`index_constituent_history.parquet`、`trading_status_history.parquet`

因此本轮确认了三层门禁行为：

1. 研究环境源码不一致会在 strict 入口直接失败，不能把环境漏同步误判为数据结论。
2. strict monthly panel 缓存 recipe 不一致会 fail-closed，不能复用旧口径 panel。
3. provenance 缺失会在训练前 fail-closed，不能伪造 legacy fixed-33 来源或继续生成不可审计预测。

当前下一步仍是补齐并审计 reference data 与 provenance-complete fixed control；生产调度继续关闭，realtime 继续使用旧模型。

### 11.4 隔离 rolling probe 的有效失败边界

- 首次 rolling probe 因隔离根的 `price` symlink 与 `ensure_dirs()` 冲突，在目录初始化阶段失败；没有把该结果当作 strict 逻辑结论。
- 改为 Docker bind mount 后，`strict_roll_probe_20260813_v2` 实际运行约 24.6 分钟，进入 strict rolling 和 execution-label 链，最终 fail-closed：
  `RuntimeError: tradable_ret NaN rate=0.261 (>0.20) — 检查 code/date join 键`。
- 产物检查：只留下 `panel_recipe.json` 和 feature summary；没有 selection manifest、purge summary 或预测文件，说明在标签完整性门禁前停止。
- 重新按首列代码解析 universe 后，确认 `mainboard_active_universe.txt` 包含 3193 个代码；状态制品包含 917 个代码，缺少 2276 个 universe code。
- `000852` index constituent history 中涉及 2839 个代码；与 mainboard universe 交集为 1835 个代码，但状态表仅覆盖其中 917 个，仍有 918 个 PIT universe code 缺少交易状态记录。
- 因此 26.1% 的 `tradable_ret` NaN 主要由状态 reference 覆盖不足造成，而不是可以通过放宽 NaN 阈值或伪造 join 键修复的程序问题。当前 `trading_status_history.parquet` 只适合其声明的 9 个 Ridge 窗口并集，不适合本轮更宽的 mainboard/PIT rolling probe。
- 结论：暂停继续重跑；必须先获得覆盖目标 PIT universe 的完整 trading-status history，或明确收窄研究 universe 并重新生成完整 provenance。生产调度继续关闭，realtime 继续旧模型。

### 11.5 状态数据生产者审计

- `quant/daily_update.py:560` 的 `refresh_pit_reference_data()` 只能刷新 security master 和 index constituent history，不负责 trading-status history。
- `quant/daily_update.py:585` 的 `refresh_trading_status_reference(codes)` 支持按代码批量调用 `datafeed.broker_history_stock_status` 并写入 reference warehouse，但当前未发现生产调度调用者自动传入目标 PIT universe。
- 远端容器的 API 符号存在：`broker_history_stock_status`、`broker_security_master`、`broker_index_constituent_history`；但 `datafeed.broker_available()` 当前为 `False`，因此不能在现有远端环境真实补齐缺失的 918 个 PIT code。
- 不能用现有 917-code 样本填充缺失代码，也不能把 broker 不可用时的空结果或旧快照宣称为完整 strict reference data。
- 下一步前置条件：获得可用 broker/真实导出文件，生成覆盖目标 PIT universe 的状态表，并记录 source、覆盖代码数、日期范围、SHA256 和 PIT scope；完成前不再启动 strict rolling 训练。

### 11.6 AmazingData 单连接补数启动

- 用户确认账号请求连接数上限为 1，订阅连接数单独计算；本轮所有历史请求严格串行，不使用并发请求 worker。
- 直接 `docker run` 未加载 `.env`，导致早先 `broker_available=False`；使用 compose 环境后 `available=True` 且 SDK 最终登录成功。登录过程曾出现连接数超限提示，随后成功，因此补数任务必须复用单进程、单 SDK 会话。
- compose 基础镜像中的 `quant.datafeed` 版本过旧，缺少 `broker_history_stock_status`；最终运行方式使用 compose 注入凭据，并挂载 `/www/A` 最新源码。
- 单代码 `000007` 内存探针通过：3097 行，日期 `2014-01-02` 至 `2026-08-12`，schema 完整，`code/date` 无重复、无空键。
- 精确缺失清单为 918 个代码，scope 为 `mainboard_active ∩ 000852 PIT history - existing status codes`，清单 SHA256：
  `79b99edb9030dfb4429596b182f4d7b371adf80a61b9318586e6dbbc2190d65d`。
- research-only 后台容器 `strict-status-backfill-20260813` 已启动：单进程、单 SDK 请求会话、每批 25 个代码，共 37 批；每批独立 parquet 落盘并支持续跑，最终才与原 917-code 制品合并。
- 首次任务在 10 批完成后 exit 139；根因是 SDK 单批临时 HDF5 写入容器根分区 `/tmp` 时 `No space left on device`，不是连接限制或数据质量问题。10 批 parquet 已保留，共约 4.6MB。
- 清理失败容器后，将 SDK `/tmp` bind mount 到 `/www/A/research_runs/strict_pit_probe_20260813/status_backfill/sdk_tmp`，续跑批次降为 5 个代码，避免单批临时文件超过根分区空间。
- 续跑容器 `strict-status-backfill-20260813-resume` 启动门禁通过：`running=true`，剩余 668 个代码、134 批，CPU 约 50.8%；宿主 `/www` 仍有约 85GB 可用，未恢复生产调度、未覆盖生产 reference、未更新 realtime。
- 一次长 SSH 监控因认证中断返回 255，但复核确认补数容器仍在运行；恢复短连接监控后已推进至 batch 78（累计 79 个分片），CPU 约 54.5%，`/www` 可用空间约 83GB，未出现第二次临时空间错误。
- 补数最终正常完成，容器 `strict-status-backfill-20260813-resume` exit=0。完整 artifact：`5,088,142` 行、`1,835` 个代码、2014-01-02 至 2026-08-12、`code/date` 重复 0、空键 0；artifact SHA256：
  `8c461c4d00dd515287a430723ff5d64f5720fb9b8b4ac2018e5172b0970849e2`。独立 pandas 复核确认目标 PIT code 覆盖 `1835/1835`。
- 完整状态表只安装到 research-only 根，原 917-code 文件改名留档为 `trading_status_history_917code_original.parquet`；生产根文件未修改。
- 使用完整状态表启动 `strict_roll_probe_20260813_v3`，但复核发现命令漏传 `--universe-file`，实际按默认全量代码构建；已在 panel batch 20 阶段主动停止，该运行不计入研究结果。
- 使用完整状态表和显式 `--universe-file mainboard_active_universe.txt` 启动 `strict_roll_probe_20260813_v4` 的尝试未留下容器或产物，未计入结果；随后确认默认 `--batch-size=200` 在 panel batch 21 附近触发 OOM（exit 137），尚未进入 strict 标签链。
- 已改为低内存参数启动有效 probe `strict_roll_probe_20260813_v5`：显式 mainboard universe、`--batch-size 25`，其余 strict/rolling/provenance/purge 参数不变；v5 已完成大部分 prepared panel，但因远端 `quant/model.py` 漏同步仍可能在模型接口处失败，已停止且不计结果。
- v4/v5 暴露同一研究环境 blocker：远端 `quant/model.py` SHA256 与本地不同，`train_lightgbm_ranker()` 缺少当前入口传入的 `model_threads` 参数。已同步并核对一致：`5cf7ae9089388ea2326236093b39c732d19ab028bcc69cc6564a2dbadb083b66`。
- 已启动 v6：显式 universe、`--batch-size 25`、完整 status、同步后的 model.py，继续验证 strict rolling/provenance/purge；v6 已确认仍因旧调用契约在 `model_threads` 处失败。
- 本地最小修复：`quant/model.py:train_lightgbm_ranker()` 增加 `model_threads`，优先映射为 LightGBM `n_jobs`；py_compile 和 ranker 定向测试通过。SSH 恢复后远端 model.py SHA256 与本地一致：`5cf7ae9089388ea2326236093b39c732d19ab028bcc69cc6564a2dbadb083b66`。
- v7 已启动并通过启动门禁：容器 `determined_mestorf`，`running=true`，CPU 约 141.3%，内存约 93MB；但运行时实际导入旧版 `model.py`，仍在窗口 28 报 `model_threads` 接口错误，不计入研究结果。
- 重新应用并验证 `quant/model.py` 最小兼容修复：ranker 签名包含 `model_threads`，本地/远端 SHA256 均为 `ba0ad9f18bd948c30b5ae4adef7b2f6a05f100f8f18b3dae50c8924aa0e7d992`；容器内 `quant.model.__file__` 确认为 `/app/quant/model.py`，签名核验通过。
- v8 已启动并通过启动门禁：容器 `/magical_snyder`，`running=true`，CPU 约 139.1%，内存约 303MB；日志已进入 panel 构建，使用正确 universe、完整 status、`batch-size=25` 和 strict/provenance/purge 参数。
- v8 正常完成，exit=0；panel、strict tradable label、rolling factor selection、模型调用和输出发布链均通过。
- v8 prediction artifact：`10,094` 行、`10,094` 非空、`17` 个预测日期；summary `total_return=0.0215843242`、`sharpe=3.9753196`、`win_rate=0.5625`。这些是 1 个 recent window、17 个预测日的链路验证，不是正式 alpha 或生产收益证据。
- v8 selection manifest：窗口 `28`，`candidate_count=51`，`selected_count=1`，字段包含 `candidate_pool_sha256`、`selected_sha256`、`generator_code_sha256`、`label_col`、边界和 `manifest_hash`；独立审计确认 manifest hash 唯一且字段完整。
- v8 purge 门禁：`purge_horizon=true`、`purge_windows_requested=1`、`purge_windows_skipped=0`、`purge_window_completion_ratio=1.0`。
- 当前结论：strict 研究链可运行性和 provenance/purge 机制通过；收益结论仍需正式多窗口、fixed control、统计检验和成本同口径验证。

### 11.7 正式 24-window rolling research-only 结果

- 复用 v8 prepared panel/cache，使用相同 strict recipe、完整 1835-code status、`000852.SH` PIT、`tradable-label`、`batch-size=25`，以 8 CPU 执行 `recent_windows=24` 正式 rolling；容器 exit=0。
- 产物：prediction、returns、holdings、summary、universe manifest、factor audit、factor selection manifest 均生成。
- 预测覆盖：311,520 行、311,520 非空，478 个预测日期，日期范围 2024-08-16 至 2026-08-07。
- selection provenance：24 个窗口，24 个唯一 manifest hash；每窗口 candidate_count 最少 51、selected_count 最少 1，字段包含 candidate pool、selected factors、generator code、label 和边界信息。
- purge 门禁：`purge_horizon=true`、requested=24、skipped=0、completion_ratio=1.0。
- 当前成本口径的收益诊断：returns 文件 477 条交易记录，gross return sum=+0.13792985，cost sum=0.17860000，net return sum=-0.04067015，最终 NAV=0.95407379，最大回撤=-0.14605154，日胜率=0.46960168，平均换手=0.18721174，平均持仓数=1.8763；summary total_return=-0.04592621、Sharpe=-0.26234476、win_rate=0.46960168。
- 结论：正式 24-window strict rolling 的链路、PIT、provenance、purge 均通过，但在当前成本口径下收益为负，不能视为 alpha，也不具备生产晋级条件。下一步只做同口径 fixed control、配对统计检验和成本敏感性核对，不继续扩大搜索空间。
- fixed control 规则已定义为 research-only `fixed_control_derived_v1`：正式 rolling 24 窗口中入选频率至少 12 次；实际仅 `intraday_range` 达标（24/24），`volatility_10` 和 `volatility_5` 各 9/24、`volatility_20` 为 5/24，未纳入。该 control 不冒充 legacy fixed-33。
- fixed provenance：candidate_count=51，selected_count=1，label=`tradable_ret_1d`，generator_code_sha256=`77b819dbf668a1becff62905528ec095aa95c67d59e7354d66d4008136add0f5`，candidate_pool_sha256=`d82343db1049d055a2acd26aa65b59638a4e2cc8627983feafcb73973c0339f2`，selected_sha256=`c4e8ab2bfaf8a34b32cc17cce03f815c2f77f29a2be13beb04d067dc95d461f1`。
- `fixed_control_derived_v1` 已安装到 research-only warehouse，并启动同口径 24-window fixed formal control；容器 `serene_mcclintock`，`running=true`，CPU 约 140.9%。
- fixed formal control 正常完成，exit=0；prediction `311,520` 行、`478` 个预测日期，returns `477` 条；summary `total_return=-0.14416501`、`sharpe=-1.19934474`、最大回撤约 `-0.21249311`、win rate `0.46750524`。
- rolling/fixed paired comparison 已完成，research-only、production_publication=false；共同交易日 `477`，prediction key hash 一致。
- 口径更正：原报告中的 `-0.00103251` 和 `-0.00103945` 分别是 rolling 相对 market equal-weight / PIT-industry equal-weight 的超额收益，不是 rolling−fixed；此前标成 rolling 相对 fixed 属于指标命名错误。
- rolling 与 fixed 的绝对收益直接配对：rolling 日均 `-0.00008526`，fixed 日均 `-0.00031749`，rolling−fixed=`+0.00023222`（约 `+2.32bp/day`）。新增 direct `rolling_vs_fixed` circular moving-block bootstrap（block=5、samples=2000、seed=42）CI95=`[-0.00020752, 0.00067449]`（约 `[-2.08bp, +6.74bp]`），单侧 p=`0.16091954`；方向为正但统计不显著。
- rolling 自身和 fixed control 自身均为负；rolling 相对 fixed 虽为正，但不改变“不支持 alpha、不生产晋级”的结论。停止继续扩大因子/模型搜索，后续只保留成本敏感性和生产 stale/freshness 门禁工作。

## 12. 收益搜索收尾与生产门禁状态（2026-08-13）

- 成本审计：已有真实远端成本敏感性报告，但不同来源存在 common-universe 差异；后续只能引用 causal common 口径，不能把非共同 universe 结果直接拼接比较。
- realtime：`active_quant_short_predictions.parquet` mtime 仍为 `2026-08-07 14:47:27 +0800`，realtime 代码已有 quote age 门禁，但未发现对 active prediction artifact 的独立 max-age/stale gate；因此 stale prediction 治理尚未完成。
- scheduler：已启动只读 final dry-run，完整 strict 参数、PIT、provenance、purge 和 `recent_windows=24` 均通过命令层验证后才可考虑后续门禁；dry-run 未执行发布、未恢复调度。
- 当前生产结论不变：不恢复 scheduler，不更新 realtime，不修改 active manifest。收益搜索停止扩张，后续仅处理成本口径审计、prediction freshness gate、scheduler dry-run 回执和本地/远端/GitHub 逐文件 hash 交付核对。
- 修正后的 scheduler dry-run exit=0：`quant_data_dir=/app/quant_data/full_a_2018_wide`，策略层识别 `recent_windows=24`；输出明确为 `[publish] dry-run: incumbent short would refresh; swing champion would remain unchanged`，没有真实发布或调度恢复。
- 收尾结论：strict 数据、rolling/fixed provenance、purge、正式 24-window 研究和 scheduler 命令层验证均完成；收益统计门未通过，realtime prediction freshness gate 尚未完成，生产晋级门保持关闭。

### 13. 新方向：为什么正向但不显著，以及成本敏感性（2026-08-13）

- rolling−fixed 的直接点估计为约 `+2.32bp/day`，但 block-5 bootstrap CI95 为约 `[-2.08bp, +6.74bp]`、单侧 p=`0.16091954`；方向为正只说明样本点估计偏正，不代表真实总体差异已确定为正。
- 不显著的主要原因：效果相对日收益噪声较小；477 个共同交易日存在时间相关，block bootstrap 的有效独立样本少于 477；差异方差较大，当前 CI 宽度显著大于 2.32bp 点估计。复核估算 MDE 约 28bp，当前实验对小于该量级的优势没有足够检验能力。
- 同一批正式 24-window rolling/fixed returns 的成本敏感性重算，不重新训练、不改变 PIT/label/provenance：
  - rolling：7bp=`+1.5811bp/day`、Sharpe=`0.4869`、net sum=`+0.075420`；10bp=`+1.0195bp/day`、Sharpe=`0.3139`、net sum=`+0.048630`；20bp=`-0.8526bp/day`、Sharpe=`-0.2623`、net sum=`-0.040670`。
  - fixed：7bp=`-0.5967bp/day`、Sharpe=`-0.2254`；10bp=`-1.1916bp/day`、Sharpe=`-0.4502`；20bp=`-3.1749bp/day`、Sharpe=`-1.1993`。
- rolling break-even roundtrip cost=`gross_sum / turnover_sum = 0.13792985 / 89.3 = 0.00154457`，约 `15.45bp`；只有总 roundtrip 成本低于该点估计，当前样本净收益才转正。
- `docker/scheduler_jobs.sh` 当前默认 20bp 的注释仍按“佣金双边万5 + 卖出印花税千1”描述，税率与当前制度不一致；默认值在真实账户成本核实前不能直接改为 7/10bp。
- 结论：成本是 rolling 净收益符号的关键变量，但 7/10bp 仍是敏感性场景，不是已核实的生产账户成本；成本降低也不会自动使 rolling−fixed 的统计门通过。下一步应核实真实账户佣金、最低佣金、印花税、过户费、成交结构和买卖方向，再用真实成本重算；不解除 `SCHEDULER_DISABLED=1`，不更新 realtime。

### 13.1 成本对 rolling−fixed 显著性的直接重算

- 使用正式 24-window research-only 的两份真实远端 returns：`strict_roll_formal_v1_bt_ridge_lightgbm_ranker_ensemble_returns.parquet` 和 `fixed_formal_v1_bt_ridge_lightgbm_ranker_ensemble_returns.parquet`；共同交易日 477，日期范围 `2024-08-16` 至 `2026-08-06`。不重训、不改变 PIT、label、provenance 或持仓，仅按 `gross_ret - turnover * cost_roundtrip` 重算。
- 0bp：rolling 日均 `+2.8916bp`，fixed `+0.7916bp`，直接差异 `+2.1000bp/day`；block-5 CI95 `[-2.2257,+6.5352]bp`，单侧 `p=0.1787`。
- 7bp：rolling `+1.5811bp`，fixed `-0.5967bp`，直接差异 `+2.1778bp/day`；CI95 `[-2.1448,+6.6224]bp`，`p=0.1684`。
- 10bp：rolling `+1.0195bp`，fixed `-1.1916bp`，直接差异 `+2.2111bp/day`；CI95 `[-2.1071,+6.6531]bp`，`p=0.1659`。
- rolling break-even `15.4457bp`：rolling 约 `0bp/day`，fixed `-2.2716bp/day`，直接差异 `+2.2716bp/day`；CI95 `[-2.0449,+6.7092]bp`，`p=0.1593`。
- 20bp：rolling `-0.8526bp`，fixed `-3.1749bp`，直接差异 `+2.3222bp/day`；CI95 `[-1.9894,+6.7543]bp`，`p=0.1541`。

解释：成本降低主要改善两套组合各自的绝对净收益，且由于 rolling 换手更高，降低成本会略微压低 rolling 相对 fixed 的差异；但该作用只有约 `0.22bp/day`（0bp 到 20bp），远小于 bootstrap 不确定性。因此“方向为正但不显著”的主因仍是相对优势小、日收益相关且波动大，而不是当前成本参数单独造成的统计失败。成本校准可以改变 rolling 是否净收益为正，却不能把 rolling−fixed 变成显著优势。

用户补充账户费率信息：佣金为万5，即单边佣金率 `0.0005`（双边佣金合计 `0.0010`）。该信息目前只确认佣金项目，不等同于完整账户 roundtrip 成本；印花税、过户费、最低佣金、成交方向和实际成交结构仍需单独确认。按“佣金万5 + 卖出印花税 0.05%”的制度口径，未计过户费和最低佣金时研究 roundtrip 成本约 `0.0020`（20bp），与当前研究默认相符；因此本次不修改成本默认值。

当前没有账单、成交回单或完整券商账户费率配置作为全部交易成本证据。复核文档中的 7/10bp 仍属于不适用于该账户佣金口径的敏感性场景，不能据此修改 `QUANT_BT_COST_ROUNDTRIP`、`REALTIME_PAPER_COST` 或任何生产硬编码。生产调度、realtime 旧模型和 active manifest 保持不变。

## 14. 收益研究第一轮：换手结构探针（2026-08-13）

### 14.1 实验边界

- 目标：在真实远端正式 24-window rolling/fixed prediction 上，研究 `rebalance_stride` 与 `hold_rank_buffer` 是否能降低换手并改善净收益；不扩大因子空间、不重训、不改生产配置。
- 输入：远端 `strict_roll_formal_v1...predictions.parquet` 与 `fixed_formal_v1...predictions.parquet`，均 311,520 行、478 个预测日期；使用完整 `trading_calendar.parquet`，避免按 prediction 日期切片造成调仓相位偏差。
- 参数：`stride={1,2,3}`、`buffer={0,5,10,20,50}`、`top_n=3`、gross exposure=`0.30`；成本同时计算 15bp 和当前默认 20bp。
- 组合数：9 个结构配置 × 2 个成本 × rolling/fixed，共 18 条正式结果；使用 8 CPU detached Docker research-only 任务，容器 exit=0，结果落盘：`/www/A/research_runs/turnover_structure_20260813/paired_candidates_v3.json`。
- 配对统计：共同交易日上使用仓库现有 circular moving-block bootstrap，block=5、samples=5000、seed=42；本轮 p 值为每组候选的裸值，未对 18 组搜索做 Holm 或 Reality Check，不能直接作晋级检验。

### 14.2 真实结果摘要

20bp 当前账户保守研究口径：

- baseline `stride=1, buffer=0`：rolling `-2.60bp/day`，fixed `-4.76bp/day`，rolling−fixed `+2.16bp/day`，CI95 `[-4.37,+8.82]bp`，p=`0.2617`，共同日 477。
- `stride=1, buffer=50`：rolling `-1.53bp/day`，fixed `-6.65bp/day`，rolling−fixed `+5.11bp/day`，CI95 `[-2.09,+12.16]bp`，p=`0.0804`，共同日 477；rolling turnover `0.2277`，fixed `0.2648`。这是相对差异最接近显著的一组，但未通过多重比较，不能称收益确认。
- `stride=2, buffer=0/5/20`：rolling−fixed 分别约 `-2.63/-3.03/-1.08bp/day`，均未改善；说明当前样本中 stride=2 的信号损失抵消了降换手收益。
- `stride=3, buffer=0`：rolling `-0.61bp/day`，rolling−fixed `+1.51bp/day`，CI95 `[-10.01,+12.99]bp`，p=`0.3837`，共同日 159。
- `stride=3, buffer=10`：rolling `+0.81bp/day`，rolling−fixed `+3.58bp/day`，CI95 `[-9.02,+16.17]bp`，p=`0.2801`，共同日 159。
- `stride=3, buffer=20`：rolling `+1.53bp/day`，fixed `-2.77bp/day`，rolling−fixed `+4.29bp/day`，CI95 `[-8.38,+16.91]bp`，p=`0.2462`，共同日 159；这是 20bp 下 rolling 自身净收益最高的候选，但样本缩短、区间极宽。
- `stride=3, buffer=50`：rolling `-0.21bp/day`，rolling−fixed `+1.83bp/day`，CI95 `[-10.45,+13.93]bp`，p=`0.3767`；buffer 继续增大后毛收益下降，不能简单按降换手排序。

15bp 基础费率敏感性下方向相同，但 rolling 的绝对净收益整体上移约 1.4~1.5bp/day；最佳 rolling 组合为 `stride=3, buffer=20`，约 `+2.93bp/day`，仍未形成显著证据。

### 14.3 第一轮结论

1. `stride=2` 当前不值得推进：在严格真实样本上 rolling 相对 fixed 反而变差。
2. `stride=3` 有降低成本、提高 rolling 自身净收益的迹象，但只有 159 个调仓日，统计功效明显下降；不能直接改生产。
3. `buffer=50` 在 stride=1 下带来最高的 rolling−fixed 点估计 `+5.11bp/day`，同时换手下降，但 p=`0.0804` 是未校正裸值；在 18 组候选中做选择后更不能按 0.08 解读。
4. `buffer=20` 与 stride=3 的组合在当前样本下 rolling 净收益为正，但毛收益也发生变化，说明它不是纯成本记账改动；需要拆分毛收益、换手和持仓稳定性后再做确认实验。
5. 本轮不能得出“stride=3”或“buffer=20/50”是新生产参数；结果只支持下一轮收窄候选、扩大独立验证窗口，并补齐多重比较/搜索偏差记录。
6. 生产继续保持 `SCHEDULER_DISABLED=1`，realtime 继续旧模型，active manifest 不变。

### 14.4 同条件候选与自身基线配对

为回答“buffer=50 是否真的优于 baseline”，补做同模型自身基线比较；这与 rolling−fixed 是两个不同问题：

- rolling candidate=`buffer=50` vs rolling baseline=`buffer=0`；
- fixed candidate=`buffer=50` vs fixed baseline=`buffer=0`；
- 两者使用完全相同的 prediction、PIT、label、top_n=3、gross exposure=0.30、交易日期和成本，仅改变 hold-rank buffer；共同日均为 477；使用 circular moving-block bootstrap，block=5、samples=10000、seed=42。
- 结果文件：`/www/A/research_runs/turnover_structure_20260813/matched_buffer50_vs_baseline.json`；protocol=`same_predictions_same_dates_same_pit_label_topn_exposure_cost`。

rolling：

- 15bp：buffer=50 净 `-0.3940bp/day`，buffer=0 净 `-1.1700bp/day`，差异 `+0.7760bp/day`；毛收益 `3.0211` vs `3.1067bp/day`；换手 `0.2277` vs `0.2851`；CI95 `[-3.1100,+4.6428]bp`，p=`0.34617`。
- 20bp：buffer=50 净 `-1.5324bp/day`，buffer=0 净 `-2.5956bp/day`，差异 `+1.0632bp/day`；毛收益仍为 `3.0211` vs `3.1067bp/day`；换手仍为 `0.2277` vs `0.2851`；CI95 `[-2.8144,+4.9317]bp`，p=`0.29577`。

fixed：

- 15bp：buffer=50 净 `-5.3220bp/day`，buffer=0 净 `-3.2848bp/day`，差异 `-2.0373bp/day`；毛收益 `-1.3503` vs `+1.1366bp/day`；换手 `0.2648` vs `0.2948`；CI95 `[-6.6164,+2.4831]bp`，p=`0.81712`。
- 20bp：buffer=50 净 `-6.6459bp/day`，buffer=0 净 `-4.7586bp/day`，差异 `-1.8874bp/day`；毛收益仍为 `-1.3503` vs `+1.1366bp/day`；换手仍为 `0.2648` vs `0.2948`；CI95 `[-6.4655,+2.6354]bp`，p=`0.79932`。

结论：同条件比较是必要且有意义的。rolling 中 buffer=50 的净改善主要来自换手下降，约 `0.78~1.06bp/day`，但没有统计显著性；fixed 中同一 buffer 反而显著方向恶化，说明 buffer=50 不是普适的组合改进，不能仅凭 rolling 点估计改生产。下一轮若继续，应只验证 rolling 自身的 buffer 候选，并预先固定候选集合和独立验证区间，避免从多组结果中挑选最优点估计。

## 15. 分钟特征验证前置审计（2026-08-13）

### 15.1 统计口径修复

- 分钟特征链确认使用 `features.py` 的 `13:55` cutoff，买入标签在 `14:50`，时间方向满足信号先于执行。
- 现有分钟特征 prepared、PIT/eligibility、adaptive labels、四折 walk-forward 和旧版 `daily_minute_enhancement_v1` 真实产物均存在，说明该方向值得做 research-only 验证。
- 发现 `intraday_1400/pipeline.py` 的 `_select_features()` 和 `_select_minute_features_grouped()` 原先把所有股票/日期拼成一个池化 correlation，日期股票数不均衡时会改变日期权重，也不能解释为逐日 IC 选择。
- 已改为每个日期内计算 Spearman IC，再跨日期等权取绝对均值；默认至少 5 个有效股票、20 个有效日期。新增回归测试构造不均衡日期样本，定向测试和完整测试通过。
- 为避免非价格 prepared 来源按 mtime 自动漂移，新增 `INTRADAY_1400_NONPRICE_PREPARED_DIR` 显式固定入口；设置后缺月直接 fail-closed。用旧报告声明的 `factor_panel_mainboard_active_h1_parts/prepared_monthly` 验证，2023-01 provenance 签名与 `prepare_state.json` 完全闭合。

### 15.2 全量分钟增强 runner 阻塞

- 使用真实远端三份 adaptive labels、固定 daily prepared、固定 minute prepared 和 `fair_race_feature_screening.json`，启动四折 baseline + all-minute/family candidate research-only 任务。
- 首次任务在 provenance 源漂移处按预期 fail-closed，未计收益结果；显式固定非价格源后 provenance 通过。
- 修复 candidate/fold 边界释放 `predictions/records/metrics/fold_panel/models` 并调用 `gc.collect()`，完整测试仍通过。
- 但全量 runner 两次均在内存峰值退出 `exit=137`：第一次 `model_threads=8` 约 12GiB 后退出，第二次 `model_threads=2` 约 14GiB 后退出；没有生成 report、execution_records 或 daily_returns，不能把该失败解释为分钟特征无效。
- 根因是当前入口在四折中保留完整 panel、所有 candidate records、模型/预测中间对象及最终汇总，释放局部对象仍不足；必须改为按 fold/候选分批持久化的 research-only runner，单次只保留一个 fold、一个 candidate 的模型对象，再合并逐折结果。

### 15.3 当前结论

- 分钟特征方向：**值得验证，当前没有收益结论**。

### 16. 2026-08-13 最新复核建议的执行状态

- 最新复核建议将下一步分成两个 research-only 前置：
  1. 在严格同条件下补 `hold_rank_buffer` 敏感性探针，候选值域为 `{0, 2, 5, 10, 20, 50}`；
  2. 构造首个训练窗口选出的 30 因子冻结 fixed control，重新测量 rolling vs fixed，避免当前 `fixed_control_derived_v1` 退化为 1 因子且含全样本后视。
- 对远端真实输入做只读核验后，当前两项均不能直接执行：
  - `active_quant_short_predictions.parquet` 缺少严格研究所需的 `buyable_close`、`tradable_ret_1d`；
  - `fair_race/window_*` prediction artifact 只有 `code/date/score`；
  - 当前 prepared 日线文件只有 `target_ret_1d`，不能把它与缺少资格列的预测直接拼接后宣称 strict execution；
  - 已有 rolling prediction 文件也没有完整 strict label/buyability/provenance 侧车。
- 因此本轮没有生成 S16 或冻结 30 因子基线结果，没有修改生产参数，也没有把旧的非严格 buffer 报告升级为严格证据。
- 正确下一步是先生成或复用带完整 `tradable_ret_1d`、`buyable_close`、同一 `(code,date)` key、同一 PIT/label provenance 的 research artifact；完成后再执行两项对照。
- 已确认的收益前置条件：13:55 causal cutoff、固定非价格来源、逐日 IC 筛选、相同 folds/labels/eligibility/execution 口径。
- 当前阻塞是实验 runner 的内存工程问题，不是分钟特征数据质量或时间泄漏证据。
- 在分批 runner 完成并通过真实四折配对前，不说分钟特征有效/无效，不启动生产，不更新 realtime。

## 17. 2026-08-14 阶段结论更新与交付状态

### 17.1 公平 rolling/frozen 对照结论

- 已构造首个训练窗口冻结的 30 因子 control：`frozen_first_window_top30_v1`。rolling 与 frozen 两侧使用完全相同的 30 因子数、PIT 数据、`tradable_ret_1d` 标签、`buyable_close`、预测 `(code,date)` key、组合规则和成本口径。
- 两侧预测均为 `229,524` 行，严格 key、标签和买入资格列一致；直接配对覆盖 `340` 个共同收益日期。
- rolling 日均净收益约 `11.0967bp/day`，frozen 日均净收益约 `16.9530bp/day`，rolling−frozen 约 `-5.8564bp/day`。
- circular moving-block bootstrap 结果：block 3/5/10/20 的 95% CI 均跨零，原始单侧 p 值约 `0.92~0.93`；Holm 校正后均为 `1.0`。
- 结论：rolling selection 在这组首窗口冻结 30 因子公平对照中没有显示优势。点估计偏负，但 CI 跨零，不能据此宣称 rolling 一定有害；同时不能再使用此前不公平基线得到的 `+2.32bp/day` 作为 rolling-selection 的确认结果。
- 不推广 rolling 或 frozen；不扩大因子搜索，不把本轮结果写成 confirmed alpha，不进入生产晋级。

### 17.2 预测 freshness 与 scheduler 门禁

- realtime 新增基于预测文件内部最新 `date` 的 fail-closed freshness 检查，默认允许最多落后 `3` 个日历日，可由 `REALTIME_PREDICTION_MAX_AGE_DAYS` 配置；预测文件不存在、不可读、无有效日期、日期过旧或未来日期均拒绝继续。
- freshness 检查同时覆盖 expected-return/reference 加载和 realtime engine 启动，避免旧制品通过 watchlist fallback 或 reference ranking 继续生效。
- scheduler dry-run 新增只读 source freshness 校验；既有 source 会被验证，缺失 source 明确标记为未验证，不执行复制、不写 active manifest。
- 远端真实候选制品 dry-run 已通过：短线和波段 source 均与行情最新日 `2026-08-07` 对齐；active short/swing/legacy prediction 及 `active_quant_model.json` 前后 SHA256 未变化。
- 远端验证通过：patched modules 编译、freshness 两个新增 unittest、realtime self-test；完整测试发现流程仍受远端已有代码/测试版本不一致和隔离网络依赖影响，失败项与本次 freshness 逻辑无关。

### 17.3 当前生产结论

- `SCHEDULER_DISABLED=1`、`RUN_ON_START=0` 保持不变；scheduler、realtime 旧模型、active manifest 均未恢复或替换。
- 当前阶段结论是：**严格研究链和安全门禁已交付，rolling 优势未被确认，生产晋级门继续关闭。**
- 后续只处理完整测试环境一致性、分钟特征分批 runner 和预注册独立验证；在统计门、完整 provenance、freshness/生产链全部满足前，不修改生产参数。

### 17.4 GitHub 交付范围

- 本次仅提交本调研记录和 freshness/scheduler 安全门相关源码及定向测试；其他远端研究产物、未相关工作树改动和未跟踪文件不纳入本次提交。

## 18. 2026-08-14 分钟特征验证启动与当前阻塞

### 18.1 已执行的依赖修复

- 远端冻结 screening report 的 `2025-08-06` 窗口只写入 40 个 minute 特征，其中 `speed=0`；这不是 speed 特征不存在，而是旧 screening 制品在 family 约束前已经把 speed 候选排除。远端历史 family validation 曾有 5 个 speed 特征，但不直接复用其收益结论。
- enhancement runner 已改为从已验证 `feature_manifest.json` 读取全部 minute source，在同一冻结 `train_end` 重新按六个 family 配额筛选；缺少注册 family 仍 fail-closed，不用空 family 伪造候选。
- prepared provenance 已用固定 `factor_panel_mainboard_active_h1_parts/prepared_monthly` 非价格来源 research-only 重建 44 个月：`2023-01` 至 `2026-08`，约 `2,725,429` 行，prepare exit=0；全量 `_prepared_provenance` 校验通过。
- runner 已增加 fold-scoped checkpoint、candidate-level feature reload、label-key loader filter 和确定性训练行数 cap；本地 `DailyMinuteEnhancementTest` 13/13 通过，远端 AST 校验通过。

### 18.2 真实远端执行结果

- 使用 3 份 adaptive labels、固定 prepared provenance、完整四折和 research-only 输出目录启动多次隔离任务。
- provenance 未闭合的首次运行按预期在输入门禁失败，不计收益结果。
- 修复 loader 和内存生命周期后，真实模型训练仍在双模型内部矩阵阶段达到约 `13.5~14.1GiB`，容器最终 `exit=137`；没有生成完整 checkpoint、report、execution_records 或 daily_returns。
- 300k、100k 训练 cap 已纳入 protocol 试验并验证过，但在当前模型实现中仍不足以保证宿主内存安全；未将任何部分运行或降采样中间结果解释为收益证据。

### 18.3 当前结论和下一步

- 分钟特征目前仍是：**前置数据和 family 筛选链已推进，收益验证未完成，没有分钟特征有效/无效结论。**
- 下一步不能继续盲目降低样本 cap；应把每个候选的 ridge/LightGBM 训练放入独立子进程或进一步拆分模型矩阵生命周期，在单候选内存预算可验证后再恢复四折。
- 任何重新采样上限必须预先写入 protocol，并由所有候选共用；在此之前不生成生产建议、不更新 realtime、不恢复 scheduler、不修改 active manifest。

## 19. 2026-08-14 分钟 runner 内存闸门与当前结论

### 19.1 已完成的工程改动

- runner 新增 research-only `--only-fold`、`--only-candidate`、`--gate-only`，默认完整四折入口行为不变；gate 模式只用于单折单候选工程验证，写入 checkpoint 和 `gate_report.json` 后退出。
- `_fit_daily_head()` 已改为面板先落盘，父进程释放面板后，Ridge 与 LightGBM 分别由独立 subprocess 读取同一 model panel 并输出独立 prediction parquet/metrics JSON；父进程校验 artifact SHA256 和 schema 后合并。
- 新增 research-only `--max-train-rows`；正式 `MODEL_RECIPE.max_train_rows=100000` 不变。gate-only 模式可显式限制训练行数，但任何降采样结果均不作为正式收益证据。
- gate-only 模式可显式限制 screening panel 行数；该限制只用于内存闸门，不改变正式 causal screening 默认行为。
- 本地和同步远端快照的分钟定向回归均为 `13/13`，新增 freshness 定向测试为 `5/5`；同步快照 hash 已逐文件核对。

### 19.2 真实远端执行结果

- 原始完整 runner、单折单候选同进程 runner、subprocess runner（`max_train_rows=100000`）和 screening/训练均为 `exit=137`；容器 `OOMKilled=true`，没有生成 checkpoint、prediction、execution_records、daily_returns 或 report。
- subprocess 版的内核记录显示 worker 单进程峰值约 `11.56GiB anon-rss`，容器 `12GiB` 限制被触发；把训练 cap 降到 `20000` 后峰值仍约 `11.57GiB`，说明主要峰值不由训练行数单独决定，screening panel、prepared panel 或模型特征矩阵仍在产生大内存峰值。
- 已解除研究容器的 memory cgroup 限制，保留 `8 CPU` 和无网络；当前无内存上限任务仍在运行中，宿主机 available 约 `12GiB`，尚无最终 artifact，不能提前解读收益。
- 之前 provenance mismatch、source mount 缺失和环境变量不一致均已被定位并修正；纯 `_prepared_provenance` probe 已通过 `44` 个月。后续任务使用固定 `SNAPSHOT_DIR`、`QUANT_DATA_DIR` 和 `INTRADAY_1400_NONPRICE_PREPARED_DIR`。

### 19.3 当前严格结论

- **分钟特征仍没有收益结论**：当前只有数据、family screening、fold 定义和工程闸门验证；没有完整 OOS returns artifact，不能说分钟特征有效或无效。
- **20k gate 不是收益实验**：它改变了 training/screening 样本上限，只能验证内存和 artifact 生命周期，不能与正式 100k recipe 或完整四折结果混合。
- **当前主要问题是内存生命周期而不是 alpha 方向**：需要继续核对 screening panel 分批加载、candidate feature projection、worker 单模型矩阵和磁盘中间制品；不能继续重复同一大面板训练。
- `SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型和 active manifest 保持不变；所有分钟产物均 research-only、不可发布、需人工复核。

### 19.4 请 Opus 重点复核

1. `_model_worker()` 的子进程边界、输入 panel 落盘、prediction/metrics hash 和异常传播是否完整；
2. `--max-train-rows`、gate-only 和 screening cap 是否可能被误用于正式收益协议；
3. `load_joined_prepared(..., max_rows=...)` 的 gate sampling 是否破坏 screening 可比性；
4. 正式四折是否必须在固定 `100000` recipe 下重新运行，且不能复用 gate artifact；
5. OOM 发生在 screening、panel load 还是模型矩阵阶段，下一步应如何做分批加载；
6. subprocess 产生的中间 artifact 是否需要纳入最终 provenance 和 protocol hash。

当前提交不包含任何分钟收益结论；只提交工程改动、定向测试和上述阻塞说明。

## 20. 2026-08-14 分钟 gate 加速复盘与 Opus5 待排查问题

### 20.1 本轮已落地的工程优化

- `intraday_1400/pipeline.py` 的每日 Spearman IC 计算已改为按日 rank 后使用分组充分统计量计算 Pearson correlation，保持原有等权日均值、最小样本数、最小日期数、常数列和缺失值语义不变。随机数据与旧实现差异约 `2.6e-18`；30,000 行、24 个特征的基准耗时由 `3.275s` 降至 `0.337s`，约 9.7 倍加速。
- causal gate 只筛选最终实际需要的 `daily_close_control` 和 `daily_close_plus_minute_control` 两个 variant，避免对无关诊断 variant 重复执行 CPU 密集筛选；默认全量 `screen_window_features(..., align_controls=True)` 路径保持不变。
- `screen_window_features()` 增加显式 `align_controls` 参数；研究 gate 在不完整 variant 子集上关闭全量 control 对齐，默认调用仍执行原有对齐逻辑。
- execution-universe panel 不再传入空 feature 列表；改为复用已验证的 base/minute feature projection，避免真实数据路径把三组特征过滤为空，同时只加载必要列。
- 所有改动均只位于 research runner/pipeline；未修改 active manifest、realtime 旧模型或 scheduler 生产开关。

### 20.2 真实远端 gate 结果

- v1 无内存限制 gate 长时间停留在 screening 计算，约 856 MiB RSS、单核满载，最终退出码 `137`；没有生成任何 checkpoint 或收益 artifact。内核记录对应此前受 12 GiB memcg 限制的进程，不能把该轮解释为收益结果。
- v2 在启动阶段因实际 mount 列表缺少 `/app/quant_data`，prepared provenance 失败；补齐显式 mount 后独立 probe 返回 `provenance_ok 44`。该轮不计研究结果。
- v4 在筛选阶段因不完整 variant 集合触发 `align_control_feature_selections()` 的隐含全量 key 假设，退出码 `1`、`OOMKilled=false`；这是接口兼容错误，不是 OOM。
- v5 已完成筛选，选出 50 个 matched base 和 40 个 minute features，但 execution panel 使用空 feature 列表，随后在 `merge_prepared_frames()` 报 `daily, asof, and minute feature groups must all be nonempty`，退出码 `1`、`OOMKilled=false`。
- v6 已进入 `wf1 / daily_asof_baseline` 候选 panel 构建，日志显示 `features=51` 和 `train_end=2025-10-28`，随后仍在 `merge_prepared_frames()` 的非空 feature group 检查退出码 `1`、`OOMKilled=false`；没有 checkpoint、prediction、metrics、daily_returns 或 report。
- v6 之后已暂停继续修复和重启，保留远端容器日志与隔离输出现场，等待 Opus5 对列投影/数据路径契约进行排查。本提交不包含新的 gate 结果。

### 20.3 测试与严格结论

- 本地完整 intraday 回归：`147/147` 通过；分钟增强定向测试：`13/13` 通过；IC 等价性、编译和 `git diff --check` 均通过。
- v4-v6 的失败均为 `exit=1` 代码/数据路径契约问题，不能与此前 `exit=137` 的内存失败混为一谈；当前没有有效分钟级 OOS return artifact。
- 当前严格结论仍是：**分钟特征收益未验证，不能判断有效或无效，也不能将任何 capped/gate-only 结果作为 alpha 证据。**
- 请 Opus5 优先核对：`load_joined_prepared()` 对空列表、缺列和按月 schema 投影的契约；`_build_panel()` 的 execution/candidate 两种调用是否应使用同一列投影；不完整 variant 子集与 control 对齐的接口边界；以及将筛选和 panel 加载改为可审计的 disk-first 分批方案。
- `SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型和 active manifest 保持不变；所有分钟研究输出继续 research-only、不可发布。

### 20.4 v7 修复与远端工程闸门通过（2026-08-14）

- v6 根因：候选 panel 对每个候选分别投影 `candidate_base`/`candidate_minute`，`daily_asof_baseline` 的 minute 列表为空，触发 `merge_prepared_frames()` 的非空 feature group 检查。
- 修复方式：候选 panel 改为对所有候选加载同一份冻结选择（`base_features` + `minute_features`），prepared join、universe 和 schema 因此完全一致；公平性边界仍由 `_project_model_panel()` 负责，只把候选自身特征喂给模型。新增定向测试 `test_model_panel_keeps_only_candidate_features` 固定该不变量。
- 本地验证：`DailyMinuteEnhancementTest` `14/14`、完整回归 `148/148`、compileall、`git diff --check` 全部通过。
- 远端 v7（`minute-gate-wf1-baseline-nomem-v7`）：`--network none`、无内存上限、8 CPU、六个显式只读/绑定 mount，参数 `--only-fold wf1 --only-candidate daily_asof_baseline --gate-only --max-train-rows 20000 --model-threads 1`。结果 `exited exit=0`、`OOMKilled=false`，`07:42:12Z → 07:44:23Z` 共 2m11s。
- 产物齐全：`gate_report.json`；`checkpoints/wf1__daily_asof_baseline.parquet/.json`（`records_sha256=17c78c09d4e605c02238207be1fc0e98f9e468c0ac7f01607a84f5c3ea124ecf`）；`model_workers/wf1/daily_asof_baseline/` 下 ridge 与 lightgbm_ranker 各自的 `predictions.parquet` + `metrics.json`，均 `120064` 行并带 `predictions_sha256`。
- 隔离性核对：容器 `/app` 由 `/www/A/research_runs/local_snapshot_20260814/source` 只读挂载，controller 源文件 SHA256 `75f2b4bb6d4966009c44316da3318946d85099ea328bec1c8d003fc4cb195661` 与本地一致；输出仅落在 `research_prepared_frozen_v1/gate_wf1_baseline_nomem_v7/`，未写入生产路径。
- 严格结论不变：该轮为 `max_train_rows=20000`、单折单候选、`gate_only` 的工程闸门。组合层 `mean_names=10` 而 `mean_filled_names=2.275`、`unbuyable=309`，被涨停/可买性严重截断，因此 `compound_return=-0.2747`、`sharpe=-9.13` 不构成收益证据；`ridge rank_ic=0.0187`、`lightgbm_ranker rank_ic=-0.0260` 同样只是截断样本下的工程数字。**分钟特征收益结论仍为空。**
- 后续仍未完成：disk-first 分批筛选/加载；`{0,5,10,20,50}` 严格 buffer 配对 block bootstrap；网格多重比较控制与选择 provenance；以及在冻结 `100000` recipe 下重新生成全部 artifact 的正式四折验证（不得复用 gate artifact）。

## 21. 正式四折验证完成与可执行性归零结论（2026-08-14）

### 21.1 运行事实

- 批次 `minute-formal-wf-100k-v3`：4 折 × 8 候选 = 32 对，每对独立 python 进程、`--gate-only` 不带 `--max-train-rows`（保留冻结的 `max_train_rows=100000` 配方，仅跳过单对无法满足的四折覆盖断言），最后一次不带 `--only-*` 的全量聚合运行执行真正的覆盖校验。
- 全部 32 对 `rc=0`，聚合 `rc=0`；`16:46:38 → 17:45:12`，共 58m34s，与优化后的估算一致。峰值内存 5.9 GiB / 15.63 GiB，`OOMKilled=false`。
- 产物：`checkpoints/` 32 组 `.parquet/.json` + `model_workers/{fold}/{candidate}/` 各自 ridge 与 lightgbm_ranker 的 predictions/metrics（合计 96 个 predictions parquet、160 个 json），`report.json`、`daily_returns.parquet`、`execution_records.parquet`。
- `report.json`：`walk_forward_oos_days=174`（40+40+47+47），通过覆盖与精确 OOS 索引断言；`decision={"status":"no_enhancement_passed","selected":"daily_asof_baseline","next_branch":"minute_feature_residualization"}`，`production_candidate=false`。
- 引入的唯一性能改动是 `_causal_screened_features_cached()`（筛选结果按完整 input/code hash 键缓存）。等价性硬证据：`wf1 / daily_plus_all_minute` 优化前后 `features_identical=True`(91)、`records_sha256` 相同、`metrics` 逐字段相同。

### 21.2 组合层结果不可解释：填充率归零

- `execution_records.parquet` 13920 行中 `unbuyable_1450=12698`、`filled_1450=1222`，整体成交率 `8.78%`。逐候选成交率：baseline `15.86%`、speed `13.22%`、context `8.97%`、risk `7.82%`、dependence `7.53%`、volume_vwap `6.32%`、path `5.40%`、all_minute `5.11%`。
- 关键对照：标签全域 `adaptive_entry_buyable` 在 OOS 期为 `97.76%`（183 日、日均 3227 只，最低 94.0%）。**因此 8.78% 不是数据缺陷，而是模型系统性地把 top-10 全部选到了全域仅 2.2% 的不可买（涨停/停牌）名单上。**
- 未成交在 `offline_race.py:245` 被记为 `net_return=0`，所以填充率越低、收益越被拉向 0。这正是 `as-is` 口径下 all_minute（`fill=0.38`）看起来比 baseline（`fill=1.38`）"更好"的全部原因。**该口径下的候选间收益不可比，不构成任何收益证据。**

### 21.3 可买性受限重算：所有候选的 OOS 排序信息为负

从已落地的 predictions 复算集成分数（`ridge_z*w + ranker_z*w`，与 `daily_minute_enhancement.py:505-519` 一致），把选股池限制在当日 `entry_buyable=True` 的名单内（14:50 下单时可观测，非未来信息），成交率恢复到 `10.00/10`，池化 174 个 OOS 日：

- 可买全域等权基准：日均 `-0.00247`，复利 `-38.8%`；扣除 `roundtrip_cost=0.002` 后大致对应毛收益接近零，即该期该标签本身已被成本吃掉。
- 每个候选的 top-10 都跑输可买全域。`top10 - 全域` 日均差与 circular moving-block bootstrap（block=5, B=2000）95% CI：baseline `-0.00413 [-0.00707, -0.00128]`、speed `-0.00446`、volume_vwap `-0.00519`、all_minute `-0.00383`、path `-0.00336` 五个候选 CI 不含 0；risk `-0.00152`、dependence `-0.00166`、context `-0.00240` CI 含 0，只是"负得较少"，不构成增益。
- 逐折 Spearman IC 在 **32 个 fold×candidate 单元中全部为负**（区间约 `-0.002 ~ -0.082`，wf4 最差）。bottom-10 同样跑输全域，说明不是单纯符号反转，而是打分同时选中了高成本/高波动的两端极值。

### 21.4 严格结论

- **分钟特征无增益**：8 个候选中没有任何一个在可比口径下优于 daily baseline，report 的 `no_enhancement_passed` 成立。
- **更上游的阻塞**：baseline 自身在 174 个 OOS 日、四折上具有一致为负的 OOS 排序信息，且 top-10 显著跑输可买全域。在此前提下继续做 `hold_rank_buffer {0,5,10,20,50}` / `rebalance_stride` 网格属于在负 IC 信号上搜索运气，不应执行。
- 需要先处理的是标签与选股口径本身：选股池是否必须限制在 14:50 可买名单内、`adaptive_realized_net_ret_t3` 在 `roundtrip_cost=0.002` 与 t+3 持有下是否还存在可捕获的毛边际、以及训练/评估目标错配（见 21.5）。
- 全部结论仅来自 research-only 隔离输出；`SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型与 active manifest 未变，未发布任何分钟研究产物。

### 21.5 根因：训练目标与评估目标错配

- 训练目标是日线面板的 `target_ret_1d`（`daily_minute_enhancement.py:401` 的 `label_col="daily_target_ret_1d"`）：原始 1 日收益，不含成本、不含可买性约束。
- 评估目标是 `adaptive_realized_net_ret_t3`：扣 `roundtrip_cost=0.002`、t+3 自适应退出，且**对不可买名单为 NaN**（OOS 窗口 13253 行不可买样本的该字段全部缺失，577250 行可买样本均值 `-0.002753`）。
- 2025-11 ~ 2025-12 日线面板与标签内连接 129069 行：不可买样本 `target_ret_1d` 均值 `+0.01907`（中位数 `+0.01441`），可买样本均值 `+0.000146`（中位数 `0.0`）——相差约 130 倍。
- 因此模型在训练目标上做对了事：涨停名单的原始 1 日收益最高，排序模型自然把它们放到最前；而这些正是 14:50 无法成交的名单。这同时解释了 8.78% 的成交率和对可执行净收益标签一致为负的 OOS IC。
- 结论：在把训练目标改为可执行、含成本、与评估口径一致的标签（并把选股池限制在下单时可买名单内）之前，任何分钟特征的增益检验都无法给出有意义的答案。

## 22. 协议 v2：可执行标签 + 下单时可买池，以及选择门禁缺陷（2026-08-15）

### 22.1 改动

- `offline_race.py`：`ExecutionConfig` 新增 `select_from_buyable_only`（默认 `False`，`direct_return_experiment` / `structural_combo_holdout` 等既有实验不受影响）。为 `True` 时在 `select_daily_top` 之前把候选池限制为当日 `entry_buyable=True`；买入可行性在 14:50 下单时刻可观测，属 PIT 合法。
- `daily_minute_enhancement.py`：`MODEL_RECIPE["target"]` 由 `daily_target_ret_1d` 改为 `adaptive_realized_net_ret_t3`，新增 `"selection_pool": "order_time_buyable"`；`PROTOCOL` 升为 `..._v2`（旧 checkpoint 无法复用）；`_project_model_panel` / `_model_worker` 改为读 `MODEL_RECIPE["target"]`；`_build_panel` 合并标签后断言训练目标存在；`_evaluate_candidate` 传 `select_from_buyable_only=True`。
- 依据：`quant/model.py` 的 `_xy` 丢弃 target 为 NaN 的行，而 `_predict_x` / `_rank_predict_x` 不丢弃——不可买样本（该标签为 NaN）自动从 train/valid 剔除，预测仍覆盖完整可交易全域，过滤只发生在下单时刻。注册折 purge 为 3 个交易日，已覆盖 t+3 标签。

### 22.2 v3 运行事实

- 首次启动 43s 即 fail-closed：`docker run` 漏了 `SNAPSHOT_DIR` 等 3 个环境变量，`industry_history` 退化为相对路径而不存在、被剔出月度签名，`_prepared_provenance` 在 `daily_minute_enhancement.py:661` 拒绝。补齐 env 后重跑，未产生任何脏 artifact。
- 补齐后：32 对全部成功 + 聚合成功，`exit=0`、`OOMKilled=false`，约 57 分钟，峰值 5.06 GiB / 15.63 GiB。产物 32 组 checkpoint、64 个 predictions parquet、`report.json` / `daily_returns.parquet` / `execution_records.parquet`。
- **成交率问题解决**：四折 × 八候选全部 `mean_filled_names=10.00`、`unbuyable=0`，`walk_forward_oos_days=174` 通过覆盖断言。候选间收益首次可比。

### 22.3 结果：可比之后，全体为负，分钟特征无显著增益

池化 174 个 OOS 日，circular moving-block bootstrap（block=5, B=20000）：

- 绝对日均收益全部为负：baseline `-0.00372`（复利 −49.8%，Sharpe −2.72）；最好的 `daily_plus_path` `-0.00246 [-0.00603, +0.00113]`（复利 −37.6%）；最差 `daily_plus_context` `-0.00498`（复利 −60.0%）。8 个候选中只有 `path` / `all_minute` 的 95% CI 含 0，其余全部显著为负。
- 配对差（候选 − baseline）经 Holm 校正（7 个候选）后 **全部不显著，`p_holm` 一律为 1.0000**：`path +0.00126 (p_raw=0.34)`、`volume_vwap +0.00064`、`speed +0.00055`、`all_minute +0.00029`、`dependence -0.00011`、`risk -0.00068`、`context -0.00126`，CI 无一例外跨越 0。

### 22.4 发现的选择门禁缺陷（已修）

- v3 的 `report.json` 给出 `status=enhancement_selected`、`selected=daily_plus_path`、`next_branch=forward_shadow`——但 `daily_plus_path` 在四折全部亏钱（−1.8% / −5.3% / −1.9% / −31.6%）。
- 根因：`select_enhancement` 的门禁完全是相对的（`mean_return > baseline`、3/4 折胜、`fill_rate ≥ 0.60`、回撤不显著变差），**没有绝对收益下限**，所以"亏得比 baseline 少"就能被判定为增益并进入 forward_shadow。
- 修复：新增 `mean_return > 0` 且 `compound_return > 0` 才可入选，并把 `mean_and_compound_return_must_be_positive` 写入 `protocol_payload().selection_gate`（进入 `protocol_hash`）；同时把完整 `protocol_payload` 写入 report 顶层，使训练目标与选股池在产物中可读而非仅经哈希背书。新增测试 `test_selection_rejects_a_candidate_that_only_loses_less_than_baseline`。本地 152/152 通过。
- 因 `controller` 参与 `input_hashes`，门禁修复使全部 checkpoint 失效，故以 `formal_wf_100k_v4` 全量重跑生成自洽产物；v3 的 `enhancement_selected` 结论作废。

### 22.5 严格结论

- 分钟特征在可比、可执行口径下对 daily h1 baseline **没有任何显著增益**（Holm 校正后 7/7 不显著）。
- 更重要的是，修正标签与选股池之后暴露出：该策略族在 174 个 OOS 日上以 `roundtrip_cost=0.002`、t+3 持有、每日 10 只等权的配置下，绝对净收益显著为负。这不是分钟特征的问题，而是整个 14:00 选股框架在该成本与持有期下的问题。
- 因此不执行 `hold_rank_buffer {0,5,10,20,50}` / `rebalance_stride` 网格：在绝对收益显著为负、且候选间差异全部不显著的信号上做网格搜索只会产出选择偏差。
- 仍为 research-only：`SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型与 active manifest 未变，未发布任何分钟研究产物；`production_candidate=false`、`human_approval_required=true`。

### 22.6 v4 复跑确认（门禁修复后的自洽产物）

- `formal_wf_100k_v4`：32 对 + 聚合全部成功，`exit=0`、`OOMKilled=false`，约 60 分钟。产物 32 组 checkpoint、64 个 predictions parquet。
- `protocol=intraday_1400_daily_minute_enhancement_v2`，`protocol_hash=50772343660c07f1...`；report 顶层现可直读 `model_recipe.target=adaptive_realized_net_ret_t3`、`selection_pool=order_time_buyable`、`selection_gate.mean_and_compound_return_must_be_positive=true`。
- `decision={"status":"no_enhancement_passed","selected":"daily_asof_baseline","next_branch":"minute_feature_residualization"}`，`production_candidate=false`、`human_approval_required=true`。门禁修复按预期把 v3 的假阳性翻转。
- 门禁修复的最小影响已证明：v3 与 v4 的 `folds`、`causal_feature_screening`、`eligible_universe_hashes_by_fold` 完全相同，`execution_records.parquet` 与 `daily_returns.parquet` 的 sha256 完全相同——只有 `decision` 变了。因此 22.3 的显著性结论（Holm 后 7/7 不显著）对 v4 同样成立，无需重算。
- 未提交的代码改动：候选面板修正、筛选缓存、可执行标签 + 下单时可买池（协议 v2）、选择门禁绝对收益下限、report 增写 `protocol_payload`，以及对应测试（本地 152/152 通过）。

## 23. 2026-08-15 现阶段结论与后续决策

### 23.1 总结判断

截至本记录，研究链已经完成了从工程可运行性到可比收益口径的关键修正，但没有得到任何可以支持生产启用的 alpha 证据。当前最重要的结论不是“分钟特征暂时没有找到显著增益”，而是：在修正训练目标、交易可行性和选择门禁之后，整个 `intraday_1400` 选股框架在现有成本与持有期设定下都表现为负收益，分钟特征没有改变这一判断。

- `formal_wf_100k_v4` 是当前唯一应作为正式结论依据的分钟批次：四折、八候选、共 174 个 OOS 日，32 个 fold×candidate 单元全部成功，覆盖断言通过，产物和协议 provenance 自洽。
- 在可比口径下，8 个候选的绝对净收益全部为负；daily baseline 日均收益 `-0.00372`、复利约 `-49.8%`、Sharpe `-2.72`。最好的 `daily_plus_path` 日均收益约 `-0.00246`，仍未形成正收益证据。
- 7 个分钟候选相对 baseline 的配对差经 Holm 校正后全部不显著，`p_holm` 均为 `1.0000`。因此不能把 `path` 的相对改善、或任何“亏得较少”的结果写成分钟 alpha。
- v3 曾错误地把 `daily_plus_path` 送入 `forward_shadow`，原因是选择门禁只有相对比较条件，没有绝对收益下限。该缺陷已由 v4 修复并通过回归测试；v3 的选择结论作废，v4 的 `no_enhancement_passed` 才是有效决策。

### 23.2 已确认的根因与协议状态

- 旧协议用原始 `daily_target_ret_1d` 训练，却用扣成本、t+3 自适应退出且对不可买样本为空的 `adaptive_realized_net_ret_t3` 评估。不可买样本的原始日收益均值约为可买样本的 130 倍，导致模型系统性把 14:50 无法成交的涨停等名单排在前面。
- 新协议 `intraday_1400_daily_minute_enhancement_v2` 已统一使用 `adaptive_realized_net_ret_t3` 作为训练目标，并在 14:50 的可买池内完成排名。该标签的 NaN 行由模型训练路径剔除，预测覆盖仍保持完整候选宇宙，注册折的 3 日 purge 足以覆盖 t+3 标签。
- v2 运行中四折 × 八候选全部 `mean_filled_names=10.00`、`unbuyable=0`，证明此前 `8.78%` 的成交率是选股口径造成的，而不是全域数据不可买。修正后的候选收益因此首次具备横向可比性。
- `protocol_payload`、`protocol_hash`、输入文件哈希和代码依赖哈希已进入报告/检查点 provenance；v3 与 v4 的折结果、筛选结果、可买宇宙和收益产物哈希一致，门禁修复只改变了最终 decision。这证明 v4 的翻转来自门禁修复，而不是数据或模型结果漂移。

### 23.3 工程与研究边界

已完成并已验证：

- 候选 panel 使用统一冻结的 prepared feature projection，模型公平性仍由 `_project_model_panel()` 限制到候选自身特征。
- causal screening 缓存保持输入与代码哈希约束，并已完成正式四折运行；IC 计算优化通过等价性测试。
- `offline_race.py` 的可买池筛选默认关闭，只有分钟协议显式开启，既有 `direct_return_experiment` 和 `structural_combo_holdout` 的行为不被静默改变。
- 本地完整回归为 `152/152` 通过；正式远端批次 `exit=0`、`OOMKilled=false`，研究输出位于隔离目录，未写入生产路径。

仍不能宣称已完成：

- `gate-only`、截断训练行数、单折单候选运行只能证明工程链可执行，不能作为收益证据；正式收益结论必须来自冻结 `100000` recipe 的完整四折产物。
- 旧审计中关于 pooled correlation、缺少 prepared 环境变量和分钟结果“因 OOM 尚未确定”的部分，已由当前代码或正式 v4 证据更新；但审计中的日线 rolling/fixed 24300 组合多重比较问题仍然独立有效，不能用分钟 v4 结果替代。
- 所有分钟研究仍是 research-only，`production_candidate=false`、`human_approval_required=true`；没有恢复 scheduler、realtime 旧模型或 active manifest。

### 23.4 后续工作优先级

1. **先决定 `intraday_1400` 分支去留。** 在绝对收益显著为负、baseline 排序信息也为负、分钟候选差异全部不显著的条件下，暂停继续做 `hold_rank_buffer`、`rebalance_stride` 或分钟家族细网格。除非先提出并验证新的标签、成本、持有期或交易机制假设，否则继续搜索只会增加选择偏差。
2. **单独处理日线 rolling/fixed 网格的多重比较控制。** 当前 short 网格有 24300 个组合，稳定性门禁没有足够的 family-wise multiplicity control；约 283 个纯噪声组合通过稳定性门禁的风险必须先被量化和修正。后续应把 bootstrap、Holm/等价的家族校正、完整选择 provenance 和独立确认样本纳入正式 gate，再解释任何 champion。
3. **修复生产安全链。** `assert_prediction_fresh` 在 predictions 文件缺失时存在静默通过路径，且有多个旁路调用、缺少单元测试。应先补 fail-closed 行为和覆盖所有调用路径的测试，再讨论恢复调度或模型发布。
4. **若重新启动分钟研究，先做协议级重设计。** 必须先说明可执行标签在 `roundtrip_cost=0.002` 和 t+3 持有下是否存在正的毛边际，并预注册新的假设、样本、成本、预测键、选股池、组合规则、统计检验和停止条件；不能从当前负收益结果直接挑选较优候选继续前推。

### 23.5 当前决策门

- 生产：**继续关闭**。不得恢复 `SCHEDULER_DISABLED=1` 之外的生产调度，不得改动 realtime 旧模型和 active manifest，不得发布分钟研究产物。
- `intraday_1400`：**当前不晋级、不进入 forward shadow，默认暂停新增参数搜索**；现有 v4 作为失败/否决性研究证据保留。
- 日线 rolling/fixed 网格：**不得从现有稳定性 gate 直接选 champion**，先完成多重比较控制与独立确认。
- 代码与文档：本阶段改动仍需在提交前进行最终 diff/provenance 复核；提交内容应包含协议 v2、门禁绝对收益下限、候选 panel 修复、筛选缓存、测试和本节总结，但不应包含任何生产启用动作。

## 24. 2026-08-15 逐项落地：freshness 复核、网格多重性控制、分支去留

### 24.1 `assert_prediction_fresh` 复核结论：审计条目已作废，无需修改

按 `复核意见_新.md` §12.4 ④ 的说法，该函数首个分支应是 `if not path.exists(): return None` 静默放行，且无任何单测。核对当前代码后该条不成立：

- `realtime/reference.py:174-176` 现在是 `raise RuntimeError(f"prediction artifact is missing: {path}")`；不可读、无有效日期、日期在未来、超过 `prediction_max_age_days` 四种情况分别抛出（`:180-196`）。函数没有任何返回 `None` 的路径。
- 决策链上的五个入口都在读取预测之前过闸：`realtime/engine.py:264`（`run()` 开头，早于订阅和参考数据构建）、`reference.py:209`（`_load_expected_return`）、`reference.py:412`（`_build_calibration`）、`reference.py:479`（`build`）、`realtime/watchlist.py:132`（`load_codes`）。
- 单测已存在且逐条覆盖上述入口：`realtime/test_reference.py:24-42` 的 5 个用例分别断言 `assert_prediction_fresh`、`_load_expected_return`、`_build_calibration`、`build`、`load_codes` 在预测文件缺失时全部抛 `prediction artifact is missing`。
- 审计列出的"六条绕过路径"里，仍未过闸的是 `realtime/diag.py:28`（诊断打印，其用途本身就是报告预测文件缺失或路径错配，不应 fail-closed）与 `realtime/weight_shadow.py:257/457`（读的是 `weight_shadow_predictions_file` 这条 research-only 影子路径，且该 job 在 `scheduler_jobs.sh` 白名单里被 SKIP）。两者都不产生生产交易决策，不构成绕过。

结论：**生产 freshness 链当前是 fail-closed 且有测试覆盖的，本轮不做改动。**审计 §12.4 ④ 应标记为已过期。

### 24.2 网格多重性控制已落地（本轮唯一的代码改动）

问题确认：`quant/watchlist_grid.py` 原文确实不含 `bootstrap` / `p_value` / `holm`。实际选择链是——`run_grid()` 在选择窗上评估全部组合（short 侧 24300），`_selection_summary()` 按 `selection_score` 排序，`scheduled_workflow.py:553` 取 `head(20)` 到 holdout 上逐个跑 `promotion_decision` + `stability_decision`。`stability_decision` 的判据此前全是阈值型（`min_significant_sharpe_gain=0.50`、`max_other_sharpe_decline=0.50`、`min_monthly_win_rate=2/3`），**没有任何 p 值，也没有任何家族校正**。

落地内容：

- `watchlist_grid.py` 新增 `_paired_block_bootstrap_p_value()`：对 non-overlap 抽样后的配对差做 circular moving-block bootstrap（默认 `samples=2000`、`block_length=5`、`seed=42`），给出单边 p 值。选用 block 重采样而非 i.i.d.，因为重叠持有期的组合收益有序列相关，i.i.d. 会低估均值方差、让噪声普遍显著。
- 新增 `holm_adjusted_p_values()`（跨 horizon 的 Holm step-down）与 `search_adjusted_p_value()`（按本次门禁实际检验的候选个数做 Bonferroni）。
- `stability_decision()` 新增 `search_family_size`、`max_search_adjusted_p_value=0.05`、`bootstrap_samples`、`bootstrap_block_length`、`bootstrap_seed` 参数；`passed` 现在额外要求 `multiplicity_passed`，即"Holm 跨 horizon 校正后再按搜索家族 Bonferroni 校正"的最小 p 值不超过 `0.05`。
- provenance：每个 horizon 的 `details` 增写 `paired_bootstrap_p_value`、`holm_p_value`、`search_adjusted_p_value`、`nonoverlap_observations`；决策顶层增写 `multiplicity_passed`、`search_family_size`、`max_search_adjusted_p_value`、`best_search_adjusted_p_value` 和 `paired_bootstrap`（含 `method` 与 `family_correction` 口径字符串）。
- `scheduled_workflow.py`：holdout 门禁传入 `search_family_size=min(20, len(selection_best))`，并在决策里增写 `selection_search_size`（上游搜索组合数，即 24300 这一量级）与 `holdout_family_size`。

口径说明（避免把校正做成表演）：holdout 检验的家族大小是**在 holdout 上真正被检验的候选数**（≤20），而不是 24300。24300 次比较发生在选择窗上，holdout 数据未被它们消耗；其后果是"冠军的期望 holdout 表现被选择偏差压低"，而不是 holdout p 值本身失效。因此把 24300 作为 `selection_search_size` 写进 provenance，让任何人读到冠军时都能看到它是大搜索的极值，而不是一次独立检验。

- 新增测试 4 个（`quant/test_model_expansion_experiment.py`）：`test_stability_gate_rejects_a_champion_without_family_wise_significance`（以 24300 为家族大小时，一个仅略微领先的候选 `search_adjusted_p_value` 打到 `1.0`、门禁拒绝）、`test_stability_gate_reports_family_corrected_p_values`、`test_search_adjusted_p_value_applies_bonferroni_over_the_search`、`test_holm_adjusted_p_values_are_monotone_step_down`。
- 验证：`quant.test_model_expansion_experiment` `156/156` 通过；仓库全量 `python3 -m unittest discover` `386/386` 通过；`compileall` 与 `git diff --check` 通过。
- 未处理的同类调用点：`model_expansion_experiment.py:560/831/1052/1286/1583` 与 `shadow_leg_evaluation.py:329` 仍用默认 `search_family_size=1`。它们都是 research-only 影子评估、`publishable=False`，但其 `champion_params` 也来自上游搜索；正确的家族大小需要各自的搜索规模，本轮不猜测，留作后续逐个接线。

### 24.5 六个影子门禁的家族大小已逐个接线（补充）

上一小节留的尾巴已做完。这六处的结构完全一致：先在选择窗上枚举一组配方（`selection` / `recipes`），按 `avg_sharpe` 取最优，再把这一个赢家拿到 holdout 上过 `promotion_decision` + `stability_decision`。因此家族大小就是"选择窗上被枚举过、且有资格成为赢家的配方数"，可以从代码里直接数出来，不需要猜：

- `model_expansion_experiment.py:560` `evaluate_confidence_gate_shadow`：`sum(1 for row in selection if row["gate_quantile"] is not None)`，即搜过的 gate 分位数个数。
- `:838` `evaluate_topk_residual_rerank_shadow`：`sum(1 for row in selection if float(row["weight"]) > 0)`，即非零 rerank 权重个数。
- `:1061` `evaluate_topk_residual_confidence_gate_shadow`：`len(recipes)`（feature × quantile 的全部组合）。
- `:1297` `evaluate_residual_state_gate_shadow`：`len(recipes)`（feature × quantile × high/low weight 的全部组合）。
- `:1597` `evaluate_market_regime_weights`：`len(candidate_weights) * max(len(selected), 1)`。这里每个 regime 各自挑一个权重，所以搜索空间是"权重 × regime"对，而不只是权重列表长度。
- `shadow_leg_evaluation.py:329` `evaluate_optional_leg`：`sum(1 for row in selection if float(row["weight"]) > 0)`。

测试：扩了两个既有用例而不是新造 fixture——`test_topk_residual_rerank_shadow_writes_no_production_artifact` 把 `weights` 改为 `(0.0, 0.05, 0.1)` 并断言 `report["stability_gate"]["search_family_size"] == 2`；`evaluate_optional_leg` 的用例把 `stability_decision` 的 mock 改为捕获调用，断言 `search_family_size == 3`（对应 `weights=(0.0, 0.03, 0.05, 0.10)` 里的三个非零权重）。全量 `386/386` 通过，`compileall` 通过。

至此 `stability_decision` 在仓库内已无遗留的"家族大小 = 1"调用点；唯一仍未验证的是真实 24300 组网格重跑。

### 24.3 `intraday_1400` 分支去留：暂停（不删除）

依据 v4 证据给出明确结论：**暂停该分支的参数搜索与增量特征试验，保留代码与产物作为否决性证据，不投入新的算力。**

理由：

- 失败点不在特征层。baseline 自身在 174 个 OOS 日上日均 `-0.00372`、复利 `-49.8%`、Sharpe `-2.72`，8 个候选全为负，7 个配对差经 Holm 校正后 `p_holm` 全为 `1.0000`。在框架级收益显著为负时，继续在特征或参数维度搜索只会产出选择偏差。
- 已排除三类"是不是实现错了"的解释：成交率归零（已修，`mean_filled_names=10.00`、`unbuyable=0`）、训练与评估目标错配（已修为 `adaptive_realized_net_ret_t3` + `order_time_buyable`）、选择门禁假阳性（已修并加绝对收益下限）。三处修完之后结论仍是负，说明问题在标签/成本/持有期这一层。
- 重启该分支的前置条件（缺一不可）：先独立证明在 `roundtrip_cost=0.002`、t+3 持有、每日 10 只等权配置下该标签存在正的毛边际；然后预注册假设、样本区间、成本、预测键、选股池、组合规则、统计检验与停止条件。不允许从当前负收益结果里挑"亏得较少"的候选继续前推。

### 24.4 本轮之后的状态

- 生产门禁未变：`SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型与 active manifest 未动，未发布任何研究产物。
- 网格门禁的收敛方向已改变：接线后纯噪声组合必须同时通过 block bootstrap 与家族校正，无法再仅靠阈值过门。该结论已在远端真实数据上实测，见 25 节。
- 仍未接线的 `hold_rank_buffer`（生产选股链零引用、搜索范围仅 {0,1,2}）保持原状，属独立工程，不在本轮范围。

## 25. 2026-08-15 远端实测：网格门禁多重性控制的真实效果

远端 HB18-8241（8 CPU / 15.63 GiB）。隔离目录 `/www/A/research_runs/grid_multiplicity_20260815/`，四个容器均 `--network none`、`quant_data` 只读挂载、输出只落 `out/`，全部 `exit=0`、`OOMKilled=false`。代码快照的四个文件 sha256 与本地工作树逐一相符（`watchlist_grid.py` = `d822aeed…`）。生产 `/www/A` 未被写入。

### 25.1 先修正一个事实：生产从未跑过 24300 组

生产上真实存在的 short 选择表 `monthly_factor_candidate_short_h1_short_1_3_selection_best.parquet`（2026-08-01）只有 **8100 行，且没有 `hold_rank_buffer` / `rebalance_stride` 两列**。当前代码的 `_base_combos` 是 `5×5×3×6×6×3×3 = 24300`（已实测确认），但生产跑过的是扩维之前的 8100 组。因此"24300 组无多重性控制"在代码层面成立，但不能写成"生产已经在 24300 组上选过冠军"。

另记：远端 `/www/A/quant/watchlist_grid.py` 比本地 HEAD 少一处 `_selection_summary` 的 gross 归一化加固（18 行差异）。本轮实测统一使用本地版本。

### 25.2 阶段 A / A2：原计划的测试台不合格（空结果，如实记录）

- 阶段 A：复现 short_1_3 的 holdout 门禁（窗口 2026-02-01 → 2026-08-01，非重叠观测 h1=119 / h2=60），对真实选择表 top-20 逐个过门。结果 20 个候选里只有 rank 19 过了 `promotion_decision`，而 `stability_decision` 在**修复前口径下也是 0 通过**，修复后同样 0 通过。原因是这些候选对现役冠军的月度胜率普遍为 `0.0`（rank 19 为 `0.167`），远低于 `0.40` 阈值；19 个候选的 `p_raw` 恰为 `1.0`，即观测均值差劣于几乎所有重采样。
- 阶段 A2：把范围扩到同一张表随机抽 300 组，仍是旧门禁 0 通过、新门禁 0 通过、翻转 0 组。
- 结论：`monthly_factor_candidate_short_h1` 这份 candidate 在该 holdout 上全面落后于现役 champion，本来就不可能晋级，所以**这两轮只能证明新门禁没有放过该拒的，不能证明它的鉴别力**。审计估算的"约 283 组纯噪声过门"在这份产物上没有复现。

### 25.3 阶段 C：换成纯参数搜索设计后，假阳性被完整量化

设计：baseline 与候选**共用同一份预测产物、同一套模型权重**（`active_quant_short_predictions.parquet` + 冠军的 lgbm/elastic/extra_trees 权重），只让选股参数变化。这样任何过门的候选按构造都是搜索噪声，正是审计那个测算要回答的量。从当前代码的 24300 组空间随机抽 400 组，holdout 2026-02-08 → 2026-08-08。

结果：

- **修复前门禁：400 组里 15 组通过，通过率 `3.75%`，按比例外推整网格约 `911` 组。审计估算的 ~283 组偏低约 3 倍。**
- **修复后门禁（family=20，生产实际口径）：400 组里 0 组通过。**
- 家族大小取整网格 24300 作参照：同样 0 组通过。
- 耗时 1600s / 400 组。

这 15 个假阳性在旧判据下"很好看"：非重叠 Sharpe 增益最高 `+1.854`，月度胜率 `0.429 ~ 0.571`，全部满足 `≥0.40`。但它们的 `p_raw` 在 `0.0175 ~ 0.554`，Holm 跨 horizon 后 `0.035 ~ 1.0`，再按 family=20 校正后 `0.6997 ~ 1.0`。最好的一例 Holm 后是 `0.035`，需要家族大小 ≤1.4 才能在 `α=0.05` 存活——**所以拒绝来自证据不足，而不是门禁被设成不可能通过**。

校准性佐证：`B=2000` 时可达最小 `p_raw` 是 `1/2001 ≈ 0.0005`，family=20 下 `p_adj` 下限约 `0.01`，仍低于 `0.05`，真实显著的候选可以通过（单测 `test_stability_gate_reports_family_corrected_p_values` 也在真实边际下通过）。但若按 24300 做 Bonferroni，`p_adj` 下限会是 `12.2`，门禁在数学上永远不可能通过——这反过来证明"家族大小必须取门禁上实际检验的候选数、而不是名义网格规模"这个口径选择是必要的，不是偷懒。

补充观察：15 个假阳性里有 4 组的 `p_raw` 完全相同（`[0.4203/0.4193/0.5112, 0.0175]` 这一簇），说明 `top_n=1` 下不同分位数参数选出的是同一组持仓。名义 24300 组里存在大量等价组合，**有效独立策略数远小于 24300**，这也是不应该按名义网格规模做 Bonferroni 的第二个理由。

### 25.4 阶段 B：24300 组网格的算力不是障碍

选择窗 2024-08-02 → 2026-02-01（364 个交易日、3008 只股票），按 buffer 分组抽样计时（每组合含 h1+h2）：

- `buffer=0`：`0.528s/组合`
- `buffer=1`：`0.590s/组合`
- `buffer=2`：`0.596s/组合`

`hold_rank_buffer>0` 会离开向量化路径、走 `_select_with_rank_hysteresis` 的逐日循环，但因为 `top_n ≤ 5`，额外代价只有约 12%。整网格外推：单核约 `3.86` 小时，6 worker 约 **`0.64` 小时**。

结论：把搜索范围从 `{0,1,2}` 扩到 `{0,5,10,20,50}` 只是线性放大到约 1 小时量级。**审计 §12.6 ⑦ 的"搜索范围不覆盖有效量级"是取值问题，不是算力问题**，没有理由不修。

### 25.5 本轮严格结论

- 多重性控制在真实数据上把该门禁的假阳性从约 `911/24300` 降到 `0/400` 抽样，且拒绝理由可逐条追溯到 `p_raw → holm_p → search_adjusted_p`。这是本轮唯一被实测确认的收益方向进展。
- 仍未做：用接线后的代码重跑完整 24300 组网格并产出新的选择表（算力已确认可行，约 1 小时）；`hold_rank_buffer` 接进生产选股链并扩范围。
- 不变：`SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型与 active manifest 未动；本节全部产物 research-only、不可发布。
- 产物与哈希：`out/stage_a_report.json`（`abc82b74…`）、`out/stage_a2_pass_rates.json`（`04456b7b…`）、`out/stage_b_feasibility.json`（`cfafae68…`）、`out/stage_c_false_positive.json`（`7c5df892…`），四个容器日志同目录留存。

## 26. 阶段 E：`hold_rank_buffer` 取值范围的实测证据

### 26.1 设置

`grid_gate_stage_e.py`，容器 `grid-gate-stage-e`（`--cpus 1`、`--network none`），全样本 `2024-02-02 → 2026-08-08`，预测产物 `active_quant_short_predictions.parquet`，冠军参数固定（`top_n=2`、`gross=0.3`、`ridge_q=0.7`、`pred_q=0.55`、`naive=0.1`、`stride=1`），只扫 `buffer ∈ {0,1,2,5,10,20,50}`。每个非零 buffer 对 `buffer=0` 做两件事：逐日收益序列的逐位比较，以及 `stability_decision(..., search_family_size=6)`。耗时 `271.9s`。产物 `out/stage_e_buffer_sweep.json`（`c987337e5f5dcbd7f1f085178ba4204e2e6c635d9973be4142deff073e3b6809`）。

### 26.2 结果

`buffer` 确实改变组合，**`{0,1,2}` 不是在重复算同一件事**：所有非零 buffer 对 `buffer=0` 都 `bitwise_identical=false`。h1 的 `mean_diff` 随 buffer 单调上升（`1.7e-05 → 4.8e-05 → 2.7e-04 → 3.7e-04 → 3.83e-04 → 4.56e-04`），换手率单调下降（`0.0865 → 0.0816 → 0.0751 → 0.0694 → 0.0615 → 0.0575 → 0.0496`），平均持仓单调上升（`0.600 → 0.704`）。h1 Sharpe `4.445 → 4.506 → 4.691 → 5.571 → 5.834 → 5.901 → 5.890`，h2 Sharpe `3.255 → … → 4.174 → 4.140`，都在 `buffer≈20` 见顶。

但**没有任何一个 buffer 取值能通过受控门禁**（family=6，与 `{0}∪` 6 个非零取值一致）：

- `buffer=1`：Holm `[0.766, 0.862]`，校正后 `[1.0, 1.0]`，h2 Sharpe 增益还是负的（`-0.216`）。
- `buffer=2`：Holm `[0.442, 0.488]`，校正后 `[1.0, 1.0]`，h2 增益 `-0.044`。
- `buffer=5`：Holm `[0.039, 0.173]`，校正后 `[0.234, 1.0]`。
- `buffer=10`：Holm `[0.015, 0.073]`，校正后 `[0.090, 0.438]`。
- `buffer=20`：Holm `[0.014, 0.025]`，校正后 `[0.084, 0.150]`，最好的一例。
- `buffer=50`：Holm `[0.033, 0.042]`，校正后 `[0.198, 0.255]`。

而且月度胜率全部低于 `2/3` 门槛（最高 `buffer=50` 的 `0.548/0.581`），所以即使不做多重性校正，这些候选也过不了稳定性判据。

### 26.3 由此得到的边界（取值决定见 27.3，已被阶段 D 推翻重写）

- **`{0,1,2}` 里的 `1` 和 `2` 在证据上等于噪声**（校正后 `p=1.0`、h2 增益为负），却让 2/3 的网格走 `_select_with_rank_hysteresis` 的逐日循环、多付约 12% 算力。
- **`buffer≈10~20` 是效应量最大的区间**，方向为"少换手、多持仓、Sharpe 更高"，与滞后逻辑的先验一致。但按本项目自己的规矩，`p_adj ∈ [0.084, 0.150]` 且月度胜率不达标 = **方向性观察，不是 alpha**，不能据此宣称扩范围会带来收益。
- 反面边界：`{0,5,10,20,50}` 会把网格推到 40500 组（+67%，约 1 小时），在当前证据下不值得——`5` 与 `10`、`20` 与 `50` 的序列差异已经很小，多两档只是多花算力买重复信息。
- **仍然没有解决的前置问题**：`buffer` 在生产发布链上根本不生效（`quant_signal._watchlist_frame_cached` 输出完整排名、无 `top_n` 截断、无前一日持仓状态、无实时可买掩码）。上面所有数字都只存在于回测口径。在发布链补齐"持仓状态 + `top_n` 截断 + 实时掩码"之前，改网格取值只影响研究结论，不影响对外输出。这一步涉及改对外输出形态，未动，等明确决定。

## 27. 阶段 D：接线后的完整 24300 组网格重跑

### 27.1 设置与产出

`grid_gate_stage_d.py`，容器 `grid-gate-stage-d`（`--cpus 7`、6 worker、`--network none`）。选择窗 `2024-08-02 → 2026-02-01`，holdout `2026-02-01 → 2026-08-01`，预测产物 `monthly_factor_candidate_short_h1_*_predictions.parquet`，`horizons=[1,2]`、`kind="short"`、`positive_only=True`，模型权重固定为冠军配置。

- **`grid_rows=48600`、`grid_combos=24300`、`grid_seconds=3925.6`**，`combos_by_buffer={0: 8100, 1: 8100, 2: 8100}`。阶段 B 的算力外推（6 worker 约 0.64h）被实测确认为 `1.09h`，同一量级。
- 产物：`out/stage_d_full_grid.json`（`d931e01e…`）、`out/stage_d_selection.parquet`（`d2ef6997…`）、`out/stage_d_selection_best.parquet`（`f554ddfd…`），容器日志 `out/grid-gate-stage-d.log`，退出码 0。

### 27.2 两个结果

**(1) 门禁侧：20 个候选全部被拒，新旧判据一致。** `promotion_decision` 放过 20/20，但 `legacy_promote=0/20`、`controlled_promote=0/20`。`raw_p ∈ [0.497, 0.698]`（远离显著），Holm 后全为 `1.0`，`best_search_adjusted_p_value` 全为 `1.0`，月度胜率 `0.333 ~ 0.500`。与阶段 A/A2 同因：这份候选模型在 holdout 上打不过 incumbent，旧判据本来就会拒，**所以阶段 D 同样不能用来展示多重性控制的鉴别力**（那个由阶段 C 承担）。它证明的是接线后的完整链路能端到端跑通并留下可追溯的 `raw_p → holm_p → search_adjusted_p` 证据。

**(2) 网格侧：`hold_rank_buffer` 不是一个可搜索的维度，而是一个单调旋钮。** 这是本阶段真正的发现：

- `top20_by_buffer={2: 20}`——top-20 **全部**是 `buffer=2`。
- 往下看仍然如此：top-100 是 `{2: 82, 1: 18}`，top-500 是 `{2: 422, 1: 78}`，top-1000 是 `{2: 776, 1: 224}`。**`buffer=0` 在前 1000 名里一次都没出现。**
- `selection_score` 按 buffer 分组的均值/中位数/最大值都单调上升：`0 → 5.492/5.862/7.367`，`1 → 5.563/6.004/7.577`，`2 → 5.684/6.157/7.856`。
- 结合阶段 E 在全样本上同样单调（Sharpe 一直升到 `buffer≈20`），结论是：**只要把某个 buffer 取值放进网格，选择窗就会几乎确定性地选中其中最大的那个**。搜索这个维度不产生信息，只产生算力开销。

### 27.3 取值决定（已落代码）

`_base_combos` 的 short 分支去掉 `for buffer in (0, 1, 2)`，固定 `hold_rank_buffer=0`，**短线网格从 24300 组回到 8100 组**（与 25.1 查到的生产实际产出的 8100 行选择表重新一致）：

```python
                                    # Searching hold_rank_buffer here is useless: the
                                    # selection score rises monotonically with the
                                    # buffer, so the grid just returns the largest value
                                    # offered while costing a multiple of the compute.
                                    # Buffer stays a research dimension, decided against
                                    # the holdout, not a searched parameter.
                                    "hold_rank_buffer": 0,
```

理由链：(a) 阶段 D 证明多档 buffer 在选择窗上等价于"取最大值"，3 倍算力换不到搜索信息；(b) 阶段 E 证明 buffer 的收益方向虽然一致，但在受控门禁下 `p_adj ∈ [0.084, 0.150]`、月度胜率最高只有 `0.581`（门槛 `2/3`），**没有任何取值有资格进生产**；(c) buffer 在发布链上还不生效。所以把它固定成 `0`（fail-closed），当作需要独立 holdout 证据才能改的决策项，而不是网格里的一档。

这同时否掉了 26.3 里"换成 `(0, 2, 10)` 零成本改进覆盖面"的想法——那个改法在阶段 D 的证据下只会让 `buffer=10` 确定性霸占 top-20，把一个未通过门禁的取值送进冠军位。

未动的部分（已由阶段 F 补齐，见第 28 节）：当时 swing 分支仍保留 `(0, 1, 2)`，因为单调性只在 short 上实测过。

同步改了单测 `test_base_grid_searches_stride_and_hold_buffer`，short 与 swing 断言都改为 `{0}`；`python3 -m unittest discover` 386 项通过。

### 27.4 本轮总账

- 唯一被实测确认的收益：多重性控制把该门禁的假阳性从约 `911/24300` 压到抽样 `0/400`，拒绝理由逐条可追溯（阶段 C）；代价经标定为低噪声下一档效应量、真实噪声下为零（阶段 G2）。
- 唯一被实测确认的成本削减：短线网格 24300 → 8100（阶段 D），swing 网格 2304 → 768（阶段 F），理由都是被证明无信息的维度。
- 三个 null result，明确记录为 null：阶段 A、A2、D 的候选模型在 holdout 上打不过 incumbent，不能用来证明门禁的鉴别力。
- 方向性但不构成 alpha：`buffer≈10~20` 的低换手高 Sharpe（阶段 E），受控 `p` 不显著且月度胜率不达标。
- 不变：`SCHEDULER_DISABLED=1`、`RUN_ON_START=0`、realtime 旧模型与 active manifest 未动；阶段 A–G2 全部产物 research-only、不可发布，未进入任何生产路径。
- 已知的固有边界（阶段 G2）：6 个月 holdout 只能识别年化 `25%~50%` 量级的边际，门禁拒绝 ≠ 无边际。
- 仍等决定：给发布链加"前一日持仓状态 + `top_n` 截断 + 实时可买掩码"才能让 `hold_rank_buffer` 真正生效。这会改对外输出形态，未动。

## 28. 阶段 F：swing 分支上的同一验证

### 28.1 设置

`grid_gate_stage_f.py`，容器 `grid-gate-stage-f`（`--cpus 7`、6 worker、`--network none`）。这个探针的存在理由是不把 short 的结论外推到 swing：27.3 固定 short 的 buffer 时，swing 侧没有任何实测证据。

`active_quant_swing_predictions.parquet`，全样本 `2020-02-03 → 2026-08-08`，`horizons=[7,10,15]`、`kind="swing"`、`positive_only=True`，走 `_base_combos` 自己的 swing buffer 档位 `(0,1,2)`。`grid_rows=6912`、`grid_combos=2304`、`grid_seconds=2544.3`，退出码 0。产物 `out/stage_f_swing_buffer.json`（`29eeea2a90feb80801dc0bc4d761c026d058d9188fceec604af6e1a934fcbf11`），日志 `out/grid-gate-stage-f.log`。

### 28.2 结果：swing 完整复现 short 的单调性

- `head_shares_by_buffer`：top-20 `{2: 20}`、top-100 `{2: 96, 1: 4}`、top-500 `{0: 188, 2: 185, 1: 127}`。**top-100 里 `buffer=0` 一个都没有**，与阶段 D 的 short 结果同型。
- 分组 `selection_score` 三个统计量全部单调上升：均值 `0.4285 → 0.5072 → 0.5849`，中位数 `0.4288 → 0.5062 → 0.5942`，最大值 `0.5624 → 0.6183 → 0.6752`，脚本判定 `mean_score_monotone_increasing=True`。
- 单调性在 swing 上比 short 更陡（均值相对增幅约 `+37%`，short 约 `+3.5%`），符合 7/10/15 日 horizon 下持仓滞后对换手成本更敏感的先验。

注意 top-500 那一档的分布看起来"混起来了"，但这不构成反例：越往下走分数差异越被其他维度盖过，而**晋升链只看 `head(20)`**，那里是 `buffer=2` 独占。

### 28.3 取值决定（已落代码）

swing 分支同样去掉 `for buffer in (0, 1, 2)`、固定 `hold_rank_buffer=0`：

```python
                        # Same monotonicity as the short branch: the selection score
                        # rises with the buffer, so the grid would only ever return
                        # the largest value offered.
                        "hold_rank_buffer": 0,
```

组合数：swing base `576 → 192`，叠上 `_template_combos` 的 3 档 `naive_weight` 后 **`2304 → 768`**。至此 27.3 遗留的分支不一致被证据消除，两个分支的处理方式一致且各有自己的实测依据。

单测 `test_base_grid_searches_stride_and_hold_buffer` 的 swing 断言改为 `{0}`；`python3 -m unittest discover` 386 项通过。

`hold_rank_buffer` 的定位由此固定为：**一个需要独立 holdout 证据、且需要发布链先具备持仓状态才有意义的决策项，不是网格维度。** 在阶段 E 的证据下（受控 `p` 不显著、月度胜率不达标），它当前的值就是 `0`。

## 29. 阶段 G/G2：受控门禁的检验力标定（阶段 C 的另一半）

阶段 C 只测了假阳性那一侧（纯搜索噪声 `0/400`）。一个把什么都拒掉的门禁也能拿到这个成绩，所以必须把另一侧测出来：**真的有边际时，它能不能过，需要多大的边际。**

### 29.1 阶段 G：常数边际是退化的正对照（记录为方法论失败）

`grid_gate_stage_g.py`，在冠军参数的真实 holdout 收益序列（`2026-02-08 → 2026-08-08`，h1 `118` 个观测、h2 `117` 个）上每天加一个常数边际，扫 `0 ~ 80bp`。

结果：`0bp` 拒，`2bp` 起全部通过，且所有非零边际的 `holm_p` 都是同一个值 `0.001`、`p_adj` 都是 `0.01999`。

原因是设计缺陷：**加常数使配对差分的方差为零**，bootstrap 的 `p` 直接掉到 `1/(B+1)` 地板，与边际大小无关。这个探针只重复确认了"门禁在数学上可通过"（family=20 下 `p_adj=0.02 < 0.05`，与 25.3 的解析论证一致），**对检验力零信息**。产物 `out/stage_g_gate_power.json`（`4a2402b0…`）保留，作为设计错误的记录。

### 29.2 阶段 G2：带真实离散度的检验力曲线

`grid_gate_stage_g2.py`（`dfffc8a8…`），把注入的边际改成 `edge + N(0, k·σ)`，`σ` 取该 horizon 基线自身的日收益标准差（实测 h1 `0.00412`、h2 `0.00767`），`k ∈ {0.5, 1.0}`，每个格点重抽 `20` 个种子，统计通过率。产物 `out/stage_g2_gate_power_curve.json`（`8ce00c6f…`）。

- **`0bp`（无边际）：旧门禁在 `k=0.5` 下通过率 `0.15`，受控门禁 `0.00`。** 这是阶段 C 结论在另一个数据生成过程下的独立复现——旧门禁的假阳性是真的，不是抽样巧合。
- **80% 检验力所需的日边际**：`k=0.5` 时旧门禁 `5bp`、受控门禁 `10bp`；`k=1.0` 时两者都是 `20bp`。
- 中位 `holm_p` 随边际单调下降（`k=0.5`：`0.818 → 0.006 → 0.001`）。

结论：**多重性控制的代价在低噪声下是一档效应量（`5bp → 10bp`），在真实量级噪声（`k=1.0`）下为零**，换来的是把 `0bp` 处的 `15%` 假阳性清成 `0%`。这个交换在本项目的语境下明显值得。

### 29.3 必须一起记住的边界

`10 ~ 20bp/日`换算成年化约 `25% ~ 50%`。也就是说，**在 6 个月、约 118 个观测的 holdout 上，这个门禁只能可靠地识别非常大的边际**；小而真实的改进会被判为不显著。这不是多重性控制引入的问题（旧门禁在 `k=1.0` 下同样要 `20bp`），而是 holdout 长度决定的固有分辨率上限。

由此得到两条不能混淆的推论：

- 门禁拒绝某个候选，**只说明"在这个样本长度下无法证明它有大边际"**，不等于它没有边际。这正是阶段 A/A2/D 那些 null result 的正确读法。
- 想识别更小的边际，唯一正当的手段是加长 holdout 或增加独立观测，**不是放宽 `α`、不是缩小家族大小、更不是取消校正**。

## 30. 阶段 H/I/J：顺着 29.3 那个边界往下查，两个假设被推翻

### 30.1 阶段 H：加长 holdout 是一个很差的杠杆

`grid_gate_stage_h.py`，沿用 G2 的带噪边际设计（`k=1.0`、20 种子），把扫的维度换成 holdout 长度。产物 `out/stage_h_holdout_length_power.json`（`65b7fe6a…`）。

80% 检验力所需日边际（受控门禁）：

- `3` 个月（h1 `64` 观测）：**即使 `20bp` 也达不到 80%**（通过率 `0.70`）。
- `6` 个月（`118`）：`20bp`。
- `9` 个月（`181`）：`20bp`（`10bp` 只有 `0.60`）。
- `12` 个月（`241`）：`20bp`（`10bp` `0.70`）。
- `18` 个月（`365`）：`10bp`。
- `24` 个月（`483`）：`10bp`。

零边际处的假阳性率：旧门禁 `3` 个月 `0.10`、`6` 个月 `0.05`，`9` 个月起归零；受控门禁全长度 `0.00`。

结论：**holdout 从 6 个月翻倍到 12 个月，可识别边际一点没变**（都是 `20bp`），要到 `18` 个月才降一档到 `10bp`，而 `5bp` 在 24 个月内始终达不到 80%。加长 holdout 要等量压缩选择窗，**换来的分辨率提升极其昂贵**，所以不改生产的 6 个月设置。唯一站得住的边界结论是：**3 个月 holdout 不可用**（受控门禁在任何测到的边际下都达不到 80% 检验力），生产现用的 6 个月已在可用区的下沿。

### 30.2 阶段 I：我关于 bootstrap 分辨率的假设是错的

假设：family=20 要求 `p_adj ≤ 0.05`，即 `holm_p ≤ 0.0025`，两 horizon 下 `raw_p ≤ 0.00125`；而 `B=2000` 能取到的 `raw_p` 只有 `k/2001`，即只有 `k ∈ {1,2}` 合格。判决阈值正好卡在 bootstrap 地板上，所以真实 `p` 略低于阈值的候选是因分辨率不足被拒。

实测（`grid_gate_stage_i.py`，12 个月窗、`B ∈ {2000, 10000, 40000}`）**推翻了这个假设**。产物 `out/stage_i_bootstrap_resolution.json`（`0bfe0303…`）：

- `10bp`：通过率 `0.70 → 0.75 → 0.70`，中位 `holm_p` 从地板 `0.001` 降到 `0.000125`——**`p` 的分辨率提高了 8 倍，通过率没动。**
- `5bp`：`0.05 → 0.10 → 0.10`。
- 单次门禁耗时 `0.099s → 0.206s → 0.725s`，`B=40000` 是 7 倍成本。

所以 `bootstrap_samples=2000` 保持不变，不改默认值。这条记录的价值是负面的：**7 倍算力买不到任何鉴别力**，而且它把真正的限制因素暴露了出来。

### 30.3 阶段 J：一次探针 bug，以及修正后的真实瓶颈

**先记录错误。** 这个探针第一版用 `decision.get("significant_horizons")` 取显著 horizon 数，而 `stability_decision` 返回的字段名是 `significant_improved_horizons`。取不到 → `int(None or 0) < 1` 恒真 → `sharpe_gain_below_0.50` 对每一次失败都记一票。第一版输出（`5bp: 37/40`、`10bp: 10/40`、`20bp: 2/40`）正好等于各档的失败总数，我据此得出"Sharpe 增益阈值是主约束"的结论，**那个结论是 bug 的产物，已作废**。发现方式是阶段 K：它报出 `rate_sharpe_gain_alone=0.0` 却同时有 `rate_legacy_gate=0.035`，而旧门禁通过必须要求显著 horizon ≥ 1，两者矛盾，于是回查字段名。

修正字段后重跑（`grid_gate_stage_j.py` / `out/stage_j_binding_criterion.json`，`ca0e3934…`），结论几乎相反：

- `5bp`（通过率 `0.075`）：`family_adjusted_p` 失败 `33`、`other_horizon_decline` `17`、`sharpe_gain_below_0.50` `14`、月度胜率 `4`。
- `10bp`（通过率 `0.75`）：`other_horizon_decline` `6`、`family_adjusted_p` `5`、月度胜率 `1`，Sharpe 增益阈值**一次都没成为障碍**。
- `20bp`（通过率 `0.95`）：只剩 `other_horizon_decline` `2`。

**真实情况：小边际时多重性校正是主要拦截者（`5bp` 处 33/37 次失败），边际变大后接棒的是 `max_other_sharpe_decline`（另一 horizon 不能退化），Sharpe 增益阈值只在小边际时排第三。** 29.2 里"多重性控制的代价"因此不需要下调——它确实在做主要的工作。29.3 的措辞也回到原样：可识别边际的上限主要由样本长度和多重性校正共同决定。

### 30.4 阶段 K：零边际下逐条判据的假阳性贡献

`grid_gate_stage_k.py`，12 个月窗、`k=1.0` 纯噪声候选、`200` 个种子（构造上任何通过都是假阳性）。产物 `out/stage_k_null_criteria.json`（`37535b3f…`）：

- 单独看 `Sharpe 增益 ≥ 0.50`：`11%` 的纯噪声候选满足。
- 单独看月度胜率 `≥ 0.40`：`49%`——**这条判据几乎不筛任何东西**。
- 单独看"其他 horizon 不退化"：`5%`。
- `Sharpe 增益 + 其他 horizon 稳定`：`3.5%`；再叠月度胜率仍是 `3.5%`（进一步确认月度胜率不构成约束）。
- 旧门禁整体：`3.5%`。**与阶段 C 在完全不同的数据生成过程下测到的 `3.75%` 几乎一致，是一次独立复现。**
- 受控门禁：`0.0%`。

零边际下 Sharpe 增益的分布：中位 `-0.556`、`p90 = 0.524`、`p95 = 0.843`、`p99 = 1.091`、最大 `1.783`。

两条直接可用的结论：

- `min_significant_sharpe_gain=0.50` 恰好坐在零假设分布的 `p90` 上，**它本身只相当于一个 `α=0.10` 的单点检验**，远不足以承担把控作用。这解释了为什么旧门禁会漏 `3.5%`。
- **纯噪声就能造出 `+1.78` 的非重叠 Sharpe 增益。** 所以任何"Sharpe 增益 +1.x 很好看"的论证（包括阶段 C 那 15 个假阳性里最高的 `+1.854`、阶段 E `buffer=20` 的 `+1.456`）单独都不构成证据。这是本轮最该记住的一条经验数字。

## 31. 阶段 L：多重性控制内部三个参数选择的审计

前面所有阶段测的都是这套机制的**输出**（假阳性率、检验力）。它内部有三个当初直接假设、从未查证的选择：bootstrap 的 `p` 是否校准、`block_length=5` 是否有依据、Bonferroni 除以名义 20 是否合理。`grid_gate_stage_l.py`，12 个月窗，产物 `out/stage_l_bootstrap_audit.json`（`634bd104…`）。

### 31.1 零假设下的 `p` 校准：轻度偏激进

300 个零假设样本，`p` 的均值 `0.485`（h1）/ `0.518`（h2），中位数 `0.494` / `0.508`，整体形状接近均匀。但左尾偏松：

- `p ≤ 0.05` 的实际占比 `0.0733`（h1）/ `0.060`（h2），名义应为 `0.05`。
- `p ≤ 0.10` 的实际占比 `0.140`（h1）/ `0.107`（h2），名义应为 `0.10`。

即在关心的显著性区域约偏激进 `1.4` 倍。**但 300 个样本下 `0.0733` 的标准误约 `0.0126`，距 `0.05` 约 `1.9σ`，属于边际证据，不能断言，需要更多样本才能定量。** 方向上要记住：这套 bootstrap 报出的 `p` 比真实略小。

实践含义：`1.4` 倍的偏激进被 `×20` 的 Bonferroni 远远盖过（后者是 20 倍保守），所以当前设置整体仍在保守侧，不需要改。但**如果将来有人想放宽家族大小，这个 `1.4` 倍就会浮出水面**，必须先把校准做实。

### 31.2 `block_length=5`：依据成立，但 `p` 对它敏感

真实配对差分（冠军参数 vs 一个明显更弱的参数集）的自相关：h1 lag-1 `0.144`，h2 lag-1 `0.224`，更高阶大多落进噪声甚至转负。**存在但很短的序列相关，所以 `block > 1` 是必要的，`block=5` 覆盖到 lag-4 也合理。**

块长敏感性（h1）：`b=1 → p=0.074`、`b=3 → 0.112`、`b=5 → 0.119`、`b=10 → 0.094`、`b=20 → 0.029`。**`p` 在 4 倍范围内变动，而且非单调。** h2 稳定得多（`0.157 ~ 0.209`）。

结论：`block=5` 在 `{1,3,5,10}` 中给出最保守的 `p`（h1 `0.119`），这个选择站得住；但 `b=20` 会把 `p` 打到 `0.029`，说明**块长不是无关紧要的旋钮**。处置：保留 `block=5`，并把这条敏感性记录下来——任何单个 `p` 值报告都应视为有约 4 倍的方法学不确定度，不能当精确数字用。

### 31.3 家族大小：两个方向相反的偏差，正好部分抵消

20 个候选的日收益序列相关结构（241 个共同日期）：**平均两两相关 `0.721`、中位 `0.715`、最大 `1.000`（存在完全重复的候选）**，按特征值算出的**有效独立检验数只有 `1.74`，名义是 `20`**。

这直接推翻了 25.3 里"Bonferroni 除以 20 是恰当口径"的说法，但推翻的方式比我预期的复杂：

- **偏保守的一侧**：真实独立检验数约 `1.74`，除以 `20` 意味着**过度校正约 11 倍**，白白损失检验力。
- **偏激进的一侧**：门禁面对的候选是从 **8100 组搜索里选出的最优 20 组**，选择极值带来的膨胀远不止 20。按名义 8100 校正在数学上不可能通过（`B=2000` 下 `p_adj` 下限 `12.2`），但按 20 校正显然低估了搜索规模。
- 所以**当前口径是两个方向相反的偏差的折中，而不是一个有原理支撑的正确值**。这是本轮对该机制最诚实的描述。

不改的理由：折中的净结果已被两次独立实测确认落在安全侧（阶段 C `0/400`、阶段 K `0.0%`，同时阶段 G2/H 证明真实边际在 `20bp/日` 处仍能通过）。而**任何改动都会往危险方向走**——若改用有效独立检验数 `1.74`，家族校正几乎消失，等于退回旧门禁（阶段 K 实测旧门禁假阳性 `3.5%`）。fail-closed 原则下保留 `20`。

### 31.4 三条留给后续的硬约束

- 不要在没重做校准（31.1）的前提下放宽家族大小或 `α`。
- 不要把单个 `p` 值当精确数字引用，它有约 4 倍的块长不确定度（31.2）。
- 不要用"有效独立检验数只有 1.74"当理由去削弱校正——那个数字只描述被检验的 20 个候选之间的冗余，**不描述从 8100 组里挑极值带来的膨胀**（31.3）。

## 32. 执行修正：一次差点污染后续全部结论的挂载错误

阶段 M/N/O 首次启动时我自己重写了 docker 命令，把脚本挂到 `/app/run.py`。这让 `sys.path[0] = /app`，**镜像内自带的旧版 `quant` 包压过了 `PYTHONPATH=/src`**。镜像内 `/app/quant/watchlist_grid.py`（`316545668a5a…`）是旧版：`stability_decision` 还是老签名（无 `search_family_size`）、全文 0 处 `multiplicity_passed`、也没有 `_paired_block_bootstrap_p_value`。

- 阶段 O 因为直接调用 `_paired_block_bootstrap_p_value` 立刻 `AttributeError` 崩掉，反而暴露了问题；**M/N 却静默跑完并退出码 0**。
- `docker inspect` 确认阶段 A–L 的挂载都是 `/research/grid_gate_stage_x.py`，`sys.path[0] = /research`，`/src` 生效。**此前所有结论不受影响。**
- M/N 首轮产物已删除并按 `/research/` 重跑。**数值确实变了**：阶段 M 的 h1 落差从 `-6.18` 变成 `-3.21`，现任落差从 `-14.08` 变成 `-0.44`。所以这不是无害差异，若不发现会把整段结论写错。

硬约束（写给后续所有人，包括我自己）：**这个目录下的研究脚本必须挂到 `/research/`，禁止挂到 `/app/` 下的任何路径**；任何一次"我改了启动命令"之后，第一件事是打印 `watchlist_grid.__file__` 并核对关键函数存在，而不是看退出码。

## 33. 阶段 M：选择窗 → holdout 的落差里，多少是过拟合

`grid_gate_stage_m.py`，复用阶段 D 已算好的选择表（不重跑 24300 网格），把 top-20 候选在选择窗与 holdout 上分别评估。产物 `out/stage_m_selection_decay.json`（`41124321…`）。

### 33.1 落差很大，且主要是过拟合

- h1：选择窗平均 Sharpe `9.13` → holdout `5.92`，落差 `-3.21`（20 个候选范围 `-3.71 ~ -2.88`，高度一致）。
- h2：`6.04` → `4.81`，落差 `-1.23`。
- **同一 artifact 上现任参数的落差只有 `-0.44`（h1）/ `-0.25`（h2）**，即环境本身只解释了一小部分。
- 所以**可归因于选择过拟合的超额落差是 `-2.77`（h1）/ `-0.98`（h2）**，是环境成分的 4~6 倍。

### 33.2 但"在 top-20 里挑得更准"这条路走不通

- top-20 内部，`selection_score` 与 holdout Sharpe 的 Spearman 相关是 **`-0.13`（h1）/ `+0.19`（h2）**——**排序在样本外没有信息**。`scheduled_workflow.py:553` 取 `head(20)` 之后的细排，排的是噪声。
- 同时：**20 个候选的 holdout Sharpe 全部高于现任**（h1 现任 `5.04`、h2 `3.39`），而门禁全否（阶段 D）。这两件事不矛盾——原始改进存在，但在 6 个月 `n≈120` 上统计不可分辨，而这正是门禁该做的事。

### 33.3 方向 D 的处置

可操作的只剩"缩小搜索空间"（本轮已做：24300 → 8100），因为落差主因是过拟合而非环境；而"选更好的候选"被 33.2 直接否掉。**方向 D 到此收敛，不再展开。**

## 34. 阶段 N：把 short 与 swing 合并能买到多少检验力

`grid_gate_stage_n.py`，6 个月共同窗口，产物 `out/stage_n_cross_branch_power.json`（`2e22f133…`）。

- **跨分支平均相关 `-0.146`**（最大 `+0.014`，6 对全部 ≤ 0），有效样本倍数 `2/(1+ρ) = 2.34`，**可检出边际缩放 `0.653`**。即阶段 G2/H 的 `20bp/日` 下限合并后约到 **`13bp/日`**，比理论 `√2`（`0.707`）还略好。
- 分支内平均相关：short `0.813`、swing `0.769`。**同一分支多加 horizon 几乎不加信息**（这也解释了 Holm 两个 p 常常一起动）。
- 有效独立序列 `2.42`，名义 `5`。

两条必须同时记住的限制：

1. **swing 差分的 lag-1 自相关是 `0.475 ~ 0.490`**，远高于 short 的 `0.180 ~ 0.206`。`block=5` 对 short 合适，**对 swing 明显偏短**，swing 单独检验应用 `block ≥ 10`（与 31.2 一致：块长不是无关旋钮）。
2. **两分支差分均值方向相反**（short `-1.6bp/日`、swing `+14.2bp/日`）。合并只对**预期在两分支同向作用的共享参数**（`hold_rank_buffer`、`rebalance_stride`、成本口径）成立；对模型/artifact 变更做合并会让真实的单分支效应互相抵消。

## 35. 阶段 O：把 buffer 从「优越性」改判为「非劣 + 成本算术」

`grid_gate_stage_o.py`，全样本 `2024-02-02 → 2026-08-08`，7 个档位，家族 6，产物 `out/stage_o_buffer_noninferiority.json`（`621a8a2e…`）。

### 35.1 结果

- `buffer=1`：成本节省 `0.099bp/日`，毛/净 p 全 `1.0`。
- `buffer=2`：`0.228bp/日`，p 全 `1.0`。
- `buffer=5`：`0.342bp/日`，族校正 p（毛，margin=成本节省）`0.228`，净优越性 `0.234`。
- `buffer=10`：`0.501bp/日`，毛收益差 `+5.0/+5.5bp`，族校正 p **`0.090`**，净优越性 `0.090`。
- `buffer=20`：`0.581bp/日`，毛收益差 `+3.3/+5.9bp`，族校正 p **`0.078`**，净优越性 `0.084`。
- `buffer=50`：`0.739bp/日`，毛收益差 `+3.8/+7.0bp`，族校正 p `0.180`，净优越性 `0.180`。

`buffer=10/20` 在 `α=0.10` 下通过非劣判据，净收益优越性（`α=0.05`）仍不过。方向与阶段 E 一致，**换判据没有把它推过 0.05**。

### 35.2 最重要的一条否证：buffer 不是成本故事

- **成本节省只有 `0.50 ~ 0.58bp/日`**，仅占 `roundtrip 0.002 + t+3` 的 `6.7bp/日` 拖累的 **7~9%**。
- 而**毛收益差是 `+3.3 ~ +5.9bp/日`**，比成本节省大 6~10 倍。

即 `buffer` 起作用的机制**不是省手续费，而是"少换手 → 赢家持有更久"的 alpha**。我上一轮给方向 A 的框架（"换手成本是确定性算术收益，不需要统计功效"）**对 buffer 不成立**——真正要证的那部分恰恰需要统计功效。方向 A 的"成本算术"这条腿，只在换手差异大一个量级的改动上才有意义。

### 35.3 两处方法学自查

- `margin = 实测成本节省` 是**数据相关**的边界，严格做法要事前登记固定 `δ`（如 `1bp/日`）。
- 脚本里 `margin=0` 那一列**不是非劣检验，就是毛收益的优越性检验**（`0.114`/`0.264`，全不过）。命名易误导，已在产物里注明。

### 35.4 与发布链的关系（本轮不动代码）

回测里 `hold_rank_buffer` 的滞后语义**线上发布链不具备**（不持久化前一日选股）。因此 35.1 的正面倾向**不可兑现**，除非发布链改动落地。按当前决定：**记为重启生产的前置条件，本轮不改代码**（生产在整个探索收益项目结项前不重启）。

## 36. 阶段 P：整轮网格搜索其实没有新信息可赢

阶段 M 出现一个反常：现任参数在 `active` 与 `monthly_factor_candidate` 两个 artifact 上的 Sharpe 落差**相同到 15 位小数**。两个文件确实不同（sha256 不同、`pred` 相关 `0.991`、最大绝对差 `0.77`）。`grid_gate_stage_p.py` 直接测，产物 `out/stage_p_artifact_equivalence.json`（`681e2bad…`）：

- **两个 artifact 上的收益序列逐位相同**：483 个观测，`max_abs_diff = 0.0`，`identical_share = 1.0`，相关 `1.0`（h1/h2 均如此）。
- **持仓也完全相同**：116 个有持仓日，`identical_pick_share = 1.0`，`mean_jaccard = 1.0`，`0` 天有差异。

原因清楚：`top_n = 2`，两个分数相关 `0.99` 时**前两名几乎从不改变**。

这条改写了整轮的因果解释：**阶段 A/A2/D 里"候选 vs 现任"的全部差异来自组合构造参数，不来自新信息。**本轮大面积 null 的根因不是"门禁太严"，而是**被搜索的对象里本来就没有新信息**；也解释了为什么多重性控制至今只在注入边际上被验证过——它没遇到过真候选。

## 37. 方向 E 单独立项：库里唯一未被使用的信息源

阶段 P 说明"没有新信息"，那就去找新信息。先做的是**清点，而不是造特征**。

### 37.1 未被日频因子管道消费的表

`quant/factors/engineering.py` 的 `build_panel()`（`engineering.py:508`）实际读入：价格、估值、`financial_yjbb`/`income`/`cashflow`/`balance`、`performance_forecast`、`margin_underlying_szse`、`block_trades`、`lhb`、交易日历。仓库里**没被消费的**是：

- `margin_detail.parquet`：**逐股两融余额，273 万行、722 个日期（2023-08-01 → 2026-07-24）**——唯一同时具备"逐股 + 可用历史长度"的未用源。
- `margin_sse.parquet`：只有 **141 个日期**且是市场级聚合，做不了横截面因子。
- `dividend.parquet`：事件型（`预案公告日`/`股权登记日`/`除权除息日`），需要单独的 PIT 构造，本轮推迟。

顺带一个发现：**在用的 `margin_underlying_szse` 只有 22 个日期（`2026-07-09 → 2026-08-12`）**，也就是说现有 `margin_*` 特征在绝大部分训练样本上是空的。这条与本项目无关但值得单独排查。

### 37.2 阶段 Q 初筛：确实有增量 IC

`grid_gate_stage_q.py`，`lag=1`（两融数据收盘后发布，`lag=0` 只作为上界记录、不得引用），对 `target_ret_1d`，478 个日期，覆盖面板 `59.5%` 的行。产物 `out/stage_q_margin_detail_screen.json`（`a9055bb1…`）。

去掉现行模型 `pred` 已解释部分后的增量 IC：

- `rz_buy_intensity`（融资买入额 / 融资余额）：`-0.049`，`t = -7.78`
- `rz_balance_chg_20`：`-0.024`，`t = -6.99`
- `rz_balance_z_60`：`-0.023`，`t = -6.30`
- `rz_balance_chg_5`：`-0.014`，`t = -4.74`
- `rz_balance_log`（余额水平本身）：`-0.006`，不显著

Holm 后 5 项在 `0.05` 下存活。**符号一致为负**：融资买入越猛，次日越差。

### 37.3 阶段 R 压力测试：信号是真的

`grid_gate_stage_r.py`，产物 `out/stage_r_margin_signal_stress.json`（`35eace8f…`）。四项检验全部指向"真信号"：

- **多元残差**（同时去掉 `pred`、`turnover`、`volatility_10`、融资余额水平这个规模代理）后 `rz_buy_intensity` IC = **`-0.025`，`t = -6.54`，IC_IR `-0.30`**。约一半来自规模/换手/波动，另一半是它自己的。
- **衰减平滑**：`lag1 -0.025` → `lag3 -0.017` → `lag5 -0.012`。真实流量信号该有的形状（数据瑕疵通常立刻死掉或翻符号）。
- **分年稳定**：2024 `-0.052`（101 日）、2025 `-0.058`（243 日）、2026 `-0.039`（134 日）。
- **形状单调**：残差十分位的次日平均收益 `15.4 / 11.0 / 10.3 / 10.4 / 9.4 / 8.1 / 8.4 / 6.6 / 6.3 / 4.9 bp`，从第 1 到第 10 单调下降 `10.5bp`。

`p` 已双侧化（方向是看过数据后定的，阶段 Q 的单侧 `p` 一律翻倍）。

### 37.4 阶段 S / T：信号真实，但和本策略没有交集

**阶段 S**（`grid_gate_stage_s.py`，产物 `21ab233b…`）：按"删掉高融资买入的行"实现否决，三档 `0.10/0.20/0.30`，家族 3。选择窗变好（h1 `11.62 → 13.45bp`），**holdout 变差**（`10.66 → 8.96 / 10.16 / 10.70bp`，Sharpe `4.11 → 2.81 / 2.99 / 3.12`），门禁全否。

而且**这不是一次干净的干预**：`pred_quantile`/`ridge_quantile` 是在"剩下的池子"上算分位数的，删行会移动阈值，结果 holdout 平均持仓从 `0.33` 涨到 `0.45`——否决效应和筛选阈值漂移缠在一起。

**阶段 T**（`grid_gate_stage_t.py`，产物 `1a940ac7…`）：改成语义正确的版本——被否决的票**留在池子里但标记为不可买**，分位阈值不动，`no_refill=True` 下被否决的首选变现金。结果：

- holdout **三个档位结果完全相同**（`11.02bp`，Sharpe `4.34`，h1 Sharpe 增益 `+0.287`，原始 `p = 0.237`，月胜率 `0.14`）→ 门禁否。
- 选择窗基本持平（h1 `11.62 → 11.65/11.68/11.77bp`，h2 反而略降）。

**决定性诊断（本该第一步就做的）**：

- **holdout 里 `94.9%` 的实际选股根本不是两融标的**；选择窗里 `66.7%` 不是。
- 落在融资买入前 `30%` 的选股只占 `10.5%`，前 `10%` 只占 `3.4%`。
- 三档阈值在 holdout 上否掉的是同样那 2 只票，所以结果完全相同。

结论：**IC 是真的，但与本策略的作用面几乎不相交。`top_n = 2` 只使用极值尾部，`0.025` 的横截面 IC 在这个尾部没有着力点。**

缺陷留档：诊断里桶内前向收益用了 `target_ret_{h}d`，而回测实际用的是 `tradable_ret`/`open_ret` 口径列，导致部分桶显示 `NaN`。**桶收益数字不可用**；上面的结论依赖的是"选股占比"与组合层面的门禁结果，不依赖桶收益。

### 37.5 阶段 U：最后一张未用表（dividend）也是 null

`grid_gate_stage_u.py`，产物 `out/stage_u_dividend_screen.json`（`a46be46e…`）。PIT 口径写死在脚本里：只用 `预案公告日`（+1 日）做 backward asof，`股权登记日`/`除权除息日` 仅在**已经过去**时才使用——把未来的除权日当成公告日就已知会构成穿越（除权日通常在后续实施公告里才定）。

8 个特征（现金分红比例、股息率、送转比例、净利同比、距公告天数、20 日内有公告、距除权天数、20 日内已除权），486 个日期，覆盖率 `4.7% ~ 100%`：

- 增量 IC 全部落在 `-0.009 ~ +0.006`，`|t| ≤ 2.30`。
- 唯一名义显著的是 `ex_within_20d`（增量 IC `-0.0092`，双侧 `p = 0.019`），**Holm 校正后 `0.152`，不存活**。
- **存活集合为空。**

至此**仓库里没有未被筛过的逐股表了**（`margin_sse` 因只有 141 个市场级日期不可用）。

### 37.6 顺带查清：`margin_underlying_szse` 只有 22 个日期的原因

`daily_update.py:484-487` 的写法是**每次只抓 `end` 这一天**（`df = fn(end)`，随后 `upsert(keys=["code","date"])`），**没有任何回补逻辑**。所以这张表的历史只能从"日更开始带上它那天"开始长，现在是 `2026-07-09 → 2026-08-12`，22 个交易日。

后果值得单独立项排查（与收益无关，但与模型正确性有关）：`engineering.py:596` 会把它接成 `margin_*` 特征，表非空所以**列存在**，但 `99%` 以上的训练行是 `NaN`。这意味着**这组特征在训练期几乎全空、在未来会逐日变满**——LightGBM 对 `NaN` 有默认分支方向，特征从"全空"变成"有值"会以不可预期的方式改变预测。这是典型的 train/serve skew 风险，不是收益问题，但在重启生产前应当处理（要么回补历史，要么先从特征表里摘掉）。

> **更正（见 39 节）**：本节的机制判断错了。该表的 `当日可融资/当日可融券` 几乎恒为 `Y`，`margin_*` 在有观测处**恒等于 1.0**，所以它不是"缺历史的特征"，回补也不会让它有信息；真正的危害是常量过 neutralize→zscore 链后被放大成**单位方差的浮点噪声**（探针面板实测 `std = 0.99958`、18890 行仅 80 个不同取值、`ICIR = 0.26`）。"回补历史"这条处置作废，改为 39.4 的两个方案。

### 37.7 方向 E 的处置（结项）

- **对当前 top-2 短线策略关闭**：不要因为 IC 好看就把两融因子加进特征表，组合层面已实测无收益（37.4）。
- 唯一可能让它有用的前提是**提高 breadth**（更大的 `top_n`、或换到覆盖两融标的的 universe）。那是产品结构决策，不在本项目范围，需要单独立项并单独预登记。
- 两张未用表全部筛完，均为 null（37.4 / 37.5）。
- 新增一条待办：`margin_underlying_szse` 的历史缺失与由此带来的 train/serve skew（37.6）。

## 38. 本轮总账（更新）

**确认的收益**：只有两项，且都不是 alpha——门禁可信度（旧门禁实测假阳性 `3.5~3.75%` → 受控门禁 `0.0%`）与算力（短分支 24300 → 8100，摆动 2304 → 768）。

**确认的 null**：

- 阶段 A/A2/D 的候选全部在 holdout 上输给现任或不可分辨；根因由阶段 P 给出——**被搜索的对象里没有新信息**（两个 artifact 的持仓逐位相同）。
- 分钟特征（`formal_wf_100k_v4`：`no_enhancement_passed`，7/7 Holm `p = 1.0000`）。
- 方向 D（阶段 M）：落差主要是过拟合，但 top-20 内部排序对样本外无信息，"挑得更准"不可行。
- 方向 E（阶段 Q/R/S/T/U）：两融流量信号真实、单调、分年稳定，但对 `top_n=2` 策略无作用面；分红表无增量信息。仓库内已无未筛的逐股源。

**仅方向性、不可作为收益证据**：`hold_rank_buffer ≈ 10~20`，族校正 p `0.078 ~ 0.090`（阶段 O），且机制被证明是 alpha 而非成本（35.2），且**语义在线上不可兑现**（35.4）。

**新增的可用工具**：跨分支合并把可检出下限从 `20bp/日` 降到约 `13bp/日`（阶段 N），仅适用于同向作用的共享参数。

**剩余未展开项**（按性价比排序）：

1. **提高 breadth 的产品结构改动**——会同时打开方向 E（让两融信号有作用面）和让 `hold_rank_buffer` 有意义。这是本项目走到现在唯一还有理论收益空间的方向，但它是产品决策，需要单独预登记。
   > **2026-08-17 更正**：用户批准后已在 42 节完成测试，`top_n ∈ {6,8,10,12}` 四个候选全部不通过受控闸门（Holm 后 p 全为 1.0，旧闸门也不通过），收益随 breadth 单调下降。**本项已关闭并判负**，方向 E 与 `hold_rank_buffer` 随之一并关闭。
2. `margin_underlying_szse` 历史缺失 / train-serve skew 排查（37.6）——**已在 39 节完成并改判**：不是缺历史，是常量被 prepare 链放大成噪声；处置方案见 39.4，列为重启前置。
3. 发布链改动（持久化前一日选股 + `top_n` 裁剪 + 实时可买掩码）——**重启生产的前置条件**，不是收益项。
4. 摘掉退化的 `margin_buyable` / `margin_shortable`（39.4）、整份重建 `factor_panel_full_a_cont_parts`（40.3，重建代价已量测：30 个因子换 1 个，见 41.1）、合并 `income_net_profit` / `yjbb_net_profit`（40.1）、给选因子器加相对覆盖率阈值（41.2，需单独预登记）——同属**重启前置条件**，都会改变特征域，故项目进行中不动。

**一句话结论**：本轮把"为什么搜不到收益"从猜测变成了证据——不是门禁太严，而是搜索对象里没有新信息（阶段 P）、真实存在的新信息与当前策略的作用面不相交（阶段 T）、而当前 6 个月 holdout 的检出下限（`20bp/日`，合并后 `13bp/日`）本身就高于所有候选改动的量级。**在不改变策略 breadth 的前提下，继续搜索的期望收益接近零**；而 42 节已证明改变 breadth 本身也无效，故收益侧全部关闭。

### 38.1 回归验证

用**本地完整仓库**（`quant` + `stock_analyzer` + `intraday_1400` + `realtime` 一起挂载到镜像 `/app`）在容器内跑：

- `quant.test_model_expansion_experiment`：**156 tests，OK**（产物 `out/unittest_fullrepo.txt`）。
- `intraday_1400.test_intraday_1400`：**152 tests，OK**（产物 `out/unittest_intraday.txt`）。

期间的一个假警报值得记一笔：只挂 `source/quant` 时该套件报 6 failures + 11 errors。原因是研究快照里只有 `quant` 一个包，`stock_analyzer` 等来自镜像内的旧版本，同时快照里的测试文件还是旧的（仍断言 `hold_rank_buffer ∈ {0,1,2}`）。**新旧混装的失败不是回归**——判断回归必须整仓同版本，这条和 32 节的挂载约束是同一类错误。

## 39. 阶段 V / V2：`margin_*` 特征的真实缺陷（并否证 37.6 的机制判断）

按 38 节遗留项 2 展开。产物：`out/grid_gate_stage_v.json`（sha256 `5fc7920d…`）、`out/grid_gate_stage_v2.json`（sha256 `097c6f75…`），脚本 `grid_gate_stage_v.py`（`cce6b19c…`）/ `grid_gate_stage_v2.py`（`bf1b90d3…`），日志 `out/grid-gate-stage-v.log` / `out/grid-gate-stage-v2.log`，均以 `/research/` 挂载、`--network none`、`SCHEDULER_DISABLED=1`、`RUN_ON_START=0` 运行，容器 `grid-gate-stage-v` / `grid-gate-stage-v2` 均 `Exited (0)`。全程只读 `quant_data`，未写入仓库、未改 manifest。

### 39.1 表本身：不是"历史缺失"，而是"这张表没有横截面信息"

`margin_underlying_szse` 实测：22 个日期（`2026-07-09 → 2026-08-12`）、46104 行、2118 个代码，市场只有深市（SZ 40538 行 + UNK 5566 行，UNK 是 15xxxx 的 ETF）。关键在取值：

- `当日可融券`：`Y` 46104 / 46104。
- `当日可融资`：`Y` 46103，`N` 1。

也就是说 `engineering.py:401-403` 生成的 `margin_buyable` / `margin_shortable` **在有观测的行上恒等于 1.0**（原始面板实测 `n_distinct = 1`、`std = 0.0`），未入名单的股票是 `NaN` 而不是 0。两列还完全共线（IC、取值分布逐位相同）。**这不是一个缺历史的特征，而是一个不含任何横截面信息的常量。** 37.6 里"回补历史即可"的判断是错的：即使把 22 天补成 8 年，它仍然是常量。

### 39.2 真正的危害：常量在 prepare 链里被放大成单位方差噪声

`prepare_features`（`engineering.py:842-847`）的顺序是 winsorize → `neutralize_cross_section`（对 `log_mv_total` + 行业哑变量回归取残差）→ `zscore_cross_section`。常量特征过这条链会发生两种结局，两种都在数据里观测到了：

- **生产面板 `factor_panel_full_a_cont`**：prepared 后 `margin_buyable` 的 NaN 占比 = `1.0`（全年、全部 103 个月）→ 该列彻底失活。**今天生产链上的这两列是惰性的**，但原因是偶然的：这套 prepared 文件建于 `2026-07-09`，而该表的第一个日期也是 `2026-07-09`，等于覆盖为零（机制由 40 节的因果实验查清）。
- **严格 PIT 探针面板 `strict_probe_panel_20260812`**：prepared 后有 18890 个非空值，`mean = 1.20e-17`、`std = 0.99958`、range `[-3.32, +3.23]`，但 18890 行里**只有 80 个不同取值**。这是中性化残差里的浮点噪声被 z-score 归一到单位方差——**一个纯数值噪声列，长得和正常因子一样**。

后者会被 IC 筛选当成因子打分：实测 `ic_mean = 0.0483`、`ic_std = 0.184`、`ICIR = 0.262`、`t = 1.08`（17 个交易日，`target_ret_1d`）。t 值不显著，但 `_ic_weights` 用的是 `mean_ic/std_ic`，`ICIR = 0.26` 与真因子同量级，且 `min_weight = 0.03` 的截断不会把它挡掉。

**所以 37.6 的机制判断要更正**：风险不是"特征从全空变满、LightGBM 的 NaN 默认分支方向漂移"（那需要它有信息），而是**一旦面板重建时该表已积累 ≥2 个交易日的覆盖，这两列就会从"全空惰性"翻成"单位方差噪声"，并有机会被因子筛选选中，向模型注入纯随机**。生产面板现在恰好落在惰性一侧，只是因为它的 `date_max = 2026-07-09` 正好只覆盖到该表的第一天。

### 39.3 事实核查：至今没有触发

- 现役模型的特征集（`model_feature_summary.parquet`，34 个）与面板特征集（`feature_summary.parquet`，54 个）**都不含任何 `margin_*`**，`event_or_margin` 类目下只有 `forecast_signal`。
- 扫描仓库里全部 15 个 `*selection*.parquet`：**`margin_*` 命中 0 次**，历史上从未被选中。
- 但它**在候选集里**：`feature_columns()` 不禁止 `margin_` 前缀（`_FORBIDDEN_FEATURE_PREFIXES` 见 `engineering.py:671-689`），且 103 个 prepared 月份（含 `files[0] = 2018-01`）**都带这两列**，而 `full_train_batched.py:1127-1131` 正是用 `files[0]` 的列集合当 `available`。所以拦住它的只有"prepared 后全 NaN"这一个偶然条件。

### 39.4 处置建议（本轮不落地，列为重启前置）

正确的修法不是回补，而是二选一：

1. **摘掉**：`_margin_underlying` 不再产出恒为常量的 `margin_buyable` / `margin_shortable`；同时在 `prepare_features` 里加一道退化守卫（逐日观测方差恒为 0、或非空覆盖率低于阈值的列直接不进 `feats`），因为**任何日内常量特征都会被这条链放大成噪声**，这不是 `margin_` 专有问题。
2. **换源**：真要"是否两融标的"这个指标，应当从 `margin_detail` 的出现与否构造——实测它覆盖 722 个交易日（`2023-08-01 → 2026-07-24`）、4417 个代码、沪深两市（SH 1168040 行 / SZ 1280711 行）、日中位 3792 个代码，比深市名单严格更好。

**本轮不落地的理由**（与 35.4 同一类）：加退化守卫会改变 `feats` 的列集合与 LightGBM 的特征顺序，即使在信息上是中性的，也可能扰动全部已测基线的数值，使本项目已完成的所有对比失去可比性。生产在项目结项前不重启，所以这条按**重启前置条件**记账，与发布链改动并列，不在项目进行中动。

38 节遗留项 2 据此关闭为"已定位、已改判、处置方案已定、待重启窗口落地"。

## 40. 阶段 W / W2：把 39 节的猜测变成因果结论，并查出一个更严重的 provenance 问题

产物：`out/grid_gate_stage_w.json`（`f85cb3de…`）、`out/grid_gate_stage_w2.json`（`48ffa942…`，生产面板 `2026-06`、horizon 5）、`out/grid_gate_stage_w2b.json`（`d4629033…`，探针面板 `2026-07`、horizon 1）；脚本 `grid_gate_stage_w.py`（`b2c28dfb…`）、`grid_gate_stage_w2.py`（`4994f870…`）；日志 `out/grid-gate-stage-{w,w2,w2b}.log`。三个容器均 `/research/` 挂载、`--network none`、`Exited (0)`，日志中均打印 `engineering=/src/quant/factors/engineering.py`（32 节的挂载校验）。全程只读 `quant_data`。

### 40.1 阶段 W：退化只此一处，但顺带扫出两个别的东西

对生产面板近 12 个月（1043129 行、65 个特征）与探针面板近 12 个月（986200 行、69 个特征）逐列量测"原始列在日内方差为 0 的交易日占比"：

- **`raw_zero_variance_day_share ≥ 0.99` 的只有 `margin_buyable` / `margin_shortable`**。39.4 里"给 `prepare_features` 加退化守卫"的必要性因此下降——它只会拦到这两列，直接摘掉更省事。
- **`income_net_profit` 与 `yjbb_net_profit` 相关系数 `0.9996`（探针面板 `0.9988`），而两者都在现役 34 个特征里**。等于一个净利润信号在特征表里被数了两遍，会抬高它在 IC 加权里的实际权重。这不是 bug，但是一处应当合并的冗余。
- 高缺失列（`prepared_nan_share > 0.5`）在生产面板有 12 个，其中 `amplitude` / `change` 96.3%、`turnover_chg_5` / `amount_chg_5` 98.3%、四个 `block_trade_*` / `lhb_*` 计数列 90%+。这些是事件类稀疏列，属预期，不是缺陷。

### 40.2 阶段 W2：噪声来自中性化，且**在当前代码里仍然活着**

用 `full_train_batched.py:900-906` 的原始调用在单月原始数据上跑 `prepare_features`，再用 `neutralize=False` 做对照。探针面板 `2026-07`（该月两融名单有覆盖）：

- `neutralize=True`：`margin_buyable` → 18890 个非空、**80 个不同取值**、`std = 0.999576`。
- `neutralize=False`：`margin_buyable` → **全 NaN**（常量的 z-score 被 `replace(0, np.nan)` 吃掉）。

两个数字与存盘文件逐位一致，所以**噪声不是历史遗留，是当前代码的行为**：`neutralize_cross_section` 把恒为 1.0 的列对 `log_mv_total` 回归，保留下来的 ~0 残差被 `zscore_cross_section` 拉回单位方差。39.2 的机制判断成立，而且进一步确认：**只要重建面板时该表已有覆盖，这两列就会立刻从惰性翻成单位方差噪声**——这一条现在是因果实验结论，不是推断。

### 40.3 更严重的一条：存盘的 prepared 面板不可由当前代码复现

阶段 W 把 `log_mv_total` 也标成了噪声嫌疑（生产面板 prepared 里 789421 个非空值只有 **389 个不同取值**、NaN 占比 24.3%，逐月看约 26~44 个不同值 / 14~45% NaN，相当于每个交易日只剩 1~2 个取值）。但 W2 在同一份原始数据上用当前代码重跑，`log_mv_total` 的 distinct ratio 是 **0.9799、NaN 0%**，且 `neutralize` 开关对它没有任何影响。

差异来源查明：`git log -L` 显示自我排除那一行

```python
regressors = [n for n in neutral_cols if n != c]   # engineering.py:786
```

是 **`2aedb38`（2026-08-12，"Continue quant audit fixes and provenance validation"）** 才加上的；在那之前 `log_mv_total` 会被对自己回归，残差恒等于 0，于是逐日要么 `std = 0` → NaN、要么只剩浮点噪声 → z-score 后 1~2 个取值。而生产 prepared 文件的 mtime 是 **`2026-07-09 14:49 ~ 23:38`**，早于该修复。

结论分三层：

1. **既有对比仍然有效**：阶段 A~T 的所有比较臂用的是同一份 prepared 面板，`log_mv_total` 的损坏是共模的，不影响配对差分。阶段 P 的"持仓逐位相同"等结论不受影响。
2. **但存盘面板与当前代码已不同版**：任何"用现在的代码重建面板"的动作都会让 `log_mv_total` 从噪声变回真正的规模因子，进而改变滚动因子筛选的入选集合与现任的数值——**现任的回测数字在重建后不可复现**。
3. `full_train_batched.py:1127-1131` 用 `files[0]`（`2018-01`，同属这份 2026-07-09 的旧面板）的列集合当候选因子域，所以这份旧面板既是训练输入，也是候选特征域的定义者。

### 40.4 处置

追加到重启前置条件清单（与 35.4 的发布链、39.4 的 `margin_*` 并列），本轮同样不落地：

- **重启前必须整份重建 `factor_panel_full_a_cont_parts`**，并在重建后重新跑一遍现任基线，明确接受"数值会变"，不能把重建前后的数字混在同一张对比表里。
- 重建**之前**先做完 39.4（摘掉 `margin_buyable` / `margin_shortable`），否则重建会同时引入 40.2 的噪声列。
- 合并 `income_net_profit` / `yjbb_net_profit` 二者之一（40.1）。
- 本轮不动的理由不变：这些改动都会改变特征域，会使本项目已完成的全部对比失去可比性；生产在结项前不重启。

## 41. 阶段 X：把 40.3 的"重建后不可复现"从断言变成量测，并查出选因子器偏爱噪声

40.3 说"重建后现任数值不可复现"，但那是推断。阶段 X 用**生产的选因子函数本体** `full_train_batched._rolling_factor_selection`（`top_n=30`、`preselect_multiplier=2`、`max_ic_corr=0.85`）在同一批月份（`2025-02 ~ 2026-07`，18 个月、1606414 行、343 个交易日、65 个候选因子、horizon 5）上跑两遍：

- **STORED**：磁盘上的 prepared 面板（建于 `2026-07-09`，前修复版本）。
- **REBUILT**：同一批原始月份用当前代码重跑 `prepare_features`（行数 1606453，与 STORED 相差 39 行）。

产物 `out/grid_gate_stage_x.json`（`76a6233c…`）、脚本 `grid_gate_stage_x.py`（`d5b05106…`）、日志 `out/grid-gate-stage-x.log`，容器 `Exited (0)`，日志打印 `engineering=/src/...`、`full_train_batched=/src/...`。

### 41.1 结果：入选集合确实变了，但只差一个

- `identical_selection = False`、`jaccard = 0.9355`、顺序也不同。
- **只在 STORED 入选**：`log_mv_total`。**只在 REBUILT 入选**：`yjbb_gross_margin`。其余 29 个一致，前 15 名逐位相同。

机制查得很干净：

- STORED 的 `log_mv_total`：`ic_count = 71`（总共 343 个交易日，因为 25% 行是 NaN、且很多日只剩 1~2 个取值导致当日 IC 无法计算），`ic_mean = -0.0083`、`icir = -0.065`、`stability_score = 0.0406`，排名第 52 —— **却入选了**。
- REBUILT 的 `log_mv_total`：`ic_count = 343`（满额）、`ic_mean = -0.0255`、`icir = -0.161`、`stability_score = 0.2767`，排名第 33 —— **被判定与 `turnover` 冗余（IC 序列相关 ≥ 0.85）而剔除**，腾出的位置给了 `yjbb_gross_margin`。

所以 40.3 的三层结论里第 2 层成立且量级明确：**重建面板会让 30 个因子里换掉 1 个**，现任数字不可逐位复现，但差异范围是有界的（1/30，且不在前 15 名）。第 1 层（既有对比仍然有效，损坏是共模）不受影响。

### 41.2 一个更普遍的设计弱点：去克隆过滤器系统性偏爱噪声

上面这一对比揭示的不只是 `log_mv_total` 的个案。`_rolling_factor_selection`（`full_train_batched.py:655-686`）的流程是：先按 `stability_score` 取前 `2 × top_n`，再逐个检查"IC 序列与已入选因子的相关性 ≥ 0.85 就剔除"。

**噪声列与任何因子都不相关，所以永远通不过不了这道过滤器——它必然存活。** 修好之后反而因为和 `turnover` 高相关被正确剔除。也就是说：这道去冗余机制在"真因子"和"噪声"之间是有偏的，噪声越纯越容易占住名额。

同时 `summary["ic_count"] >= 20`（`full_train_batched.py:657`）是**绝对阈值**：本例中 71/343 = 21% 的覆盖率就通过了。这两条合起来使得任何"高缺失 + 无相关性"的退化列都能稳定挤进 30 个因子。

建议（列入重启前置，本轮不落地）：把 `ic_count >= 20` 改成同时要求相对覆盖率（例如 `ic_count >= 0.6 × 窗口交易日数`）。这条比 39.4 的"摘掉 margin"更根本——它拦的是这一类问题，而不是某两列。注意 40.1 里的高缺失事件列（`amplitude` / `change` 96.3%、`block_trade_*` 90%+）也会被这条规则影响，其中 `amplitude` / `change` / `turnover_chg_5` / `amount_chg_5` 目前正排在 stability 前四位，所以该阈值需要单独预登记后再定，不能顺手改。

## 42. 阶段 Y：breadth（唯一剩下的收益方向）—— 直接判负

用户在 2026-08-17 批准继续，唯一未关闭的方向是 breadth。本节是它的预登记与结果。

### 42.1 预登记（跑之前写进脚本 docstring，`grid_gate_stage_y.py` sha256 `50fe391c…`）

- **轴**：只动 `top_n`。生产网格 `watchlist_grid.py:154` 搜的是 `(1,2,3,4,5)`，从未看过 5 以上，所以 `6/8/10/12` 是真正未搜过的区域，不是对已完成搜索的再切片。
- **族**：恰好 4 个候选，其余参数逐字继承现任冠军。
- **暴露口径**：固定 `gross_exposure = 0.30`，`slot_weight = gross / top_n`。另一种口径（固定 `slot_weight`、让 gross 随 `top_n` 增长）在本探针的零成本回测下只是给日收益序列乘一个常数，Sharpe 代数上不变 —— 那是杠杆，不是 breadth。
- **`hold_rank_buffer` 一律为 0**：buffer 只有在 breadth 大了之后才有意义，但放进来会成倍放大族大小；只有 breadth 本身通过了才做它。
- **判定**：`stability_decision`，`search_family_size=4`、`min_monthly_win_rate=0.40`，与本项目其他所有方向同一道闸。selection 窗口数字只作参考，不参与判定。
- **可测性守卫**：`n_pick = min(top_n, 当日可选数)`，所以必须先证明轴真的咬得住 —— 报告里记 `avg_holdings`。若持仓数不随 `top_n` 上升，结论只能是"不可测"，不能算"负"。

### 42.2 环境与基线

`universe = 3193` 只股（`MAINBOARD_UNIVERSE_FILE`），holdout 每日可选 2974~3011 只，所以候选池远大于 12，`top_n` 不受池子限制。现任冠军：`top_n = 2`、`gross_exposure = 0.30`、`ridge_quantile = 0.70`、`pred_quantile = 0.55`、`lgbm_weight = 0.85`、`ic_weight = 0.15`。selection `2024-02-02 → 2026-02-08`，holdout `2026-02-08 → 2026-08-08`（118/117 个观测）。

### 42.3 结果：轴咬得住，但四个候选全部不通过

`avg_holdings` 从基线 0.33 单调升到 2.23（holdout，h1），所以这是一次**有效的**测试，不是不可测。

holdout / h1 平均日收益（bp）随 breadth 单调下降：基线 10.66 → `top_n=6` 7.43 → `8` 8.25 → `10` 6.36 → `12` 5.54。h2 同样单调：20.69 → 13.45 → 11.18 → 9.04 → 7.55。

受控闸门四个候选全部 `passed = False`：

- 单侧原始 p 值全部在 0.746 ~ 0.968 之间（h1/h2 各一），即"候选优于基线"的方向根本不成立；Holm 调整后全部为 1.0，`best_search_adjusted_p_value = 1.0`。
- 月胜率最好的一个是 `top_n=8` 的 h1 = 0.571，其余多为 0.429，`top_n=10/12` 的 h2 直接是 0.0。
- `legacy_gate_passed` 也全部为 False —— 连不做多重比较校正的旧闸门都过不了，所以这次判负与 α 口径无关。

Sharpe 上有几个正的 `nonoverlap_sharpe_gain`（`top_n=8` h1 为 +1.82），但对应的 p 值是 0.746：那是分散化把波动压下去的算术效果，样本内不可区分于噪声，按项目既定标准不得作为收益证据。

### 42.4 判定与机制

**breadth 判负。** 而且机制是自洽的，不是"力量不够"：

1. 收益随 breadth 单调下降，说明排名 3~12 位的名字期望收益本来就低于前 2 位 —— 模型的排序是有信息的，把名额往下摊必然摊薄。
2. 真正的持仓上限不是 `top_n` 而是筛选条件：`positive_only` + `pred_quantile = 0.55` + `ridge_quantile = 0.70` 使得即便 `top_n = 12`，holdout 平均也只持 2.23 只，基线更是只有 0.33 只（八成以上交易日空仓）。
3. 而这两个 quantile **本来就在搜索网格里**（`rq ∈ 0.45~0.70`、`pq ∈ {None, 0.50~0.70}`），搜索自己选中了最紧的一端 `rq=0.70`。也就是说"少而集中"不是没试过的默认值，而是搜索在与"多而分散"对比后选出来的。42.3 只是在 5 以上把同一结论又验证了一遍。

因此 breadth 不需要第二步（不做 `hold_rank_buffer × top_n`、不做放宽 quantile 的复搜）：放宽 quantile 是重搜已搜过的维度，而 buffer 的前置条件（breadth 有效）已经不成立。

产物（远端 `/www/A/research_runs/grid_multiplicity_20260815/out/`）：`grid_gate_stage_y.json` sha256 `40a3fbe0…`、`grid-gate-stage-y.log` sha256 `ed32b2aa…`，容器 `grid-gate-stage-y` `exited exit=0`，挂载自检 `watchlist_grid=/src/quant/watchlist_grid.py`。

### 42.5 对结项的意义

breadth 是本项目最后一个有理论收益空间的方向，现在它也判负了。**收益侧全部方向已关闭，没有找到任何可作为 alpha 的增量收益来源。** 可以进入结项。

## 43. 结项（2026-08-17）

收益侧全部方向已关闭（42.5）。结项报告：`收益搜索项目结项报告_20260817.md`，内容为本文件 0 ~ 42 节的收敛，不引入任何新结论或新数字。重启生产的前置条件清单见报告第 6 节，禁止项见第 7 节，未平的 commit 决策见第 8 节。本文件到此封版，后续若重启工作另起记录。

## 44. 阶段 Z1/Z2/Z3：对"基线是否被公平对比"和"基线本身是否有效"的复核

用户在 2026-08-17 质疑：探索了这么多却没有收益，是不是基线恰好最优、或者对比方式不公平。复核查出**两处真实的不对称**，其中一处证伪了我自己的检验设计。三个阶段的结论是：基线确有真实排序能力，对比不公平的那一处修掉之后 breadth 仍然判负，但**"现任是最优"这个说法不成立**，结项报告的措辞需要收紧。

### 44.1 阶段 Z1：把调优不对称修掉，breadth 仍然判负

阶段 Y 只换 `top_n`、其余参数沿用现任值，而现任值是 8100 组网格在选择窗上的 argmax——"调过优的老参数 vs 没调优的新参数"。`grid_gate_stage_z1.py`（sha256 `5df5a2f6…`）把这一条补齐：`top_n ∈ {2,6,8,10,12}` 每档各自在选择窗上搜完整 540 组（`ic_weight` 5 × `ridge_quantile` 6 × `pred_quantile` 6 × `naive_weight` 3），取各自 `selection_score` 的 argmax，再上 holdout，族大小 5（比生产的 `min(20, …)` 更宽松）。产物 `grid_gate_stage_z1.json` sha256 `e4526960…`，耗时 2765s。

各档重调优后的 holdout（h1 日均 bp / Sharpe）：

- 现任 `top_n=2`：`10.66` / `4.11`
- `top_n=6`：`7.46` / `4.50`，原始 p `0.819`
- `top_n=8`：`7.64` / `5.32`，原始 p `0.787`
- `top_n=10`：`5.25` / `3.88`，原始 p `0.907`
- `top_n=12`：`4.70` / `3.43`，原始 p `0.921`

四档 `passed=False`，Holm 后全为 `1.0`，`legacy_gate_passed` 也全 False。**所以 42 节的判负不是调优不对称造成的**，收益随 breadth 单调下降这一点在各自调优后依然成立（Sharpe 在 `top_n=8` 处确实更高，但 p=0.787，仍属分散化的算术效果）。

### 44.2 阶段 Z1 的副产物：选择分数在样本外没有鉴别力

对照组 `top_n=2` 的重调优结果与现任**不一致**：搜出来的是 `ic_weight=0.06`、`rq=0.65`、`pq=0.60`、`naive=0.2`，选择窗上更好（h1 `12.80bp` vs 现任 `11.62bp`，`selection_score` 4.044），**holdout 上更差**（h1 `8.52bp` / Sharpe `3.38` vs 现任 `10.66bp` / `4.11`，两个 horizon 的 Sharpe 增益都是负的，月胜率 `0.0`）。

两条限定必须同时记住：我的搜索固定了 `gross_exposure=0.30` 与模型融合权重，且用的是当前选择窗（`2024-02-02 → 2026-02-08`），未必是现任当初晋级时的窗口，所以"现任不是 argmax"**不足以判定为 provenance 缺陷**。但可判定的是：**在同一个选择窗内，`selection_score` 更高的参数在 holdout 上更差**。这与 33 节（阶段 M：落差主要是过拟合、top-20 内部排序对样本外无信息）是同一件事的更强表述——生产用来挑候选的那个判据本身在样本外没有鉴别力。

推论很重要：本项目"没有候选打得过现任"，一部分原因是**选择机制识别不出更好的候选**，而不能直接读成"更好的候选不存在"。

### 44.3 阶段 Z2：基线本身第一次被拿去和零假设比，但检验设计有缺陷

整个项目此前测的都是"候选 vs 现任"，从未测过现任 vs 零假设。`grid_gate_stage_z2.py`（sha256 `426e6e50…`，产物 `984ad5a7…`）：holdout 上把打分列（`lgbm_pred`/`ridge_pred`/`base_pred`/`elastic_z`/`extra_trees_z`/`ic_z`/`rule_score`）按日内联合置换到别的股票头上，标的/日期/标签/可买标记不动，200 个种子。

结果 0/200 达到实际值（h1 `+10.66bp` vs 零假设均值 `-10.59bp`、最大 `+5.87bp`；h2 `+20.69` vs `-13.92`、最大 `+9.12`），经验单侧 p ≈ `0.005`。

**但预登记的匹配条件没通过**：零假设日均持仓 `1.96` 只，实际只有 `0.33` 只。原因是置换了分数却把 `buyable_close` 留在原股票上——现任的前 2 名经常涨停买不进、槽位变现金，而随机选中的票基本买得进。所以 Z2 比的是"排序能力 + 83% 交易日空仓"对"真投进去"，且零假设的 `-10.6bp/日` 说明在同一筛选池里随机买是明显亏钱的，**空仓本身就可能是收益主因**。Z2 单独不能作为"基线有排序能力"的证据。

### 44.4 阶段 Z3：把成交率匹配上，基线的排序能力成立

`grid_gate_stage_z3.py`（sha256 `4253cd05…`，产物 `b6baa472…`）：把 `buyable_close`/`buyable_next` 与分数用**同一个置换**搬走，于是 (分数, 可买性) 的联合分布被完整保留。预登记判据：持仓数须匹配到 0.05 以内才算有效检验。

匹配是**逐位精确**的：`avg_holdings` 实际 `0.3305084745762712` vs 零假设均值 `0.3305084745762712`，空仓天数 `88` vs `88.0`（h2 为 `87` vs `87.0`）。

- h1：实际 `+10.66bp`，零假设均值 `-2.96bp`、`p95 = +0.90`、最大 `+2.84` → **0/200**，经验单侧 p `0.005`。Sharpe `4.11` vs 零假设最大 `1.47`。
- h2：实际 `+20.69bp`，零假设均值 `-5.79bp`、最大 `+4.06` → **0/200**，p `0.005`。Sharpe `4.28` vs 零假设最大 `1.45`。

`ranking_skill_confirmed=True`。**结论：现任基线确有真实的横截面排序能力，不是噪声、也不是"涨停买不进转现金"的副产物。** 对比 Z2 与 Z3 的零假设均值（`-10.59` → `-2.96`）可知，不参与确实贡献了一部分表观收益，但把它匹配掉之后基线依然远在零假设分布之外。

### 44.5 这次复核对结论的修正

一条量化闭环值得单独记住：Z3 给出的基线**总**排序价值是 h1 约 `10.66 - (-2.96) = 13.6bp/日`，而 30.1 节测得当前 6 个月 holdout 的可检出**增量**下限是 `20bp/日`。**任何增量改动都只是这 13.6bp 中的一部分，而门禁需要 20bp 才能看见**——这就是"为什么什么都测不出来"的定量答案，比 38 节的定性表述更硬。

因此结项结论修正为（措辞已同步到结项报告）：

- **成立**：基线有真实排序能力（44.4）；未找到任何可作为 alpha 的增量收益来源（0~42 节）。
- **不成立、须删除**："基线恰好是最优版本"。44.2 已证明选择分数在样本外没有鉴别力，现任只是"目前手上最好的、且确实有效的一个配置"，不是最优点。
- **正确表述**：在现有 universe、breadth 与 6 个月 holdout 分辨率下，**既找不到可证明的增量收益，也无法证明现任已达最优**——两者都是同一个原因：可检出下限（20bp/日）高于整个策略的总边际（13.6bp/日）。
- 由此产生的唯一有理论价值的后续方向不是继续搜参数，而是**提高检验分辨率或降低基线噪声**（例如更长 holdout 需 18 个月才降到 10bp/日，见 30.1；或用 Z3 这类零假设匹配检验替代 candidate-vs-incumbent 作为主判据）。这是方法论方向，不是收益方向，需单独立项。

容器 `grid-gate-stage-z1` / `z2` / `z3` 均 `Exited (0)`，日志已归档为 `out/grid-gate-stage-z{1,2,3}.log`，挂载自检均为 `/src/quant/watchlist_grid.py`。

## 45 阶段 Z4：修"识别"——把 Z3 的匹配零假设做成候选判据并标定（2026-08-17）

44.2 的结论是选择机制坏了：`selection_score` 样本内最优的候选在样本外输给现任（12.80→8.52bp，月胜率 0）。44.4 的结论是有一个测量是好的：Z3 的成交率匹配置换零假设。Z4 只回答一个问题——**那个好用的测量能不能替掉那个坏掉的判据**。判据能不能用，不能靠看一个数好不好，必须先知道它的假阳性率和检验力，所以这一阶段的主体是标定，不是搜参数。脚本 `research_runs/grid_multiplicity_20260815_probes/grid_gate_stage_z4.py`（sha256 `7e1d3a5d…`），预登记写在脚本 docstring 里，运行前落盘。

### 45.1 两个判据形式

在**选择窗**（2024-02-02→2026-02-08，487 个交易日）上，对候选 c、期限 h：

- **绝对形式** `z_perm(c,h) = (实测日均 − 零假设日均均值) / 零假设日均标准差`，零假设即 Z3 的构造（日内把全部打分矩阵与可买掩码用同一置换搬到别的股票，标签矩阵不动）。
- **配对形式** `z_delta(c,h)`：把现任放到**同一张被打乱的面板**上重算，用逐次抽样的差值 `候选 − 现任` 作为零假设分布，检验实测差值。

主判据取两个形式在 h1/h2 上的均值 `z_perm_avg` / `z_delta_avg`；副判据取经验单侧 p 在期限上的最大值。绝对形式的理由：`selection_score` 是 2 只票组合的原始绩效统计量，而这个组合大部分交易日在空仓，于是它主要由"少数成交日运气如何"决定；用候选自己的置换离散度去标准化，正好把这份运气定价掉，因为零假设继承了候选自己的筛选、breadth 与成交率。

**须披露的一处顺序问题**：只有绝对形式是最初预登记的。B 部分刚跑出的前 3 个噪声单元就暴露了它不够用——"现任 + 一列没用的列"这种候选会继承现任真实的排序能力，绝对 p 恒在 `0.005` 附近，判据实际只剩"原始均值谁高"这个抛硬币的比较。配对形式是在**读到任何 C 部分结果之前**补上的，两个形式并列报告，没有悄悄把绝对形式替换掉。

### 45.2 三个部分

- **A 桥接/有效性**：用矩阵级置换在 holdout 上重跑现任 200 次，必须复现 Z3 的帧级结果。预登记容差：零假设日均与 Z3 相差 ≤1.5bp、日均持仓相差 ≤0.05、h1 经验 p 仍 ≤0.05。桥接不过，则 B/C 都不可解释。
- **B 假阳性与检验力**（holdout，沿用阶段 G2/K 的带噪边际设计）：合成候选 = 现任 + `catboost_z` 槽位（现任该权重为 0，槽位是空的）× κ。**噪声臂**注入纯噪声，任何"通过"都是假阳性；**信号臂**注入 h1 未来收益的横截面 z 加单位噪声，即一个真实但不完全的预测器，**实测**增量 bp 而不是假设增量。判据 D1（绝对）= p ≤ α/family 且两期限原始均值均高于现任；D2（配对）= 差值的配对置换 p ≤ α/family。family 取 1/5/2700。预登记门槛：假阳性 ≤0.10，且在实测增量 ≤10bp/日 处达到 ≥80% 通过率（30.1 测得当前门禁在这段 holdout 上的下限是 20bp/日）。
- **C 样本外鉴别力**（真正的问题）：候选集就是 Z1 的 2700 组（top_n 5 × ic_weight 5 × ridge_quantile 6 × pred_quantile 6 × naive_weight 3，融合权重与 gross 固定在现任值，`slot_weight = gross / top_n`），现任本身是其中一员。在选择窗上给每个候选算 `selection_score`（走生产的 `_selection_summary`）与两个 z（30 次置换抽样），然后在未动过的 holdout 上各评一次。报告每个判据与 holdout 日均收益的 Spearman 秩相关、top-1/10/50 的 holdout 收益、以及 top-1 是否打得过现任的 10.66bp。

**采纳规则（事先固定）**：只有 (i) A 桥接通过、(ii) B 中 D1/D2 至少一个满足假阳性 ≤0.10 且 ≤10bp 处 ≥80% 检验力、(iii) C 中该判据的 Spearman ρ>0 且严格大于 `selection_score` 的 ρ、top-1 的 holdout 收益不低于现任——三条全中，才允许把它作为**提案**提出。任一条不中，则"识别"这一环在现有数据上修不好，结项报告原样成立。三条全中也只是提案，不构成晋级授权，不动任何 manifest。

### 45.3 A 部分：桥接逐位通过

`grid_gate_stage_z4a.json`（sha256 `5a184642…`）：现任实测 h1 `+10.660575342126032bp`、h2 `+20.68501831702924bp`，与 Z3 完全一致；200 次抽样的零假设均值 h1 `-2.9610bp`（Z3 为 `-2.96`，差 `-0.001`）、h2 `-5.7924bp`（Z3 `-5.79`，差 `-0.002`）；日均持仓 `0.3305084745762712` vs 零假设均值 `0.3305084745762712`（h2 `0.3333333333333333` vs `0.33333333333333326`）；两期限 0/200，经验 p `0.005`。`bridge_passed=True`。

矩阵级置换因此与 Z3 的帧级置换等价，但每次抽样从 `8.6s` 降到约 `0.01s`（holdout），这是能在 2700 个候选上跑置换判据的前提。容器 `grid-gate-stage-z4a` `Exited (0)`，挂载自检 `/src/quant/watchlist_grid.py`。

### 45.4 B 部分补充噪声臂：两个判据都不可用（2026-08-17）

补充臂（噪声 κ=0.03/0.07/0.15 × 20 次重复 = 60 单元，信号 κ=0.2 × 20 = 20 单元）`Exited (0)`，`elapsed_seconds=12452.5`，产物 `out/z4b_noise_extra/grid_gate_stage_z4b.json`。按 45.2 预登记门槛（假阳性 ≤0.10 且实测增量 ≤10bp/日 处 ≥80% 检验力）逐条对：

- 假阳性率：`D1_absolute` family 1/5 均为 `0.25`，family 2700 因经验 p 下限而恒为 `0.0`；`D1_absolute_normal_tail` 三个 family 全为 `0.25`。`D2_paired` 为 `0.10 / 0.0 / 0.0`，`D2_paired_normal_tail` 为 `0.05 / 0.0 / 0.0`。
- 检验力：信号臂实测中位增量 `median_realised_gain_bp_h1 = 2.64bp`。`D1_absolute` 在 family 1/5 通过率 `0.90`、family 2700 为 `0.0`；normal_tail 变体三个 family 全为 `0.90`。`D2_paired` 通过率 `0.15 / 0.0 / 0.0`，normal_tail 变体 `0.20 / 0.0 / 0.0`。
- `min_realised_gain_bp_for_80pct_power`：D1 两个变体在 family 1/5 为 `2.64`（normal_tail 在 2700 也是 `2.64`），D2 两个变体全部为 `null`。
- 结果：`rule_usable` 四个变体全部 `false`，`calibration_passed=false`。

含义是分开的两件事：D1 的 0.90 通过率不是检验力，而是 0.25 假阳性的同一枚硬币——它把注入噪声的候选也放过 1/4；D2 把假阳性压到门槛内，代价是在 2.64bp 的真实增量上几乎看不见（≤0.20）。因此 45.2 的采纳规则第 (ii) 条不成立，"识别"这一环在**当前样本量**下修不好，结项报告原样成立。

主臂（噪声 κ=0.05/0.1/0.2 × 20 = 60 单元，信号 κ=0.05→3.2 共 7 档 × 20 = 140 单元）已 `Exited (0)`，`elapsed=23720.6s`，产物 `out/grid_gate_stage_z4b.json`（632786 字节）。主臂自身假阳性 `D1_absolute` `0.15/0.15/0.0`、`D1_absolute_normal_tail` 三 family 全 `0.15`、`D2_paired` `0.0333/0.0/0.0`、`D2_paired_normal_tail` 全 `0.0`；`min_detectable_gain_bp` 中 D1 两变体在 family 1/5 为 `1.64`（normal_tail 在 2700 也是 `1.64`），D2 两变体全 `null`；`calibration_passed=False`。

**按预登记池化两臂（60 + 60 = 120 个噪声单元，报池化值而非较好的那个）**：

- `D1_absolute`：family 1/5 假阳性 `0.20`，family 2700 为 `0.0`（经验 p 下限所致，不是判据变好）。
- `D1_absolute_normal_tail`：三个 family 全部 `0.20`。
- `D2_paired`：`0.0667 / 0.0 / 0.0`。
- `D2_paired_normal_tail`：`0.025 / 0.0 / 0.0`。
- 假阳性集中在小 κ 一侧（family 1，D1 按 κ 计数 `0.03:7, 0.05:4, 0.07:5, 0.1:1, 0.15:3, 0.2:4`；D2 为 `0.03:5, 0.05:2, 0.07:1`，κ≥0.1 全 0），即注入噪声越小越容易被误判为"增量"，这正是绝对形式继承现任真实能力的表现。

于是分工清楚：D1 池化假阳性 `0.20` 超门槛一倍，不可用；D2 池化假阳性 `0.0667` 在门槛内，但检验力不成立——主臂 family 1 上 `2.64bp → 0.15`、`3.63bp → 0.15`、`16.1bp → 0.25`，family 5/2700 上全为 `0`。也就是说即使给它一个 `16.1bp/日` 的真实增量（比 44.5 里基线自己的总边际 13.6bp/日 还大），配对判据在实际 family 规模下也看不见。这不是"样本再大一点就好"的量级差距，45.6 的 1.5× 标准误收缩不足以补上。

C 部分（2700 组样本外鉴别力）5 个分片在第 16–18/30 抽样处经批准手动停止（`Exited (137)`，各 elapsed≈13,000–13,900s，按设计只在全部抽样结束后落盘，故无产物，约 3 小时算力作废）。停机理由不是它的预登记终止条件被触发——那条规则的条件（估值数据可跨 2018 回填）在 45.6 的探针里判否，规则未触发，不做事后套用；停机是为了先解决 45.5 暴露的口径分歧，因为在口径确定前 C 的鉴别力结论会落在错误的策略上。

### 45.5 口径分歧：选参数用补位、判定用不补位（2026-08-17）

复核候选生成端时发现的问题，与判据无关，但它决定上面所有 bp 数字属于哪个策略。脚本 `research_runs/grid_multiplicity_20260815_probes/provenance_scoring_check.py`（sha256 `5f2f4182…`），预登记 MATCH/MISMATCH 判据与参照值硬编码在脚本内。两个容器同输入（`prepared rows=357432 dates=119 codes=3011`、`buyable_present=True`、`QUANT_BT_COST_ROUNDTRIP=0.002`、现任参数相同），分别加载冻结快照代码与镜像 `/app` 代码：

- 快照：h1 `10.660575342126032bp`、`avg_holdings=0.3305084745762712`、`avg_turnover=0.0686`、`win_rate=0.1864`、`sharpe=4.671`、`max_drawdown=-0.0171`；h2 `20.68501831702924bp`。
- 生产镜像：h1 `13.996162200275577bp`、`avg_holdings=2.0`、`avg_turnover=0.9068`、`win_rate=0.6102`、`sharpe=2.268`、`max_drawdown=-0.1400`；h2 `35.968964794228526bp`。
- `verdict=MISMATCH`（h1 Δ `+3.3356bp`，h2 Δ `+15.2839bp`，holdings Δ `+1.6695`）。

根因在 `_fast_combo_metrics`：镜像版在排序**前**做 `mask &= buyable`，涨停买不进时从第 3 名起回填，持仓恒为 `top_n`；快照版 `no_refill = True`，排序后用 `picked &= np.take_along_axis(buyable, order, axis=1)` 把买不进的名额记为现金。次要差异还有 `avg_holdings` 的分母、`gross_exposure/counts` 权重、`backtest._weight_turnover`，以及快照独有的 stride 与 `hold_rank_buffer`/`_select_with_rank_hysteresis` 分支。

发布链核实（活容器 `a-scheduler-1` 内省，非仓库推断）：`daily_optimization_pipeline._metrics` 固定 Top-N 后 `selected["filled"] = buyable_next`、买不进记 0、分母仍是 `TOP_N`——**不补位**；同一容器 `watchlist_grid` 源码中 `no_refill` 出现 0 次——选参数的网格**补位**。所以研究一直用的不补位口径与发布判定口径一致（44.x 的"~30 个有效日 / 20bp/日 下限 / 88 个空仓日"前提对判定指标成立），出问题的是候选生成端：参数由补位指标挑出、由不补位指标判定。此项只覆盖网格指标/选择链，实盘下单链（`backtest`/`select`）未核。

**【2026-08-19 撤回：上一段的结论是错的。】** 错误在于"文件在活容器里"被当成了"它被生产调用"。实测：`grep daily_optimization_pipeline /app/docker/ /app/quant/scheduled_workflow.py` **零命中**，全仓引用它的只有它自己、它的测试与文档；仓库 `问题:186`/`:314` 早已写明它是**不发布**的研究控制器（模型族扫描 + 绝对门槛 + block bootstrap）。而部署中的 `backtest.portfolio_from_predictions`（容器 `dbfaaa9d`）**连 `no_refill` 参数都不存在**——`no_refill` 是 `2aedb38`/`9c11ad8`（08-12）才进仓库的，晚于容器代码的 `8a7bd68`（08-10）。

因此正确表述是：**部署中的生产，选参与验收都是补位，内部一致，不存在口径分歧。** 不补位只存在于两处：那个不发布的研究控制器，以及仓库里尚未部署的新代码（`backtest.py:212` 默认 `no_refill=False`，但 `watchlist_grid.py:412` 硬编码 `True`、`full_train_batched.py:1884` 传 `True`）。所以"口径分歧是已确认的生产缺陷、修它是 bug fix"这个说法作废；真实形态是**部署代码与仓库代码的口径不一致**，属于部署风险，不是既存缺陷。

代码来源已定位（容器内 md5 精确等于 git blob）：`daily_optimization_pipeline.py` `8ca01139`、`full_train_batched.py` `669f1057`、`model.py` `0865f98b`、`factors/engineering.py` `55873068` **全部 = 提交 `8a7bd68`（2026-08-10）**；镜像里的对应版本是 `c02d9267`（07-26，前两者）与 `3890e628`（08-04，engineering），`daily_optimization_pipeline.py` 在 08-10 才被创建故镜像中缺失。即一次"改完拷进容器、未重建镜像"的部署，不是手改残留。

### 45.6 历史扩样：把 `--recent-windows` 的截断放开（2026-08-17，进行中）

预探针（`out/history_probe_part3.json`，sha256 `d40a1205…`）结论：仓库内无 2018 年前数据（报表类均自 2018-03-31，`block_trades`/`lhb` 自 2023-08-01，日历回到 1990-12-19）；源侧价格可回到 2013-01-04，但 `valuation()` 依赖的 `ak.stock_value_em` 文档口径为"2018 至今"，因此**跨 2018 回填按预登记判为 DEGRADED，不做**。

真正的杠杆不在数据侧：特征面板已覆盖 2018-01→2026-08（104 个月度分片），而预测只有 606 个交易日（2024-02-02→2026-08-07），差距来自生产的 `recent_windows=24` 对 `total_windows=67` 的截断。因此在隔离研究仓 `/www/A/research_runs/wf_full_history_20260817/` 复刻发布配方（取自 `active_quant_model.json` 的 params 与活容器 `scheduled_workflow` 的命令构造，`TRAIN_MONTHS=36`：horizon 1、ridge_quantile 0.35、lgbm_weight 0.85、ic_weight 0.1、positive-only、elastic-net、extra-trees、rolling-factor-select、purge-horizon），只改两处并记录为偏离：`--recent-windows 24 → 0`、`--refresh-months 1 → 0`（避免重建面板；隔离仓最初把 `research` 与面板分片软链到只读仓，`--refresh-months 1` 触发的 `rmtree` 曾以 `Errno 30` 拦下一次对生产面板的写入，之后改为逐月软链的可写覆盖层）。

2 窗冒烟 `Exited (0)`，产物齐（`prediction_merge rows=66127 / 15055`）；全量 67 窗 `wf-full-history` 已启动（6 核 / 12g，`window=0` 起，`date_range=2018-01-02..2026-08-07`，`factors=33`）。预期只买到样本量：预测日 606 → ~1400+（2.25×），标准误缩 √2.25≈1.5×，可判定增量下限由 20bp/日 降到约 13bp/日，有效交易日约 30 → 约 68。它不修 45.4 暴露的判据结构问题，只是把 D2 从"无检验力"推向"可能有检验力"的必要条件。

**事后更正两处**：(1) 全量运行 `Exited (0)`，`periods=1334`、`n_predictions=3992380`，但预测起点是 **2021-02-02**，不是 2018——36+1 个月的训练/验证预热吃掉 2018-01→2021-01，面板覆盖 2018 不等于预测覆盖 2018；预登记的 `2018_2020` 分段因此必然为空段。(2) 45.6 末句"必要条件"的说法应撤回：45.4 主臂功率曲线显示 D2 在 16.1bp/日 才到 0.25，SE 缩 1.5× 不足以补上这个缺口，扩样对候选判别不是必要条件、也不是充分条件；它只改善"对现役自身的绝对测量"。

### 45.7 Stage Z5：长样本上重测现役（2026-08-19，完成）

脚本 `research_runs/grid_multiplicity_20260815_probes/long_sample_baseline_check.py`（sha256 `70527c2d…`，两侧哈希已核），容器 `z5-baseline`（`--network none`、4 核 / 10g、只读挂载源码快照与两份预测、`QUANT_BT_COST_ROUNDTRIP=0.002`）`Exited (0)`，`elapsed=1166.1s`，产物 `out/z5/long_sample_baseline_check.json`。执行口径 `use_open=False filter_untradable=True cost=0.002 refill=False`（发布/判定口径，非选参口径）。

**硬控制门先过**：用部署预测在 Z4a 同一 118 日重算，`mean_ret_bp=10.660575342126032`、`avg_holdings=0.3305084745762712`，与 Z4a 位对位一致（容差 1e-6 / 1e-9），因此长样本数字与 Z4 可比。

各跨度（family=1、单假设、置换零假设为日内联合置换分数矩阵与可买掩码、标签固定）：

- `full` 2021-02-02→2026-08-07：有效交易日 1334，日均 **10.78bp**，日 sd 38.68bp，SE **1.06bp**，t=10.18；置换零均值 -2.50bp / sd 0.60bp → z=22.3，经验 p=1/201=0.00498（下限即此值），正态尾近似 2.1e-110；持仓 0.364/2、换手 0.070、sharpe 5.04、最大回撤 -3.49%、胜率 19.9%；**95% 可判定增量下限 4.15bp/日**。
- `2018_2020`：空段（0 日），按预登记作空段处理。
- `2021_2023`：706 日，8.43bp，SE 1.25，t=6.72，z=13.5，下限 4.92bp。
- `2024_2026`：628 日，13.44bp，SE 1.75，t=7.69，z=17.8，下限 6.85bp。
- `deployed_holdout`（同 118 日，长样本模型）：7.12bp，SE 3.54，t=2.01，z=4.75，下限 13.86bp。

**分段未重划**：预登记的 `2021_2023` 与 `2024_2026` 已无缝覆盖全部可用跨度（2021-02→2026-08），空段只是标签作废，不存在需要补的窗口，因此不做"2021-2022 / 2023-2024 / 2025-2026"重跑——重划不会增加任何一天数据。

**可以说的**：现役配方在 2.25× 长的样本上仍显著优于同口径随机置换，且分段无衰减（8.43 → 13.44bp）；可判定增量下限从 118 日的 14.86bp 降到 1334 日的 4.15bp，扩样确实把测量精度提高了约 3.6×。

**不能说的**：`verdict=RECENT_ONLY`，family=1、单假设、检验对象是现役自身，**不携带任何晋升授权**，也完全没有回答"是否存在更好的候选"——Z4 的 C 分片（生成端）在第 16–18/30 抽被 `Exited 137` 中止且无产物，至今未测。45.4 的判据结构问题（D1 结构性无效、D2 在 family=2700 下功率≈0）不因这次扩样而改变：4.15bp 的下限是"现役 vs 随机"的绝对测量下限，不是"候选 vs 现役"的配对判别下限，后者仍受 D2 的功率曲线约束。

**补一条口径说明（2026-08-19）**：上面所有 bp 都是**不补位（研究）口径**，不是部署中生产自己的指标。同一套现役参数在补位口径下（606 日面板）是 40.90bp/持仓 2.0，不补位是 11.43bp/持仓 0.60，比值 3.55 ≈ 2.0/0.6——差异主要来自敞口口径（已投资本 vs 含现金拖累的已分配资本），不是"虚高"。置换显著性不受影响（同口径对同口径）。

### 45.8 Stage Z6：换会计约定后冠军是否还是冠军（2026-08-19，完成）

脚本 `research_runs/grid_multiplicity_20260815_probes/caliber_champion_sensitivity.py`（sha256 `ab81491f…`，两侧已核）+ `caliber_compare.py`（`4547b729…`）。同一预测面板、同一 8100 行标准网格（`_base_combos` short 支，模板路径故意指向不存在的文件以杜绝自喂），三条臂跑**真实代码**而非模拟：`PYTHONPATH=/app` 生产镜像版（md5 `5ab3a3e7`，补位）、`PYTHONPATH=/src` 冻结快照（`4cee89b9`，不补位）、以及在快照模块内只切换补位这一件事的 `refill_emulated`（不可买位置的全部打分矩阵置 NaN，等价于生产的 `mask &= buyable` 且在分位数之前；同时把补位后过滤置为 no-op）。模型混合权重钉死在现役 manifest 值，不参与搜索。预登记的偏离：只用 horizon 1（长面板只有 `tradable_ret_1d`，重建 h2 目标会让两臂不可比），两臂用同一 horizon 集故对比仍有效。

复刻忠实性先过：生产臂在 606 日部署面板上的 argmax = `ic 0.15 / top_n 2 / gross 0.30 / rq 0.70 / pq 0.55 / naive 0.1`，与 `active_quant_model.json` 的现役参数**逐字段相同、rank=1**。

结果：
- 606 日面板，生产（补位）rank 1 → 快照（不补位）rank **4032/8100**，argmax 变为 `ic 0.06 / top_n 5 / pq None / naive 0`；`selection_score` 排名 Spearman **-0.504**、Kendall -0.291，top-10 / top-100 / top-500 重叠**全为 0**；`mean_ret_bp` 的 Spearman 仍为 +0.719。
- `refill_emulated`（只切补位）：现役 **rank=1**、argmax = `param_id 7117` 与生产臂一致。→ **补位这一件事单独就能解释全部翻转**，另外三处版本差异（`selection_score` 是否按 gross 归一、换手定义、空仓日整行丢弃）对 argmax 无影响。
- 长样本 1334 日、不补位：现役 rank **5443/8100**，argmax `ic 0.15 / top_n 5 / rq 0.45 / pq 0.70 / naive 0`，其 10.95bp 与现役 10.78bp 基本相同，`avg_sharpe` 6.74 vs 5.04（靠持仓 1.18 vs 0.36 摊平波动）。

**结论（改写自 45.5 撤回后的正确形态）**：这不是"生产在用自己都不认的指标选参"，而是**把仓库里的不补位代码部署上去、却不重新选参，生产就会跑在一套在新代码口径下排 5443/8100 的参数上**——部署风险，不是既存缺陷。现役参数在生产**自己**的一致口径下是货真价实的 argmax。哪种约定更接近现实（买不进时换下一名 vs 留现金）取决于从未审计的实盘下单链，这是本项目留下的未决问题。

**不允许的推论**：8100 组无多重性控制，任何 bp 差都不是可晋升证据；Z6 只被允许支持"argmax 在换会计约定后是否稳定"这一条。

### 45.9 结项与恢复生产（2026-08-19）

研究侧停机与清理：`z6-refill-emu` / `z6-long-snapshot` 在停机指令前均已 `Exited (0)`，产物完整。`research_runs` 删除全部 `*.parquet/*.pkl/*.zip/*.whl` 与 4.1G 的 `status_backfill/sdk_tmp` h5 临时文件，**保留** `grid_multiplicity_20260815` 全目录（6.6M，含三臂 ranking parquet 与全部 stage JSON）与 `wf_full_history_20260817` 的长样本预测（926M，67 窗走查唯一产物，重跑代价数小时）；JSON 410 个、脚本 448 个、日志 223 个、md 全部保留。磁盘 118G → 82G，可用 95G。五个研究容器日志归档至 `research_runs/logs_archive_20260819/`。

镜像固化：`docker commit a-scheduler-1 stock-scheduler:deployed-20260819`（`41f5c0b3`，3.02GB），把只存在于容器内的 `8a7bd68` 代码（含镜像原本缺失的 `daily_optimization_pipeline.py`）固化，四文件 md5 在新镜像内逐一复验通过。此前"只要有人 recreate 容器就会静默丢代码/回退版本"的脆弱点解除。

恢复生产：`docker-compose.yml` 备份为 `docker-compose.yml.bak-20260819`，改两处——`image: stock-scheduler` → `stock-scheduler:deployed-20260819`、`SCHEDULER_DISABLED=1` → `0`（`RUN_ON_START=0` 不动）。`docker compose up -d scheduler` 重建，验证：容器镜像正确、`cron.env` 内 `SCHEDULER_DISABLED='0'`（该文件由 entrypoint 从容器 env 生成，故必须改 compose 而非改文件）、四文件 md5 不变、cron 守护进程在、16:30:01 心跳落盘、`active_quant_model.json` md5 `2fdbcc8e` 未变。

**恢复范围是完全恢复历史行为，无任何行为变更**：日更 `daily-light`/`weekly-full`（`incumbent-refresh`）照原样跑，含锚定现役的有界微调（邻域 `ic±0.03 / top_n±1 / rq±0.05 / pq±0.05 / naive±0.10`、`gross` 精确相等，代入现役参数约 54 组，`champion_score_params` 不被改写）；月度 `monthly-factor`（`candidate-upgrade`，每月首个周六 01:00，8100 组 argmax 且会重写锚点）**也一并恢复**。我曾建议关掉月度、并给日更加 `--skip-short-grid`，两条都被否决——理由成立：那是没有证据支撑的行为变更，而"恢复生产"的本意是回到原状。留档的反对意见是：8100 组 argmax 在 45.4 已被证明在该家族下不可信（约 283 组纯噪声组合能过稳定性门），随机跳参的期望收益因换手成本轻微为负；但这是期望值论证，不是实测，不足以支撑变更。

**仍然未决（留给下一次）**：实盘下单链（`select`/下单）从未审计，因此"买不进时换下一名还是留现金"这个决定生产到底怎么做的仍不明；仓库里 16 项脏改动（含不补位的 `watchlist_grid.py`）未提交、也未部署——若将来部署，按 Z6 必须同时重新选参，否则参数与代码口径不匹配。



