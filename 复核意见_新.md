# A股量化系统 · 提升收益方向复核（按优先级排序）

> 快照：本文 §0~§7 基于全仓库源文件 mtime `2026-08-12 15:59:01` 写成。**仓库已于 `2026-08-13 13:21` 被整体替换为新快照**、并于 `2026-08-13 15:17` 再次整体重拷（该次仅一个文件内容变化，见 §8 开头的二次复核说明），对新快照的逐条复核见 **§8（本文档的当前权威结论）**。§0~§7 保留原文不动，作为审计留痕；凡被 §8 推翻的判断，在原处以「【0813 更正】」标注。
> **只读审计，仓库内零修改**，本文件写在仓库外。
> 配套文档：`HANDOFF_20260812_1600.md`（当前状态）、`AUDIT_PROBLEMS_20260812.md`（问题清单 v2）、`AUDIT_HANDOFF_20260810.md`（证据库）；对方新写的两份在仓库内：`解决清单_供Opus复核.md`（C01–C46 修复台账）、`下一轮调研记录_20260812.md`（含正式 24 窗结果）。
> **§9 是「方向为正但不显著」的完整清单 + 检验方法本身的审计**（2026-08-13 追加）。
> **§10 是对 `2026-08-13 17:26` 第三次快照的复核**（两个文件变更），含 **L1 的降级**与 **`--workers` 新旋钮**两条更正 —— 若 §2/§6.1 与 §10 冲突，以 §10 为准。
> **§11 是「可比性与可信度分级」审计**（A~E 五级）：只有 24 窗 rolling−fixed 是严格同条件配对，但其基线由处理组反推、退化成 1 因子 vs 30 因子；vs 市场等权（敞口 19% vs 100%）与 buffer=20（收益差 95% 非省成本）两条应撤下 —— 与 §9.1 清单冲突处以 §11 为准。
> **§12 是对 `2026-08-14 10:34` 第四次快照的收益方向复核（本文档最新权威结论，与 §1~§11 冲突处一律以 §12 为准）**。两条结论级变化：① §11.8 唯一的施工建议已被执行，30 因子冻结对照给出 **rolling−frozen = −5.8564bp/天、Holm 后 p = 1.0** → **A 级的 `+2.32bp/天` 正式作废**，rolling 选因子的价值判定为「测不出差别」；② 唯一算术上真实的杠杆收敛到**降换手**，同条件 rolling buffer=50 vs 0 = **+1.0632bp/天 @20bp**（毛收益几乎不变、换手 −20%），但 p=0.29577 且 fixed 侧反号 −1.8874bp/天，**不改生产**。另有对我自己三条旧判定的更正（短线网格 24300 而非 8100、`hold_rank_buffer` 是真搜索维度、月胜率池化已修）见 §12.4。

---

## 0. 阅读须知：四条纪律与一个前置事实

**表述纪律（沿用，不可越线）**
- 分数特征 / 分钟特征 / sentiment 是「**未测出**」，不是「无用」。检验方法本身失效（拟合前单变量 IC 筛选、拟合后 ridge≈OLS 的同向族 ~1/k 摊薄），这只能说明结论不置信，**不能反推信号存在**。
- 「模型比随机差」是**被证伪的**：`−29.10bp/天` = `−18.98bp 成本` + `−13.33bp 的 −10% 尾部惩罚` + `+3.21bp 正毛收益`。毛收益为正。
- 每条杠杆都标注三态之一：**已在生产启用** / **有实现但生产未启用** / **有实现但连搜索都进不去**；并且必须标注**真实收益改善**还是**记账假设变化**。
- **「实现 + 测试都在」≠「今天能打开」。** 本轮复核在本仓库里数出四种"fixed-but-not-enabled"变体：① 参数在网格里是字面量常量（L5）；② 有实现但无生产 job 传参（L2 六个 flag）；③ 传了也会被 `fixed_params` 整列压回常量（§6.1 C）；④ **改了也会被自己的 manifest 隔天回滚**（§6.1 J，本轮新发现）。判定任何参数的生产等效值，必须一路追到 `docker/scheduler_jobs.sh` **并且**追到 manifest 回读点。

**前置事实：12 个调度 job 里只有 `realtime` 在跑。**
`docker-compose.yml:60 SCHEDULER_DISABLED=1`，`docker/scheduler_jobs.sh:49-57` 的 `case` 只放行 `intraday-1400-*|realtime`，其余一律 `SKIP job=... reason=scheduler-disabled`。本机对 `docker/scheduler.crontab` 逐条判定：里面**没有任何 `intraday-1400-*` 条目**，所以三类量化 job（`daily-light` 11:40/14:00/15:05、`weekly-full`、`monthly-factor`）以及 `snapshots`/`news-daily`/`top10-eval`/`realtime-weight-shadow` 等**共 11 个全部 SKIP**，只有 `realtime` 命中白名单在跑。

→ **任何"提升收益"的讨论都以恢复调度为前提**。当前状态下线上没有在跑选参与训练，下面所有杠杆的收益都是"恢复后可得"，不是"立刻可得"。

---

## 1. 中心对照：严格研究链已经是强正收益，而生产不传任何严格开关

> **【0813 更正】本节标题的后半句已过期。** 新快照里四个生产 job 都已补传六个严格开关（见 §8.1 末行），但它们仍全部被 `SCHEDULER_DISABLED=1` SKIP。另外「严格研究链是强正收益」这一判断的口径也要换：新的 24 窗正式严格跑是 **净 −0.85bp/天**（毛 +2.89、成本 −3.74），见 §8.6。本节以下内容保留原文作留痕。

这是本轮复核最重要的发现，也是排序的依据。

| | 严格研究链 rolling_v2 | 严格研究链 fixed_33 | 生产盘中链 |
|---|---|---|---|
| 净收益 | **+23.56bp/天** | **+18.96bp/天** | **−29.10bp/天** |
| 成本 | 5.10bp/天 | 3.91bp/天 | 18.98bp/天 |
| 毛收益 | 28.66bp/天 | 22.87bp/天 | +3.21bp/天 |
| 尾部惩罚 | 无（真实 tradable 标签） | 无 | −13.33bp/天 |
| Sharpe | 3.53 | 2.87 | — |
| 敞口 | gross 0.30 | gross 0.30 | **无敞口概念 = 100% 资金** |

来源：`audit_20260812_rolling_fixed_paired_validation.json` 的 `paired_effect.rolling_net_mean 0.0023559` / `fixed_net_mean 0.0018959`、`cost_ablation.stride1_buf0`（turnover .25511 → cost 5.102bp）；`audit_20260812_strict_ridge_sentiment_validation.json` 的 `rolling_ridge_provenance_v2`（9 窗口、67 候选→每窗选 30、purge_span 3、lgbm 9 窗全 skip、Sharpe 3.533、turnover .25511、dd −.10380）。176 个配对交易日，2025-11-17..2026-08-06。敞口零证据：`grep -c "gross_exposure\|slot_weight\|max_weight" intraday_1400/evaluation.py` → **0**。

**两条链的配置差异（这才是差距的来源）**

严格研究链在 `ridge_only.protocol` 里跑的是：`train_target_mode tradable-label`、`pit_index_code 000852.SH`、`purge_horizon true`、四个 strict flag 全 true、`ridge_only true` / `lgbm_weight 0.0`。

生产 `docker/scheduler_jobs.sh` 四个 job 传的是：**以上一个都没传**。grep 结果 —— `pit` → 0 命中、`min-adv20` → 0、`--strict-execution-labels` / `--strict-calendar-factors` / `--strict-announcement-lag` / `--strict-pit-min-price-rows` / `--require-selection-provenance` / `--purge-horizon` → 全 0。而 `audit_20260812_strict_ridge_sentiment_validation.json` 的 `default_behavior` 字段自证：**每一个 strict flag 默认都是 off**。

**必须同时说清的三点，否则这个对照会被误读：**
1. **不是同一口径的苹果比苹果。** 标签不同（`tradable_ret_1d` vs 盘中分钟目标）、股票池不同（`000852.SH` 中证1000 PIT vs `mainboard_active`）、区间不同、敞口不同（0.30 vs 1.00）。按单位持仓资金折算，严格链毛收益 28.66/0.30 ≈ 95.5bp/天，盘中链 3.21bp/天 —— 差距很大，但**这个折算本身放大了口径差异**，不能当作可交付的业绩预期。
2. **严格链自己的封锁项没解除**：配对 CI 跨零、Holm block-5 校正后 `p = 0.4112`（`reject_0_05: false`），JSON 自己的判决是 `rolling_is_directionally_better_but_not_confirmed_alpha_or_promotion_ready`。
3. **来源不可审计**：`selection_provenance_gate.default_enabled: false`，且探针**拒绝** `factor_selection_lh1000_cont`（那份冻结手工因子清单，全仓库 grep 只有 3 处命中且全是消费者）。这意味着严格链的因子清单来源无法追溯。

→ 所以正确的表述是：**"把已实现但未启用的严格链在生产打开"是收益杠杆里最高性价比的方向，但它的定量收益尚未通过显著性检验，而且"已实现"≠"今天传得上去"**——六个 flag 里四个会立刻 `RuntimeError`，见 §6.1 F。

---

## 2. 杠杆清单（按性价比排序）

### L1 —— 成本口径重定价 20bp → 7~10bp ｜ **+9.5~12.4bp/天** ｜ 真实收益改善

> **【0813 更正 · 降级，见 §10.3】** 本条的前提（「万2.5 佣金」）**不适用于用户的实际账户**。对方 `下一轮调研记录_20260812.md:583` 记下用户补充的费率：**佣金万5，单边 `0.0005`、双边合计 `0.0010`**；叠加卖出印花税 0.05% 后研究 roundtrip ≈ **`0.0020`（20bp）**，**与当前默认值一致**。所以「改一个数就落袋 +9.5~12.4bp/天」不成立，本条从「决定符号的那一项」降级为「**敏感性场景，需真实账单/成交回单核实**」。真正成立的部分是：注释里「印花税卖出千1」这个**分项**确实过时（2023-08 已减半为 0.05%），但因为原注释同时高估了税、低估了别的费项，**合计仍落在 20bp**。另记 rolling 的 break-even roundtrip = `0.13792985 / 89.3 = 15.45bp`（`:562`）—— 当前 20bp **在盈亏平衡点之上 4.55bp**，这才是 24 窗净收益为负的直接原因。

**状态：有实现但生产未启用**（是可配置项，但生产写死默认值）。

`docker/scheduler_jobs.sh:14-19` 的注释自证口径来源：

```
# 真实交易成本（roundtrip）：佣金双边 万5×2=0.001 + 印花税卖出 千1=0.001 = 0.002。
export QUANT_BT_COST_ROUNDTRIP="${QUANT_BT_COST_ROUNDTRIP:-0.002}"
```

两个分项都过时：印花税 2023-08 已减半为**卖出单边 0.05%**（注释按 0.1% 计）；万5 佣金对当前零售常见费率（万2.5 及更低）偏高。按佣金万2.5 双边 + 印花税 0.05% 单边重算 ≈ **0.001**（10bp），乐观口径 ≈ 7bp。

按 `cost_ablation.stride1_buf0` 的 turnover `0.25511`：20bp → 成本 5.10bp/天；10bp → 2.55bp/天。**严格链上省 2.55bp/天**。而生产盘中链是 100% 敞口每日全换手，成本 18.98bp/天 → 20bp 降到 10bp 直接省 **9.49bp/天**，降到 7bp 省 **12.34bp/天**。

**这是唯一"改一个数就落袋"且量级超过功效边界的杠杆**，且方向上无争议（是把口径改正确，不是调参调优）。

**落地不是一处**：`quant/backtest.py:38-40` 走 `QUANT_BT_COST_ROUNDTRIP`（可用 env 一处生效），但 `intraday_1400/` 与 `realtime/` 下另有 **7 处硬编码 `0.002`** 不读 env：`structural_combo_experiment.py:232`、`forward_evaluation.py:30`、`direct_return_experiment.py:63`、`daily_minute_enhancement.py:61`、`offline_race.py:17`、`target_redesign.py:113`、`structural_combo_holdout.py:301`、`adaptive_exit_replay.py:31`（另有 `pipeline.py:184/229`、`evaluation.py:253` 走 `INTRADAY_1400_ROUNDTRIP_COST` 这个**不同名**的 env）。改口径必须同时对齐这两组 env 名，否则研究与生产再次分叉。

**注意**：这不会让盘中链翻正（−29.10 + 12.34 = −16.8bp/天），因为 `−13.33bp` 尾部惩罚仍在。它是必要但不充分。

---

### L2 —— 在生产打开严格链（`tradable-label` + PIT + purge + strict flags）｜ **不是零新代码：六个 flag 里四个今天会直接抛异常** ｜ 记账假设变化 + 真实收益改善（两者混合）

**状态：有实现但生产未启用，且大部分"启用不了"**——先前把这条写成「零新代码，只要在 `scheduler_jobs.sh` 补传六个参数」，**本轮复核推翻了这个判断**。逐条追到 raise 语句后：只有 `--train-target-mode tradable-label` 与 `--purge-horizon` 今天能跑，其余四个（`--strict-execution-labels`、三个 strict panel flag、`--pit-index-code`、`--require-selection-provenance`）各自撞上一个缺失的上游产物而 `RuntimeError`；另有第七个 `--enforce-c30-gates` 会因四个 job 的 `refresh_months=1` 而 `ValueError`。**完整失败矩阵、根因（四个"零生产调用者"的生产函数）、以及必须按序执行的四步门控见 §6.1 F。**

参数实现位置与生产现状（实现确实都在，`quant/scheduled_workflow.py:1301-1311`，且有测试）：

| 参数 | 实现位置 | 生产现状 | 今天能否打开 |
|---|---|---|---|
| `--train-target-mode tradable-label` | `scheduled_workflow.py:1311` 默认 `"baseline"`，`:1037-1038` 仅在 != baseline 时转发 | 未传 → baseline | ✅ 但会打断 `weight_shadow`（§6.1 G）、并踩缓存坑（§6 批2） |
| `--purge-horizon` | 有独立顶层开关 | 未传（h=1 现役腿危害小，见 §3） | ✅ **L2 里唯一真·零成本的一支**；注意 `tradable-label` 下有跳窗风险（§6.1 I） |
| `--strict-execution-labels` | `:1301-1308` `store_true` | 未传 | ❌ 缺 `trading_status_history.parquet`；且与 baseline 互斥 |
| `--strict-calendar-factors` / `--strict-announcement-lag` / `--strict-pit-min-price-rows` | 同上 | 全未传 | ❌ 等价于强制 `--rebuild` 全面板 |
| `--rolling-factor-select` | **没有顶层开关**，只能靠 `strategy-mode candidate-upgrade` 或 manifest 回读 `incumbent_rolling_factor_select`（`:1013-1015`）打开 | `monthly-factor` 走 candidate-upgrade → 有；`daily-light`/`weekly-full` 走 incumbent-refresh → 仅当 manifest 里已写 `rolling_factor_select: true` 才有（即 S10 探针要查的正是这个） | ⚠️ 加顶层开关只需 2 行 |
| `--pit-index-code` | `full_train_batched.py:1916` 默认 `""` | **`scheduled_workflow.py` 里 `pit_index_code` 零出现 → 连转发都不可能** | ❌ 缺两张 PIT 参照表 |

**PIT 股票池是第三例、也是最彻底的一例「fixed-but-not-enabled」**：`full_train_batched.py:48-107` 的 `resolve_pit_window_universe` 是**正确实现**（在每个 `train_start` 上用 `security_master.list_date/delist_date` ∩ 指数成分史 `in_date/out_date` 求交），但 (a) 默认空串，(b) `scheduled_workflow.py` 完全不认识这个参数，(c) `scheduler_jobs.sh` grep `pit` = 0，(d) 上游数据生产者 `daily_update.py:560 refresh_pit_reference_data` **零调用者（测试外）**，其 docstring 直接写 "without activating them"。四层同时断开。

**分类必须诚实**：打开 `tradable-label` + strict labels 的一大部分是**记账假设变化** —— 它把"不可买/不可卖"从 `−10%` 惩罚改成真实 T+1..T+3 出清，账面变好但钱早已按 T+2/T+3 实际成交过。真实改善的部分是 PIT 股票池与 purge：它们移除的是**在位者的历史优势幻觉**，方向上让历史指标**变差**而非变好，但让"哪个候选更好"的判断变可信。

→ **所以 L2 的价值主要不是"多赚多少 bp"，而是"让 L4/L5 的实验能得出可信结论"**。它仍排第二，但理由要改：**不再是"零代码成本"，而是"它是下游一切实验可信性的前置条件"**。落地成本已从"改四行 shell"上调为"四个上游产物 + 一次全面板重建 + 两处下游修复"。

---

### L3 —— 拉长持有期 / 非重叠调仓（h1→h3、stride 3）｜ **成本 20bp/天 → 6.04bp/天，+14bp/天** ｜ 真实收益改善，但证据被污染

**状态：有实现但连搜索都进不去**（见 L5，stride 在网格里是常量）。

算术是确定的：每日全换手 = 每天付一次 roundtrip；h3/stride3 = 每 3 天付一次，成本摊到 `20/3.31 ≈ 6.04bp/天`（按 `cost_ablation.stride3` 实测 turnover .28814 折算 5.763bp/天，periods 从 176 降到 59）。

**但仓库自己的实测反对它**：`cost_ablation` 里 `stride2` 净 `+0.31420` / `stride3` 净 `+0.13177`，而 `stride1_buf0` 是 `+0.49844` —— **拉长 stride 让总收益显著下降**，省下的成本抵不上信号衰减（这是 h=1 短线信号的正常表现）。`opus5_review_request_20260812.md` 也明确写 `stride=2/3` materially reduces performance。

另一份 h3 证据（净 +21.97bp/天）**不可用**：`h3_strict: false`、Holm `p = 0.9492`、45.51% 现金占比。

→ 正确结论：**"拉长持有期"在成本侧算术上确定有利，但在信号侧实测有害，净效应目前是负的**。它值得做的是**扫 horizon**（P00，`_minimum_validation_months` 已就位使之安全），而不是直接把 stride 调大。排第三是因为它是唯一能同时动成本与信号结构的方向，但必须先做实验而非先改生产。

---

### L4 —— 修 `selection_score` 敞口归一化 + 重跑网格 ｜ 量级未知 ｜ 真实收益改善（前提是先修评分）

> **【0813 更正：本条已修，从杠杆清单移出。】** `watchlist_grid.py:545-565` 已把 `avg_annual_return` / `worst_drawdown` / `avg_turnover` 三项全部除以 `gross`。剩下的只是"重跑一次网格看冠军是否变"。见 §8.1。

**状态：已在生产启用（但评分函数的敞口维度是人工制品）。**

`watchlist_grid.py:543-551` 的 `selection_score` 六项里，成本口径修好后只剩 `worst_drawdown × 0.25`、`avg_turnover × 0.05`（两个惩罚项）和 `avg_annual_return × 0.15`（一个奖励项）携带敞口。

**⚠️ 更正交接文档 §3.2 的验算**：那次算出"单调递减、恒选下界 0.18"，是因为固定 `Sharpe=0.8` 且**把 `avg_annual_return` 项当作不随敞口缩放**。实际 `_evaluate_returns:204-210` 里 `annual_return` 由 `nav` 复利算出，必然随敞口放大。用严格链 rolling_v2 实测量级（年化 78.4%、dd −10.38%、turnover .2551 @ gross 0.30）本机重算网格三档：

| gross | 日收益 | 年化 | 年化波动 | Sharpe | selection_score |
|---|---|---|---|---|---|
| 0.18 | 14.14bp | 42.8% | 10.00% | 4.275 | +4.4385 |
| 0.24 | 18.85bp | 60.7% | 13.33% | 4.554 | +4.7363 ↑ |
| 0.30 | 23.56bp | 80.9% | 16.67% | 4.856 | +5.0611 ↑ |

**单调递增，恒选上界 0.30（= 现役取值）。** 且 Sharpe 本身也随敞口递增 —— 因为 `annual` 是复利（凸），`annual_vol` 是精确线性，所以 Sharpe 并非严格尺度不变，方向同样偏高敞口。

真实结论是**符号取决于策略强弱**：临界年化 `a* = (0.25×0.1038 + 0.05×0.2551)/0.15 = 25.8%`（在 gross 0.30 处）。

- 年化 > 25.8% → 递增 → 恒选 **0.30**（严格链 78.4% 落在此侧）
- 年化 < 25.8% → 递减 → 恒选 **0.18**（生产盘中链年化 −52.0% 落在此侧，本机验算 0.18→−1.7345 递减到 0.30→−1.8108）

→ **缺陷仍然成立，但性质要改述**：不是"恒选下界"，而是 **`gross_exposure` 这一维的选择完全由 `selection_score` 的项系数决定、与"该敞口是否真的更优"无关** —— 强策略永远顶到上界、弱策略永远贴到下界，两种情形都是人工制品。修法不变（`worst_dd / gross`、`turnover / gross`、`annual_return / gross` 三项都要归一化），但"旧冠军 gross=0.30 全部失效、必须重跑"这个推论**不成立** —— 在严格链量级下评分仍然选 0.30，冠军参数不变。

**因此 L4 的重跑必要性下调**：`selection_score` 该修（它让 gross 维度形同虚设），但它**不构成"必须重跑 8100 网格"的理由**。真正强制重跑的是 L5（buffer 变成维度后网格行数变了）。

**另一个更重要的发现（见 §6.1 施工细节 D）**：稳态日更走**邻域剪枝**，`gross_exposure` 被锁死为精确相等，所以生产日更里这一维**根本不搜索** —— 本机复现：邻域剪枝后只剩 243 行（全网格的 3.0%），且 `gross` 取值集合 = `[0.3]`。修 `selection_score` 只影响 `monthly-factor` 那一次全网格跑。

---

### L5 —— 降换手参数进入搜索维度（stride / hold_rank_buffer）｜ **低于功效边界，当前不可判定** ｜ —

> **【0813 更正：buffer 支已维度化，stride 支仍全封。】** `watchlist_grid.py:167-186` 两个分支都已 `for buffer in (0,1,2)`，三处 `fixed_params` 改为"仅显式请求才放键"；但 `rebalance_stride` 仍是字面量 `1`、仍被三处 `fixed_params` **无条件**押回、`scheduler_jobs.sh` 仍零命中。S16（先定 buffer 值域）依然该做 —— 对方自己的 C44 实测（`解决清单_供Opus复核.md:497`）也复现了「`buffer=2` 在该样本无效果」。见 §8.1 / §8.3-1。

**状态：有实现但连搜索都进不去。**

`watchlist_grid.py:167-168` 把 `rebalance_stride: 1` / `hold_rank_buffer: 0` 写成**字面量常量**，8100 行网格两列各只有一个取值；`run_grid:822-823` 的 `combos[key] = value` 是整列赋标量，**覆盖成另一个常量而非维度**；`docker/scheduler_jobs.sh` grep `rebalance-stride|hold-rank-buffer|short-rebalance|short-hold` → **零命中**。实现侧全链路都齐（`backtest._apply_rebalance_stride:124` 含 fail-closed 日历校验、`_select_with_rank_hysteresis:168`、`_selection_summary:532` group_cols、`_upgrade_gate` param_keys、测试 `:1348-1436`）。

这是 `HANDOFF_20260812_1600.md` §3.1 判定的 P0。**但从"提升收益"角度它排在第五，不是第一** —— 因为实测差异全部低于功效边界：`cost_ablation` 的 `buf2` 净 `+0.51573` vs `buf0` `+0.49844`，差 1.73pp / 176 天 ≈ 1.0bp/天；`buf20` `+0.48357`、`buf50` `+0.36291` 都更差。**配对 MDE ≈ 28bp/天，1bp 的差异连符号都测不出**。JSON 自己带 `decision: do_not_change_production_stride_or_hold_buffer`。

**本轮新证据：`{0,1,2}` 这个具体值域在真实数据上大概率完全无区分度。** `audit_20260811_portfolio_hysteresis_validation.json` 的 `buffer_diagnostic`（生产 `active_quant_short_predictions.parquet`、180318 行 / 60 个交易日、Top2 gross 0.30）：

| hold_rank_buffer | avg_turnover | sharpe | total_return |
|---|---|---|---|
| 0 | 0.279310 | 34.4032 | 0.512826 |
| **2** | **0.279310** | **34.4032** | **0.512826** ← 与 0 逐位相同 |
| 20 | 0.258621 | 36.9323 | 0.536997 |
| 50 | 0.248276 | 34.6479 | 0.529718 |

该文件的 `interpretation_limit` 自己写明「buffer 2 had no effect in this sample」。→ **若按 `{0,1,2}` 扩维，3.18 倍算力换到的很可能是三组逐位相同的指标**（`{1}` 无实测，但夹在两个相同值之间）。**要么把值域抬到有区分度的量级（如 `{0, 5, 20}`），要么先在生产 artifact 上跑一次 buffer 敏感性再定值域** —— 后者是只读操作，已作为探针 **S16** 写入 §5 与阶段 0。

→ 它作为**工程缺陷**是 P0（能力被静默阉割、`_selection_summary` 保留了键却永远只有一个取值 = 假的可搜索性），作为**收益杠杆**是"打开后才知道有没有用"，收益期望约 1bp/天。两个判断不冲突，不要混为一谈。**施工细节远不止改字面量：4 处必改（含一处会让改动 100% 静默失效）+ 4 项必评估，见 §6.1 L。**

---

### L6 —— `swing_7_15` 从不训练、预测与短线逐字节相同 ｜ 量级未知 ｜ 真实收益改善（若修）

**状态：已在生产启用（作为设计选择）。**

`scheduled_workflow.py:82-83` `DERIVE_FROM = {"swing_7_15": "short_1_3"}` 把波段腿从 `TRAIN_STYLE_ORDER` 剔除，有明确注释说是为省一半训练耗时。`audit_20260811_c33_real_active_horizon.json` 实测确认：`requested_horizons {short:1, swing:10}` 而 `corrected_prediction_horizons {short:1, swing:1}`，7 个比对列**全 true** → **波段腿的预测与短线腿逐字节相同**。

所谓"两条腿"实际是一条腿用两套持仓规则跑。真正训练一条 h=10 的模型是有可能带来独立 alpha 的方向（也是唯一"新增信号"性质的杠杆），但代价是训练耗时翻倍，且 h=10 才是 purge 真正重要的那条腿（验证段 48% 标签伸入测试段）。

---

### L7 —— 重跑 sentiment / 分数特征实验（口径对齐 + ridge 泄漏已修）｜ 量级未知 ｜ 记账口径修正

**状态：有实现但生产未启用**（三个 arm 全 `enabled: False`）。

`audit_20260812_strict_ridge_sentiment_validation.json`：三个 sentiment arm 的 `holdout_ic_1d` 分别 0.01461/0.01502/0.01502（**过门**），唯一未过的门是 `holdout_direction_1d` 0.4958/0.4942/0.4942 vs 阈值 0.50 —— **差 0.4~0.6 个百分点**。JSON 自己带 `prohibited_claim: sentiment_is_generally_useless`，判决措辞是 `keep_disabled_under_this_protocol`（"在这个协议下"，不是"永远"）。

`model.py:1026-1029` 的 ridge `predict_start` 泄漏已修 → **历史那批 sentiment 报告的 ridge 臂 test 指标是在 valid 段上算的，全部作废、必须重跑**。同时评估口径要对齐主线：`sentiment_feature_experiment.py:211-216` 现在是 `filter_untradable=False`、`positive_only=False`、`max_weight=1.0/top_n`（100% 敞口）、无 `no_refill`/`require_tradability`。

→ 这是**唯一有正面 IC 证据的未启用信号源**，但离过门差一点点，且此前的检验都不可比。属于"值得重做一次实验"而非"值得直接上线"。

---

### L8 —— `QUANT_BT_SELL_ROLL_MAX_DAYS`、行业分化尾部惩罚 ｜ **≈0bp** ｜ 不建议

- `backtest.py:31-33` 默认 3，`scheduler_jobs.sh` 从未 export → 生产永久为 3。上调的收益上界 ≈ **1.13bp/次建仓**，且会拉长 purge span（`_purge_span:403-408` 的 `tradable-label` 分支 = `horizon + bt_sell_roll_max_days() - 1`）并**使整个标签缓存失效**（且该 env 改标签却不进哈希，S17）。
- 行业分化的 ±20% 尾部惩罚：**生产影响 0bp**，因为 universe 的 `_MAINBOARD_PREFIXES` 已排除 300/688 创业板科创板。

---

### L9 —— 尾部惩罚 −10% → 真实 T+1..T+3 出清 ｜ 账面 +12.21bp/天 ｜ **主要是记账假设变化，不是真钱**

`intraday_1400/evaluation.py:144-149`：`INTRADAY_1400_UNSELLABLE_RETURN` 默认 `-0.10`，不可卖持仓按 `-0.10 + cost` 记账。改成真实出清后惩罚从 `−13.33bp` 降到 `−1.12bp`，账面 **+12.21bp/天**。

**但这几乎不是真实收益**：`INTRADAY_ASOF_1400_PLAN.md:405` 自证 92.9% 的受阻持仓在 T+2/T+3 已出清、且一致的 −10% 过于严苛 —— 也就是说钱早就按真实价格成交了，改的只是账本。同一行还显示自适应退出让**同一批持仓变得更差**（分钟链 −6.33bp、日线链 −30.66bp）。

我把它排在最后而不是按 bp 排第一，**正是因为 bp 数字最大的这一条最不值钱**。它的正确用途是：修正账本以便让 §1 那个 −29.10bp 的对照不再被 −13.33bp 的假设主导，从而看清真实的 `+3.21bp` 毛收益。

---

## 3. 功效边界：为什么 L4~L9 目前"不可判定"

> **【0813 更正】本节的三个数字是旧样本（176~225 天 Top2 主板）的量级，方向对但不能套用到 24 窗正式跑上。** 24 窗（477 天）的实测口径是：日 σ ≈ **51.6bp**（本机由 Sharpe −0.2623 反解）、配对 σ_diff ≈ **49.1bp**、配对 SE ≈ **2.25bp**、**配对 MDE80 ≈ 5.6bp/天**。样本翻 2.7 倍 + 换手减半让 MDE 从 28bp 降到 5.6bp —— 但观测效应量也同步缩小（+4.60 → +2.32bp/天），**t 值几乎没动**（0.64 → 1.03）。逐条清单与检验方法审计见 **§9**。

用 176~225 天配对样本算出来的检验能力，是所有杠杆的硬约束：

- 单日收益标准差 σ_p ≈ **234bp/天**
- 单样本 MDE ≈ **44bp/天**
- **配对 MDE ≈ 28bp/天**（σ_diff ≈ 152bp/天）

只有 **L1（9.5~12.4bp，但它是算术恒等式不需要检验）** 和 **L3 的成本侧（14bp，同样是算术）** 在量级上有意义；L5 的 1bp、L7 的 0.5pp 方向胜率差、L8 的 1.13bp **全部远低于 28bp**，用现有样本**连符号都测不出**。

**这解释了为什么"什么探索都超不过日更"**：候选之间的真实差异约 16bp/天 < 配对 MDE 28bp/天。这不是 bug，是样本量不够，只能靠延长评估窗或提高配对精度（block bootstrap 已在 `daily_optimization_pipeline.py:314-338` 落地并强制 CI 下界 > 0）解决。

**推论（这是本节最重要的一条）**：**凡是靠回测数字排序的杠杆，当前都排不出可信顺序。** 所以上面的排序不是按"实测收益大小"排的，而是按 **"算术确定性 × 落地成本"** 排的 —— L1/L2 之所以在前，是因为它们不依赖显著性检验：L1 是把过时费率改正确，L2 是把已实现的正确路径打开。

---

## 4. 晋级前必须清除的封锁项

> **【0813 更正】** 下面「本轮复核新增四条施工封锁项」G/H/I/J **在新快照里已全部处理**（I 是"总量 fail-closed，逐窗仍静默"），见 §8.1 / §8.2。而本节开头的三条**结论晋级**封锁项**一条都没清除**（provenance gate 仍未在任何 job 传入，见 §8.4）。

任何"这个改动能提升收益"的正式结论，在下列三条清除前都不能提交晋级：

1. **`factor_selection_lh1000_cont` 来源不可审计** —— 全仓库 grep 只有 3 处命中且**全是消费者**（`select.py:159` / `pipeline.py:72` 的默认名是裸 `factor_selection`），是一份来源不明的冻结手工清单（50 行），而每窗滚动选择只能在**它的子集内**重挑。严格链的 Sharpe 3.53 建立在这份清单上。
2. **`selection_provenance_gate.default_enabled: false`** —— 来源校验门默认关闭，且探针**明确拒绝**上述清单。
3. **Holm block-5 校正后 `p = 0.4112`，`reject_0_05: false`，配对 CI 跨零** —— 严格链 vs 固定链的 +4.60bp/天 优势**未达显著**。

另外两条方法论债：`stability_decision`（`watchlist_grid.py:706-718`）的月胜率仍跨 3 个 horizon 池化同 7 个日历月当 21 独立样本；`pipeline.py:149-152` 是教科书式早停泄漏（`predict=valid`）—— 现役不受影响，但**据它得出的历史结论全部作废**。

**本轮复核新增四条"施工封锁项"**（性质不同：上面三条挡的是*结论晋级*，下面四条挡的是*改动落地*）：
- **G** `tradable-label` 会让 `realtime-weight-shadow` 静默失效（列在但全 NA） → 恢复调度前必修，**阻塞级**。
- **H** `DailyICCache` 磁盘键漏掉全部标签口径字段，且在窗口缓存关闭时仍启用 → 换标签后会读到旧口径的 IC。
- **I** `--purge-horizon` + `tradable-label` 下 `_purged_end_by_code` 只 `print` 不抛异常 → **静默跳窗**。
- **J** stride/buffer 改动会被 manifest 回读**隔天回滚**（`_apply_incumbent_training_config` 无 `cli_*` 保护）。

四条的 `file:line` 与修法见 §6.1 G/H/I/J。

---

## 5. 前置：五个 10 分钟可结案的只读探针

这些都只需在生产主机上做只读操作，且**它们的结果会改变上面的排序**，应先做：

| 编号 | 动作 | 判读 |
|---|---|---|
| **S10** | 读任意一次训练 summary 的 `purge_horizon` 键（写入点在 `full_train_batched.py:1842`，注意旧文档记的 `:1179` 已漂移），或读 `quant_data/full_a_2018_wide/active_quant_model.json` 的 `params.rolling_factor_select` | 决定共线性解药（`max_ic_corr=0.85`）在生产日更里**是否存在**。若不存在 → L7「分数/sentiment 未测出」的结论就能收敛；若存在 → L2 里 rolling 那一支可以从待办移除 |
| **S9** | 比对 `quant_data/price/` 的 parquet 数与 universe 行数 | 若退市 parquet 仍在，则研究口径比生产更诚实，"研究跑不出生产分数"有一部分只是**口径差异**而非模型问题 |
| **S14** | `ls -l quant_data/full_a_2018_wide/factor_selection_lh1000_cont.parquet` 看 mtime 与行数 | mtime 远早于今天 + 行数=50 → 证实 §4.1 的封锁项，严格链结论需重新奠基。**注意口径**：`audit_20260812_strict_ridge_sentiment_validation.json` 的 `factor_selection_provenance` 记 **rows=33**（不是 50），且 `producer_metadata_present/selection_window_metadata_present/label_provenance_present` 全 `false`、`promotion_blocker: true`。真值以生产文件为准 |
| **S15（新增）** | `ls -l quant_data/full_a_2018_wide/active_quant_short_predictions.parquet` 看 mtime | **这是最该先看的一条**。`realtime` 是唯一在跑的 job，它读这个文件（`realtime/config.py:113-115`）。若 mtime 停在调度关闭那天，说明实时层**一直在用一份越来越旧的预测**（`engine.py:208-215` 只用 mtime 判变化、**不校验新鲜度**，`grep stale/max_age` 在 reference/engine 里零命中）。这是当前唯一**正在发生**的风险，与所有杠杆无关 |
| **S16（本轮复核新增）** | 在生产 `active_quant_short_predictions.parquet` 上扫 `hold_rank_buffer ∈ {0,2,5,10,20,50}`（只读预测、只写 `/tmp`） | 决定 L5 的值域，也是**唯一能让阶段 2 少烧 3.18 倍算力**的探针。仓库自己的 60 天记录（`audit_20260811_portfolio_hysteresis_validation.json`）显示 `buffer=2` 与 `buffer=0` 三个指标**逐位相同**，只有 20/50 才动换手 → 若在更长样本复现，`{0,1,2}` 扩维不该做，换 `{0,5,20}` 或放弃 L5。详见 §6.1 L |

---

## 6. 执行顺序（**与 §2 的价值排序不同**，落地按本节）

§2 的 L1~L9 是**价值排序**（算术确定性 × 落地成本），回答"哪条更值得做"。但它**不能直接当施工顺序**。本节按"是否使缓存/回测失效"重新分组 —— 分组依据见 §6.1 的施工细节复核（本轮实测，**推翻了我先前几处施工判断**）。

### 阶段 −1 —— 一件现在就该确认的事（不是杠杆，是风险）
`realtime` 是白名单里唯一在跑的 job（本机对 crontab 逐条判定：12 个 job 名里只有 `realtime` 命中白名单，`daily-light`/`weekly-full`/`monthly-factor`/`snapshots`/`news-daily`/`top10-eval` 等 11 个全部 SKIP）。它读 `active_quant_short_predictions.parquet`（`realtime/config.py:113-115`），而该文件的唯一写入者是被 SKIP 的 workflow。`engine.py:208-215` 只用 mtime 判"是否变化"，**全链路无新鲜度校验**。

→ 先做 S15（见 §5）。若预测已陈旧，这是当前唯一**正在发生**的问题，优先级高于本文档任何杠杆。（`realtime/paper_trader.py:4` 确认是虚拟账户、非真实下单，所以是研究失真而非资金风险。）

### 阶段 0 —— 只读探针（10 分钟，零风险，仓库零修改）
S15 / S10 / S9 / S14 / S16（见 §5）。**它们的结果会改变 §2 的排序**：S10 若证明 `rolling_factor_select` 生产已启用，L2 就少一支；S14 若证实 `factor_selection_lh1000_cont` 无 provenance，严格链 Sharpe 3.53 的说服力要打折、L2 需重新奠基。顺带确认 `SCHEDULER_DISABLED=1` 是否该恢复（`scheduler_jobs.sh:48` 注释 `while research disables other schedules` 表明是研究期刻意关的）。

**S16 单独强调（本轮复核催生，直接决定阶段 2 该不该扩 buffer）**：仓库已有一次 60 天的记录（`audit_20260811_portfolio_hysteresis_validation.json`）显示 **`buffer=2` 与 `buffer=0` 三个指标逐位相同**、只有 20/50 才动换手。→ 若在更长样本上复现，则 `{0,1,2}` 的扩维**不该做**，应换成 `{0,5,20}` 或直接放弃 L5。**这是唯一一条能让阶段 2 少烧 3.18 倍算力的探针，务必在改 `_base_combos` 之前跑。**

### 阶段 1 —— 按"缓存失效等级"分批改（**这是本轮复核改动最大的一节**）
本机读 `full_train_batched.py` 的三层缓存判定后，先前"L1/L4/L5 都会使回测作废、所以要一次改完"的判断**不准确**。实际分级：

| 改动 | 是否使窗口缓存失效 | 证据 |
|---|---|---|
| L1 成本口径 | **不失效** | `recipe_params`（`:1281-1307`）**无 cost 字段**，本机 grep `cost` 在 `full_train_batched.py` 全文零命中 → 改成本**无需重训**，只需重跑网格/回测 |
| L4 `selection_score` | 不失效（不参与训练） | 只影响 `_selection_summary` 排序 |
| L5 buffer 维度 | 不失效（不参与训练） | 只改网格行数 |
| L2 `--train-target-mode tradable-label` / `--strict-execution-labels` | **全窗口重训** | `_label_recipe_params:422-448` 在非 baseline 时注入 `train_target_mode`、`sell_roll_max_days`、`trading_status_source_signature` → `recipe_sig` 变 |
| L2 `--pit-index-code` | **全窗口重训 + 每窗口独立 sig** | `:1342-1345` 用 `pit_universe_manifest_hash` 重算逐窗 sig；`_legacy_window_cache_allowed:210-214` 在 pit 或 strict 下关闭 legacy 提升通道 → 存量缓存全不可复用 |
| L2 `--require-selection-provenance` + `--rolling-factor-select` **同时**打开 | **窗口缓存彻底关闭** | `_rolling_cache_allowed:217-221` 返回 False → `recipe_params=None` → `cache_dir=None`（`:1305-1312`）→ 每次全量重算，日更再也没有缓存 |

**因此改成两批，不是一批：**

**批 1（便宜，不动训练）** —— L1 成本口径 + L4 `selection_score` + L5 buffer 维度 + `stability_decision`。改完只需重跑网格（读现成 predictions，见阶段 2），**不重训模型**。⚠️ L5 那一支的施工面比先前估计的宽得多：**4 处必改（含一处会 100% 让改动静默失效）+ 4 项必评估**，见 §6.1 L；且是否值得做取决于阶段 0 新增的 S16 探针。

**批 2（昂贵，全窗口重训）** —— L2 严格链开关。**必须单独一批，而且本轮复核后它不再是"补传六个 flag"，而是一条有四个前置产物的门控序列 —— 详见 §6.1 F。** 除那四个前置外，还有两个缓存层面的坑：

1. **`price_source_signature` 会让缓存每天全失效。** `_label_recipe_params:433` 在非 baseline 时注入 `_price_source_signature()`（`:192-201`），它是 `price/` 目录**全部** parquet 的 `(name,size,mtime_ns)` 哈希。而 `daily-light` 传 `--force-latest-price`（`scheduler_jobs.sh:161`）每天重写 price parquet → **该签名每天都变 → 打开 tradable-label 后日更的窗口缓存每天 100% miss**。对比 `_window_month_signature:175-179` 的 docstring 明确说它是**刻意**设计成按窗口稳定的（只有触及被刷新月份的窗口才 miss）—— 两者设计意图相反，`price_source_signature` 是全局的，会摧毁这个优化。**这是"打开严格链"最大的隐藏成本，不是几个 flag 的事。**
2. **`--require-selection-provenance` 与 `--rolling-factor-select` 不能同时开**（会彻底关闭缓存）。而 §4 的封锁项要求解决 provenance —— 两个需求直接冲突，**必须先重建 `factor_selection` 的 provenance 元数据，而不是靠打开这个 gate**。

**`stability_decision` 归入批 1**：理由不是它本身值钱，而是它会否掉本该通过的候选（`avg_sharpe_gain +2.009`、3/3 改善，却因伪重复的 `monthly_win_rate 0.5238` 被 `stability_gate` 拦掉）—— 不修它，阶段 2 跑出来的结果照样读不出。

### 阶段 2 —— 跑一次，拿新基线
重跑网格（**不重训**，`run_grid:813` 直接读现成 `*_predictions.parquet`）+ 视批 2 决定是否重训。

**网格规模：改 `hold_rank_buffer ∈ {0,1,2}` → 24300 行**（本机复现：当前 8100 = `5(ic) × 5(top_n) × 3(gross) × 6(rq) × 6(pq) × 3(naive)`，`watchlist_grid.py:152-169`；×3 = 24300 行且全唯一，若连 stride 一起交叉则是 72900）。⚠️ **这个值域先等 S16 定论** —— 若 `buffer=2 ≡ buffer=0` 在长样本复现，则 `{0,1,2}` 是白烧 3.18 倍算力换三份相同结果，应改 `{0,5,20}`（规模不变，区分度有）或整条不做。

**但"24300 组合"要和三个别的数字分清（本轮实跑校准）：**

| 量 | 当前 | 扩维后 |
|---|---|---|
| 组合数（= 算力单位） | 8100 | **24300** |
| 网格 parquet 行数（`:885` 落盘） | 16200 | **48600** |
| `_selection_summary` 行数 | 8100 | **24300** |
| 单核耗时（467日×3193票实测） | 1.35 core-h | **4.28 core-h（3.18×）** |
| @12 workers 墙钟 | 6.7 min | **21.4 min** |（【0813 更正】新代码默认 8 workers，见 §6.1 E → 10.1 min → **≈32 min**）

`SHORT_GRID_HORIZONS = [1,2]` **不参与组合枚举**（`_evaluate_combo_process:782` 消费它，每个 `(combo,horizon)` 出一行）→ 所以"×3"对组合数是对的，但落盘行数是 ×2 之后再 ×3。

**两个反向修正：**

- **↑ 更贵但不是数量级**：`buffer > 0` 会让快路径从**全矩阵向量化**退化成**逐日 Python 循环**（`_fast_combo_metrics:426` vs `:443` 的 `np.argpartition`）。先前估"5~10 倍"**过头了** —— 本机实测单组合只贵 **8~9%**（0.5985s → 0.6487/0.6531s），总倍数 **3.18×** 而非 3.00×。
- **↓ 更便宜**：稳态日更**根本不跑全网格**。`daily-light`/`weekly-full` 走 `incumbent-refresh`，`:1401-1411` 从 manifest 读 `champion_score_params` 当 `neighborhood`，`:827-846` 剪枝到 **243 行**。且邻域 `limits` **不含 `hold_rank_buffer`** → 日频严格 ×3：真实冠军锚点 54 → 162 组合，代码默认锚点 486 → 1458 组合。

→ **结论：3.18 倍只落在 `monthly-factor` 的全网格上（6.7 min → 21.4 min），日频绝对成本可忽略。** 算力从来不是这条的瓶颈。

**⚠️ 但在跑之前必须先定 buffer 的值域。** 仓库自己的 `audit_20260811_portfolio_hysteresis_validation.json` 在生产 artifact 上实测 `buffer=2` 与 `buffer=0` **三个指标逐位相同**（见 L5）。**按 `{0,1,2}` 跑 = 3.18 倍算力换 0 信息量，还附带把晋级门 top-20 的有效多样性从 20 稀释到 ~7**（§6.1 L-a）。→ 先做只读的 buffer 敏感性扫描（`{0,2,5,10,20,50}`）定值域，再决定扩哪几档 —— 已作为 **S16** 落进 §5 与阶段 0。

**另外三条施工细节（本轮实测，先前判断有误）见 §6.1。L5 的完整施工清单（4 处必改 + 4 项必评估）见 §6.1 L。**

### 阶段 3 —— 探索性实验（依赖阶段 2 的新基线）
L3 扫 horizon（`_minimum_validation_months` 已就位使之安全）、L6 真训一条 h=10 波段模型、L7 重跑 sentiment（口径对齐主线）。这些是**做实验**不是修问题。

### 不做
L8（`QUANT_BT_SELL_ROLL_MAX_DAYS` ≈1.13bp/次；且它**进哈希**——`_label_recipe_params:436` 在非 baseline 时注入 `sell_roll_max_days`，所以在严格链下改它会触发全窗口重训。注意这更正了交接文档 S17「改标签却不进哈希」的说法：**baseline 下确实不进（因为 `:429-430` 直接返回空 dict），非 baseline 下是进的**）。行业分化 ±20% 惩罚生产影响 0bp。L9 单独做没有意义 —— 它是记账修正，应作为 L2 的副产品自然发生。

**时机上的有利条件**：量化 job 全部 SKIP，线上没有训练/发布在跑 → 既没有"先修问题保住线上"的紧迫性，也没有"先上收益立刻赚钱"（SKIP 状态下改成本口径即时收益为 0）。这正是放心做阶段 1 大改的时机。**唯一例外是 `realtime` 仍在跑**（见阶段 −1）。

---

## 6.1 施工细节复核（本轮实测，**更正了先前几处判断**）

> **【0813 更正】本节是对 `08-12 15:59` 快照的复核，多条已被新快照修掉。** 逐条对照见 §8.1（已修 8 条）、§8.2（purge 的修法细节）、§8.3（仍成立 8 条）。本节原文保留作留痕，不要单独引用。

### A. 成本口径的落地点是**三组** env，不是两组
先前说"两组 env 名"。实际本机 grep 出三个互不相干的入口：

| 入口 | 读取点 | 生产是否设置 |
|---|---|---|
| `QUANT_BT_COST_ROUNDTRIP` | `backtest.py:40` | ✅ `scheduler_jobs.sh:19` 唯一 export |
| `INTRADAY_1400_ROUNDTRIP_COST` | `pipeline.py:184/229/289/939`、`evaluation.py:253` | ❌ 从未设置 → 永久 0.002 |
| `REALTIME_PAPER_COST` | `realtime/config.py:186` | ❌ 从未设置 → 永久 0.002 |

外加 **8 处纯字面量硬编码**（`intraday_1400/` 下 `structural_combo_experiment.py:232`、`forward_evaluation.py:30`、`direct_return_experiment.py:63`、`daily_minute_enhancement.py:61`、`offline_race.py:17`、`target_redesign.py:113`、`structural_combo_holdout.py:301`、`adaptive_exit_replay.py:31`）与 **1 处模块级常量**（`daily_optimization_pipeline.py:32 COST = 0.002`）。

**`daily_optimization_pipeline.py:32` 那处是硬约束，不能只改数字**：`COST` 经 `_execution_environment():163-169` 进入 manifest，而 `:204-205` 有 `if manifest.get("execution_environment") != _execution_environment(): raise RuntimeError("daily optimization execution environment mismatch")` —— **改 COST 会让所有历史 attempt.json 校验失败**。要么保留旧值做兼容分支，要么接受历史 attempt 全部作废，必须显式决策。

### B. `hold_rank_buffer` 在快路径确实实现了（此前不确定）
`_fast_combo_metrics:422-441` 逐日调 `backtest._select_with_rank_hysteresis`（`backtest.py:168-200`）并维护 `previous_codes`，**不是忽略参数**。所以展开维度不会产生"每 3 行**必然**结果相同"的白烧 —— 代价只是退化成 Python 循环（实测单组合贵 8~9%，见 §6.1 L-d）。

**但配套测试证明不了这一点，须更正先前的说法。** `test_fast_and_formal_grid_share_rank_hysteresis_and_empty_days`（`test_model_expansion_experiment.py:1543-1585`）的 fixture 是 3 日 × 4 票、中间一日全不可买 —— 实跑逐个 buffer 值（`0/1/2/5`）得到的 `total_return`/`avg_turnover`/`sharpe` **完全相同**。**它证明的是 fast/formal 一致，不是 buffer 生效。** 真正验证 buffer 生效的是 `:1524-1535`（`portfolio_from_predictions` 的 holdings 断言），且只覆盖正式路径。快路径实现正确是我在随机 40×30 面板上直接调 `_fast_combo_metrics` 独立复现的（换手随 buffer 单调下降、fast/formal 逐位相等）。

**更关键的是"实现了" ≠ "在真实数据上有区分度"**：仓库自己的实测显示 `buffer=2` 与 `buffer=0` 在生产 artifact 上三个指标逐位相同（见 L5）。这两件事必须分开陈述。

### C. `run_grid` 的 `fixed_params` 会**压回常量**，改 `_base_combos` 不够

`:822-823` `for key, value in (fixed_params or {}).items(): combos[key] = value` 是整列赋标量。而**三处**调用都传了这两个键（本轮补上先前漏掉的 CLI `main`）：
- `watchlist_grid.py:931-934`（CLI `main`）：`args.rebalance_stride` / `args.hold_rank_buffer`，argparse 默认 1/0（`:914` 附近）
- `scheduled_workflow.py:164-172`（`run_short_grid`，日频）：`args.short_rebalance_stride` / `args.short_hold_rank_buffer`
- `scheduled_workflow.py:502-509`（`_promotion_gate`，月频）：同两键（按 style 分短线/波段）

argparse 默认 `--short-rebalance-stride 1`（`:1266`）、`--short-hold-rank-buffer 0`（`:1274`），且 `scheduler_jobs.sh` 零命中 → **即使把 `_base_combos` 改成维度，`run_grid` 也会立刻把整列压回 0**（实跑验证：24300 行网格被压后**行数仍 24300，唯一参数身份只有 8100** → 3 倍算力换完全相同的结果）。

→ **施工必须四处同改**，完整清单见 §6.1 L。先前只列了两处，会改不动。

### D. 稳态日更的 `gross_exposure` 维度**根本不搜索**（新发现）
`:827-846` 的邻域剪枝对 `gross_exposure` 用的是**精确相等**（`abs(gross - anchor) <= 1e-12`），不是 `limits` 里的容差。本机复现：以现役冠军（ic .03 / top_n 2 / gross .30 / rq .55）为锚，邻域剪枝后 **243 行，`gross` 取值集合 = `[0.3]`**。

→ 这解释了为什么 L4 的 `selection_score` 缺陷在日更里"看不出来"：**日更从不比较不同敞口**。修 `selection_score` 只影响 `monthly-factor` 的全网格跑。同时也意味着 `pred_quantile=None` 那 1/6 的组合在稳态下**全部被剔除**（`:841` 的 `values.notna()` 对 `None` 为假）。

### E. 网格是**多进程并行**的（算力估算的关键）

> **【0813 更正】** `17:26` 快照把并行度改成了可传参：`run_grid(..., workers=None)`（`:833`），`:885` `worker_count = max(int(workers or min(8, os.cpu_count() or 1)), 1)`，`:886-889` 超过 CPU 数抛 `ValueError`，`:899` `chunksize = max(1, min(16, len(items) // max(worker_count * 8, 1)))`，CLI 新增 `--workers`（`:941` `type=int, default=8`）。→ **硬上限从 12 变成默认 8**，本机 96 核；且**两个生产调用点都没传 `workers`**（`scheduled_workflow.py:154` 短线网格、`:520` 选参网格），`scheduler_jobs.sh` 里的 `--workers` 命中（`:372/:385/:409`）全是数据更新作业，与网格无关，也没有测试覆盖。→ 下面的 `min(12, cpu_count)` 与「@12 workers」应读作 **@8 workers**，墙钟估算相应从 21.4 min 变为 **≈32 min**（8100→24300 组合的全网格，仍只痛在 `monthly-factor`）。这是第六种「fixed-but-not-enabled」变体：**新加的旋钮连生产调用点都没接**。

`:864-878`：`ProcessPoolExecutor` + `fork`，`max_workers = min(12, cpu_count)`，`chunksize = max(1, min(16, len(items) // (cpu_count*8)))`，通过全局变量 `_GRID_PRED/_GRID_PREPARED` fork 继承只读数据（避免 pickle 开销）。且每个 combo 内部对 `SHORT_GRID_HORIZONS = [1, 2]` **各跑一遍**（`_evaluate_combo_process:781`）→ **实际评估次数是 combos × 2**，8100 combos = 16200 次评估。

→ 先前说"8100 网格"在评估次数上偏小一半。24300 combos = 48600 次评估，其中 2/3 走慢路径。

可用的耗时锚点：`audit_20260812_strict_ridge_sentiment_validation.json` 的 `ridge_only.execution_checks.elapsed_seconds = 149.911`（9 窗口、h=1、`prepared_parts_reused: 55`、`rebuild: false`）—— 这是**训练**耗时，不含网格。仓库内无网格耗时记录，需实测。

### F. L2 今天**根本传不上去**：六个 flag 里四个会立刻抛异常（本轮最重要的更正）

先前把 L2 写成"零新代码，只要在 `scheduler_jobs.sh` 补传六个参数"。本机逐条追到 raise 语句后，**这个判断是错的**。下面每一行的 `file:line` 我都回读确认过：

| flag | 补传后的实际结果 | 抛出位置 |
|---|---|---|
| `--train-target-mode tradable-label` | ✅ **能跑** | — |
| `--purge-horizon` | ✅ **能跑** | — |
| `--strict-execution-labels` | ❌ 四个 job 全部 `RuntimeError` | `tradability.py:120-121` `PIT trading status unavailable: …/trading_status_history.parquet` |
| `--strict-calendar-factors` / `--strict-announcement-lag` / `--strict-pit-min-price-rows` | ❌ 四个 job 全部 `RuntimeError`（除非同时加 `--rebuild`） | `full_train_batched.py:742-744` `strict panel recipe does not match cached monthly panel; use --rebuild` |
| `--pit-index-code` | ❌ `RuntimeError` | `full_train_batched.py:57-58` `PIT universe reference files are unavailable` |
| `--require-selection-provenance`（非 rolling 时） | ❌ `RuntimeError` | `full_train_batched.py:1110-1114` `selection provenance unavailable for …: missing=[…]` |

**根因是四个缺失的上游产物，每一个都对应一个"零生产调用者"的生产函数：**

1. `trading_status_history.parquet` —— 唯一写入者 `daily_update.py:585-638 refresh_trading_status_reference`（`:612`/`:626` 两处 `warehouse.save`）。本机 grep 全仓库调用者：**只有 `test_model_expansion_experiment.py:339/363` 与归档副本**，`scheduler_jobs.sh` 零命中。
2. **三张 strict 面板** —— `panel_recipe` 由 `include_trading_gap_risk` / `strict_calendar_factors` / `strict_announcement_lag` / `strict_pit_min_price_rows` / `min_price_rows` 五键构成（`:728-734`），`strict_panel = any(...)`（`:736`）→ 只要打开任一个且已有缓存面板且未传 `--rebuild`，就直接 raise。**即"打开这三个 flag 等价于强制全面板重建"**，不是"加个开关"。
3. **两张 PIT 参照表**（`SECURITY_MASTER_FILE`、`INDEX_CONSTITUENT_HISTORY_FILE`）—— 唯一写入者 `daily_update.py:560 refresh_pit_reference_data`，生产调用者同样为零（只有 `test_model_expansion_experiment.py:318`）。
4. **`factor_selection` 的 provenance 侧车** —— 需要 9 个字段齐全（`:1105-1107` 的 `required` 集合：`selection_name/label_col/train_end/valid_end/predict_start/candidate_count/selected_count/candidate_pool_sha256/selected_sha256/generator_code_sha256`），且**必须恰好一行**（`:1115-1116`）。这正是 §4 那条封锁项。

→ **L2 的正确写法是一条门控序列，不是一次补参**：

```
① 接上 refresh_trading_status_reference（生成 trading_status_history.parquet）
   └→ 解锁 --strict-execution-labels、min_adv20、min_listing_sessions
② 一次性 --rebuild 重建月度面板（带三个 strict panel flag）
   └→ 解锁 --strict-calendar-factors / --strict-announcement-lag / --strict-pit-min-price-rows
③ 接上 refresh_pit_reference_data（生成 security_master + index_constituent_history）
   └→ 解锁 --pit-index-code
④ 重建 factor_selection provenance 侧车（10 字段、恰好一行）
   └→ 解锁 --require-selection-provenance；同时解除 §6 批2 坑 2 的冲突
```

**今天就能单独打开的只有 `--train-target-mode tradable-label` 和 `--purge-horizon`**。前者会全窗口重训并踩上面的 `price_source_signature` 坑，后者不改标签、代价最小 —— **所以 L2 里唯一"真·零成本"的一支是 `--purge-horizon`**。这条要写进 §2 的 L2，避免把它整条当成"零代码"卖给决策者。

**附带更正一处 §6 的 flag 依赖**：`--strict-execution-labels` 与 baseline **互斥**（`_validate_execution_gate_params:411-419`，`strict labels require a non-baseline target mode`；且 `min_adv20 requires strict execution labels`）。所以它不是独立开关，必须与 `tradable-label` 同批。

**还有一处此前混淆的绑定**：`_validate_c30_refresh_months:449-451` 只在 **`--enforce-c30-gates`** 打开时才要求 `refresh_months != 1`，**与任何 strict flag 无关**。而 `monthly-factor` 显式传 `--refresh-months 1`（`scheduler_jobs.sh:211`），另外三个 job 走 argparse 默认 `1`（`scheduled_workflow.py:1281`）→ **四个 job 只要加 `--enforce-c30-gates` 就会全部 `ValueError`**。这是第七个不能直接补传的 flag，先前的清单里没有它。

### G. `tradable-label` 会打断下游 `realtime-weight-shadow`（新缺陷，阻塞级）

这是 §6.1 F 里"能跑"的那个 flag 唯一的硬代价，先前完全未识别。

`tradable-label` 模式下标签列被改名：`ab_label_col = f"tradable_ret_{horizon}d"`（`full_train_batched.py:1201-1206`），预测输出的 `keep_cols` 用 `label_out_col = ab_label_col or target`（`:1762-1763`），源码注释自认「此时预测输出里没有 `target_ret_{h}d`」。而下游：

- `realtime/weight_shadow.py:29 TARGET_COLUMN = "target_ret_1d"`，`:243-246` 硬校验 `required = {"code","date",TARGET_COLUMN,*MODEL_COLUMNS}` → `ValueError: weight shadow source missing columns: ['target_ret_1d']`。它读的正是 `active_quant_short_predictions.parquet`（`realtime/config.py:294-295`），与 `realtime` 引擎同一份文件。
- 更隐蔽的是**它不会报错，而是静默出 0 样本**：`merge_active_predictions:249-256` 做**列并集**合并（`for col in fresh.columns: if col not in old.columns: old[col] = pd.NA`，反向亦然）→ 新写入的行 `target_ret_1d` 全是 `NA`，列**存在**所以 `:243-246` 的校验通过，但 `:180-181` 的 `realized = np.isfinite(data[TARGET_COLUMN])` 把这些行全滤掉 → 影子评估的样本停止增长，`:349-350` 报 `insufficient_oos_days`。**"列在但全 NA" 比 "列缺失" 危险得多。**
- 对比 `watchlist_grid` 是**能自愈**的：`:781-782` 无 `target_ret_{h}d` 就 `continue` 跳过该 horizon，但 `_ensure_targets:109-122`（在 `run_grid:820` 调用）会从 price 重新 join 补齐三种目标列 → 网格不受影响。`weight_shadow` 没有这个自愈路径。
- `realtime/reference.py:391-392` 有回退链 `[target_ret_{h}d, tradable_ret_{h}d, open_ret_{h}d]` → **实时引擎本身能容忍**，只有 weight_shadow 不能。

→ 施工必须把 `weight_shadow.py` 的 `TARGET_COLUMN` 改成同样的回退链，**或**在 `keep_cols` 里同时保留 `target_ret_{h}d`（`model_window` 里本来就有这一列，`ab_cols:1748-1757` 只是把它排除在合并之外）。后者更小、且不动实时层。**这一条属于必须与 L2 同批提交的下游修复，不能事后补。**

### H. `DailyICCache` 的磁盘键漏了所有标签口径字段（新缺陷）

`:531-535` 的键是 `sha256("horizon|target|factor1|factor2|…")`，`target = label_col or f"target_ret_{h}d"`（`:545`）。切到 `tradable-label` 时 `target` 变成 `tradable_ret_1d`，键**确实会变** —— 所以这一次是安全的。

**但键里不含任何改变该列数值的参数**：`sell_roll_max_days`（`QUANT_BT_SELL_ROLL_MAX_DAYS`，`backtest.py:31-40`）、`min_adv20`、`min_listing_sessions`、`strict_execution_labels` 全部会让 `tradable_ret_1d` 的**数值**改变而**列名不变** → 复用旧 IC。

且它的 `cache_dir` 是 `Path(window_cache_dir)/"factor_ic"`（`:1257-1260`），**只依赖 `window_cache_dir` 是否传入，不受 `_rolling_cache_allowed` 门控**（后者只控 `recipe_params`/窗口预测缓存，`:1278-1280`）→ **窗口缓存关闭时 IC 缓存仍然开着**。所以 §6 批 2 坑 2 里"缓存彻底关闭"只对预测缓存成立，IC 缓存这层的陈旧风险仍在。

→ 修法：把 `_label_recipe_params` 已有的那几个键并进 IC 缓存键，或在切换标签口径时清空 `factor_ic/` 目录。工作量小，但**必须在批 2 之前做**，否则批 2 的第一次跑就会用旧 IC 选因子。

### I. `--purge-horizon` 在 `tradable-label` 下有静默跳窗风险

`_purge_span:403-407`：`tradable-label` 下 `purge_span = horizon + bt_sell_roll_max_days() - 1 = 1 + 3 - 1 = 3`（现役 h=1），比 baseline 的 `1` 大三倍。而分支选择在 `:1430-1436`：

- `purge_horizon and strict_execution_labels` → `_purged_end_by_calendar`（全局交易日历，安全）
- **仅 `purge_horizon`** → `_purged_end_by_code`（`:504-517`）：逐股 groupby，**任一股票在边界前不足 `purge_span` 个可用日期就整个返回 `None`** → `:1440-1444` 打印 `purge leaves insufficient train/valid dates` 后 `continue`，**只是一行 print，不是异常**。

而 §6.1 F 已确认 `--strict-execution-labels` 今天传不上去 → **只能走 `_purged_end_by_code` 这条较脆的分支**，且 `purge_span` 从 1 变 3 会显著提高触发概率（新上市/长期停牌股尤其）。

补充一条对既有判断的更正：`_minimum_validation_months(1) = 1`（`scheduled_workflow.py:952-957`），而现役 `TRAIN_STYLE_ORDER` 只含 `short_1_3`、`short_horizon` 默认 1、`validation_months` 默认 1 → **`:1081-1086` 的 fail-fast 在现役配置下不触发**。它保护的是 L3 扫 horizon 的场景（这一点 §2 的 L3 写对了），但**不保护批 2 的 purge 跳窗** —— 两者是不同的失效模式，别混为一谈。

→ 施工上应把 `:1440-1444` 的 print 改成计数 + 阈值告警（如跳窗 > 10% 则 raise），否则批 2 可能"跑成功了"但实际训练窗口少了一截。

### J. manifest 回读会让 stride/buffer 的施工产生"隔天自锁"

§6.1 C 说要三处同改。补一条第四处的隐患：`_apply_incumbent_training_config:972-1020` 在 `main` 里**无条件**执行（`:1341`，不区分 strategy_mode），且 `:991-998` 会用 manifest 的 `short_rebalance_stride` / `short_hold_rank_buffer` **覆盖 CLI 值**，而 `build_params:1168-1170` 每次发布又把当前值写回 manifest。

对比 `recent_windows` 有显式保护（`:1340` `cli_recent_windows` 先捕获、`:1349-1350` 显式回填，注释写明 "explicit CLI intent, captured before manifest override"）—— **stride/buffer 没有这个保护**。所以在 `scheduler_jobs.sh` 里传了 `--short-hold-rank-buffer 2`，只要 manifest 里还是 `0`，第二天就被压回 `0`，而且没有任何日志。

→ 施工时必须给 stride/buffer 加上与 `cli_recent_windows` 同样的"CLI 优先"捕获，否则 §6.1 C 的三处同改会在下一次日更后静默失效。**这是「fixed-but-not-enabled」模式的第四个变体：改了也会被自己的 manifest 回滚。**

### K. `realtime-weight-shadow` 也在被 SKIP（更正阶段 −1 的措辞）

本机对 crontab 逐条复现白名单（`scheduler_jobs.sh:49-57` 的 `case` 只放行 `intraday-1400-*|realtime`）：`realtime-weight-shadow`（crontab `:27`，工作日 18:30）**同样 SKIP**。所以 §6.1 G 那条 `weight_shadow` 断裂**当前不会立刻发作**，但会在恢复调度的当天发作 —— 属于"恢复调度前必须先修"的一项，不是"已经在坏"的项。

同时这也意味着：`active_quant_short_predictions.parquet` 的唯一写入者（被 SKIP 的 workflow）和它的两个读者（`realtime` 引擎在跑、`weight_shadow` 被 SKIP）里，**只有一个读者活着**。S15 探针的判读因此更简单：那个文件的 mtime 就是调度关闭日。

### L. L5（buffer 维度化）的完整施工清单 —— 「只改字面量」远远不够

§6.1 C 已指出 `fixed_params` 会压回常量。本轮把整条链跑完，**至少四处必改，另有四项必须评估**。以下数字为本机 AST 抽函数 + `exec` 真跑复现（Python 3.6.5 / pandas 1.1.5，仓库零修改）。

**必改 4 处：**

1. **`watchlist_grid.py:152` 加一层循环、`:168` 改成变量。** 加第 7 维 `{0,1,2}` 实跑得 **24300 行 / 24300 唯一**。`slot_weight = gross/top_n`（`:163`）是派生列、不受影响（15 个 `(gross,top_n)` 组合塌成 13 个唯一值，`0.18/3 = 0.30/5 = 0.24/4 = 0.06` 三处碰撞，与 buffer 无关）→ **无需改派生列**。
2. **三处 `run_grid` 调用点全部要停止无条件注入 `fixed_params`**（先前只列了两处，漏了 CLI `main`）：
   - `watchlist_grid.py:931-934`（CLI `main`，`args.hold_rank_buffer` 默认 0，`:914`）
   - `scheduled_workflow.py:164-172`（`run_short_grid`，日频）
   - `scheduled_workflow.py:502-509`（`_promotion_gate`，月频）

   实跑确认后果：`fixed_params={"hold_rank_buffer":0}` 作用在 24300 行网格上 → **行数仍 24300，但只有 8100 个不同参数身份 → 3 倍算力换完全相同的结果**。
3. **`test_model_expansion_experiment.py:2995-2997` 的硬断言必须同步**（`assertEqual(run_grid.call_args.kwargs["fixed_params"], {"rebalance_stride": 1, "hold_rank_buffer": 0})`）—— 改了必然红。
4. **给 stride/buffer 加"CLI 优先"捕获**（见 §6.1 J）—— 否则次日日更会被 manifest 回读压回。

**新发现的第五处（晋级门每月把冠军的 buffer 强制归零）：** `scheduled_workflow.py:506-509` 用 args（=0）**覆盖** `fixed_model_params["hold_rank_buffer"]`，而 `:490` 附近的 `incumbent_params.setdefault(...)` 只是兜底。→ 即使某轮网格选出 `buffer=1` 并写进 `champion_score_params`，下个月的 selection 网格仍被钉在 0 → **冠军的 buffer 永远无法被复验**。（读代码 + 覆盖语义推断。）

**四项必须评估（不改代码但会影响结论）：**

**L-a. 晋级门 top-20 的多样性被稀释 3 倍。** `:543` 只对 `selection_best.head(20)` 复测、`:580-581` `promote = passed_count > 0`（首个通过者胜出）。构造"buffer 完全无区分度"的 24300 行网格实跑：**top-20 里"忽略 buffer 后的不同底层组合"只有 7 个**（20 个名额被 3 个 buffer 变体挤占），而 family-wise error 仍按 20 次试验累积。好消息：并列打破在行序扰动下是确定性的（`group_cols:532` 含 `param_id`），不会随机漂移。**这比 CPU 代价更值得关注。**

**L-b. `template` 自喂回路会永久污染网格。** 生产每次把上一轮的网格输出当 template（`scheduled_workflow.py:156 template=eval_file` 且 `:161 output=eval_file` 是同一个文件），`_base_combos:171-173` 做 `pd.concat([standard, template]).drop_duplicates()`。实跑：改造后首跑 24300 ✓、第二跑仍 24300 ✓；**但任何一次手工 `--hold-rank-buffer 20` 跑 CLI，那个 20 会被写进 `eval_file`，此后每轮网格永久多出 8100 行**（`:144-150` 只做 `clip(lower=0).astype(int)` 规范化，**不做值域白名单**）。既有设计缺陷，扩维后放大 3 倍。

**L-c. UI 的"白名单历史验证"表会膨胀 3 倍。** 网格输出文件同时是 UI 的 `evaluation_file`（`scheduled_workflow.py:54` ≡ `stock_analyzer/quant_signal.py:82`），而 `selected_evaluation:481` 的 `prefer` 只过滤 `ic_weight/top_n/gross_exposure/max_weight/ridge_quantile/pred_quantile` —— **既不含 `naive_weight` 也不含 `hold_rank_buffer`**。实跑：当前每 horizon 3 行（`naive_weight` 未纳入已是既有缺陷）→ 扩维后 **9 行**，且 `app.py` 侧全量 `st.dataframe` 不去重。

**L-d. 算力是 3.18 倍而非 3 倍，且只痛在月度作业。** 本机 467 日 × 3193 票（逼近生产 1.40M 行 / 3193 票）实跑单组合含 2 个 horizon：`buffer=0` 0.5985s、`buffer=1` 0.6487s、`buffer=2` 0.6531s —— `buffer>0` 走 `:426` 逐日 Python 循环、`buffer=0` 走 `:443` `argpartition`，单组合贵 8~9%。单核总量 **1.35 core-h → 4.28 core-h**；`max_workers = min(12, cpu_count)` 硬上限 12（本机 96 核也只用 12），**@12 workers 6.7 min → 21.4 min**。`:873-878` 无任何早停/剪枝。

**但日频不痛**：邻域剪枝的 `limits`（`:828-833`）**不含 `hold_rank_buffer`** → buffer 维度完整通过，日频严格 ×3。实跑两种锚点：真实冠军 54 → **162** 组合；代码默认 `FINAL_TRADE_STYLES` 冠军（`pred_quantile=None`）486 → **1458** 组合。绝对成本可忽略。→ **3.18 倍只落在 `monthly-factor` 的全网格上。**

**三处对先前判断的更正：**

- **§6.1 E 的"8100 网格评估次数偏小一半"仍对，但"组合数 16200"是错的。** `SHORT_GRID_HORIZONS = [1,2]`（`scheduled_workflow.py:96`）**不参与组合枚举**，它在 `_evaluate_combo_process:782` 的 `for horizon in _GRID_HORIZONS` 里被消费，每个 `(combo, horizon)` 产出一行输出。→ **组合数 8100 → 24300（"×3"正确）；网格 parquet 行数 16200 → 48600；`_selection_summary` 输出行数 = 组合数 = 24300**（实跑确认，`horizons` 列值 `"1/2"`）。
- **§6.1 B "快路径确实实现了 buffer"得到独立复现，但仓库引用的那个 parity 测试证明不了它。** `test_fast_and_formal_grid_share_rank_hysteresis_and_empty_days`（`:1543-1585`）的 fixture 是 3 日 × 4 票、中间一日全不可买，实跑逐个 buffer 值：`0/1/2/5` 的 `total_return`/`avg_turnover`/`sharpe` **完全相同** → 它证明的是 fast/formal 一致，**不是** buffer 生效。真正验证 buffer 生效的是 `:1524-1535`（`portfolio_from_predictions` 的 holdings 断言），且只覆盖正式路径。我在随机 40×30 面板上直接调 `_fast_combo_metrics` 独立复现了换手随 buffer 单调下降、且 fast/formal 逐位相等（`top_n=2, buf=2`：`fast −0.01554969` = `formal −0.01554969`）。
- **`top_n=1` 与 buffer 无兼容性问题，无需加约束。** 实跑逐日持仓：`top_n=1, buffer=1` 会多扛一天，`buffer=2` 语义正常，`buffer=3 ≡ buffer=2`（候选池饱和）。24300 行里 `top_n==1` 有 4860 行、其中 `buffer>0` 有 3240 行 —— 这些不是重复行（参数身份不同），但**指标可能与 buffer=0 相同**。真正的退化风险不在 `top_n=1`，而在 L5 §2 那条：真实数据上 `buffer=2` 整体无效果。

**无需改：** `slot_weight` 派生、`_selection_summary` 的 `group_cols`（已含两键）、`_prepared_stride_indices`（`:331-344`，stride=1 时返回 `np.arange`）、`_select_with_rank_hysteresis` 的 `top_n` 约束、快路径实现本身。

**顺带两条：** `scheduled_workflow.py:115-136` 的 `_daily_short_neighbor` **全仓无调用者**（本机 grep 确认），是 `run_grid` 内联剪枝的死重复实现；`param_id` 是位置序号（`:851 int(idx)+1`），加维度后同一个 `param_id` 指向完全不同的参数（新网格前 6 个 `param_id` 的 buffer 依次 `0,1,2,0,1,2`），**跨轮次按 `param_id` 对比历史网格结果会全部错位**（本来就脆弱，扩维后必然错位）。

---

## 7. 一句话总结

> **【0813 更正】以 §8.7 为准。** 本节针对 `08-12` 快照，其中「四个 job 一个严格开关都不传」「六个 flag 里四个会抛异常」已被新快照推翻（shell 层已补全），但「四个 job 全被 SKIP」这条**更强地成立**了。

**当前没有任何"新信号"是可靠的收益方向；唯一算术上确定能提升收益的是把过时的 20bp 成本口径改成真实的 7~10bp（+9.5~12.4bp/天）。** 排第二的严格链（L2）**不再是"零新代码"** —— 本轮逐条追到 raise 语句后确认：六个 flag 里只有 `--train-target-mode tradable-label` 和 `--purge-horizon` 今天能跑，其余四个各自撞上一个缺失的上游产物而 `RuntimeError`（另有第七个 `--enforce-c30-gates` 会 `ValueError`），落地成本是"四个上游产物 + 一次全面板重建 + 两处下游修复"，见 §6.1 F。它仍排第二，但理由从"零成本"改成"它是下游一切实验可信性的前置条件"。

其余方向的真实效应量全部低于配对 MDE 28bp/天，在现有样本下不可判定；账面数字最大的那条（−10% 尾部惩罚改真实出清，+12.21bp/天）几乎全是记账假设变化，不是真钱。

**本轮复核对施工计划的三条实质修正：**
1. **L2 从"补四行 shell"变成四步门控序列**，且 `tradable-label` 会静默打断 `realtime-weight-shadow`（列在但全 NA，比缺列更危险，§6.1 G）。
2. **L5 从"改两处"变成"4 处必改 + 4 项必评估"**，其中 `fixed_params` 那处会让改动 100% 静默失效；而 `{0,1,2}` 这个值域按仓库自己的实测很可能**全无区分度** → 新增 S16 探针先定值域（§6.1 L）。
3. **算力估计从"5~10 倍"下调到 3.18 倍**，@12 workers 是 6.7 min → 21.4 min，**且只痛在月度作业** —— 算力从来不是瓶颈，真正的代价是晋级门 top-20 的有效多样性被从 20 稀释到 ~7。

**并且这一切以恢复调度为前提** —— `docker-compose.yml:60 SCHEDULER_DISABLED=1`，本机对 crontab 逐条复现白名单（`scheduler_jobs.sh:49-57` 只放行 `intraday-1400-*|realtime`，而 crontab 无 `intraday-1400-*` 条目）：**12 个 job 里只有 `realtime` 在跑**，`realtime-weight-shadow` 同样 SKIP。

---

## 8. 对 `2026-08-13` 新快照的逐条复核（**本文档当前的权威结论**）

仓库已被整体替换。本节全部判断由本机在新快照上逐个 `file:line` 复读得出，**不转抄对方两份文档的任何结论**。

> **二次复核（`2026-08-13 15:17` 快照）**：仓库又被整体重拷了一次（438 个文件 mtime 全部刷新），但**内容真正变化的只有一个文件**：`quant/rolling_fixed_relative_comparison.py`（`059dd258…` → `4689e780…`，177→184 行），新增内容正是我在 8.5 指出缺失的那段 rolling−fixed 直接配对。其余 8 个锚点文件 SHA256 **逐字节相同**，8.1/8.2/8.3/8.4/8.6 的每一条我都回到 `file:line` 重读确认**无回退**，结论原样有效。根目录两份 md 被重写，改动见 8.5。

### 8.0 快照锚点

仓库不是 git 仓库，SHA256 是唯一版本锚：

```
77dd8ced49830f5854f80b267bc21d13138db112e4d24aadd14889d6a290b961  quant/full_train_batched.py
1e34c0ce3333c179fdcb8608c4bdd5d0a0581764db683f9a435830ccdf68eca0  quant/watchlist_grid.py
d43955ce50a491e33bb53aa3d687ff5da5ca7a9d837e30c81013fdb6cd9fee2f  quant/scheduled_workflow.py
34509787594875a7b2b9769d7342bed175070cefd4f8a40983a8b0e915de3ebf  quant/backtest.py
4689e7809f8a4461af4971106acaf066a8e694b397625a092bdd7f976b88d9e7  quant/rolling_fixed_relative_comparison.py   ← 15:17 快照唯一变更文件（原 059dd258…，177→184 行）
29d6ca0789c867ecda1a1a82366d05129a2b3afa1a36116ee5a8771f6ab0076c  realtime/weight_shadow.py
5fa091d14e7b454d211f529bbd8d92a0f846b892e22ab857285e67bfce8c9f72  docker/scheduler_jobs.sh
0b6bd82990866d111cf36edd7cbf64cbc1467e62e0835165e762a798c2cf88b7  docker/scheduler.crontab
869826424cd5e325c8af3ba67eda6b5ac9f1461cc6bc89d0818cca8bd433e224  docker-compose.yml
```

### 8.1 我先前的判断中，已被新代码推翻的（8 条）

| 我原先的判断 | 新快照实际状态 | 证据 |
|---|---|---|
| §6.1 J：stride/buffer 改动会被 manifest 回读隔天自锁 | **已修**。新增 `_restore_explicit_portfolio_params()`，在 `_apply_incumbent_training_config(args)` **之后**用 argv 嗅探恢复显式 CLI 值 | `scheduled_workflow.py:982-997`，捕获点 `:1378-1386`，`*_explicit` 置位 `:1364-1365` |
| §6.1 H：`DailyICCache` 磁盘键漏掉全部标签口径字段 | **已修**。键从 `(horizon,target,factors)` 变为 `(recipe_signature,horizon,target,factors)`，且 `set_recipe_signature()` 变更时清空内存帧 | `full_train_batched.py:545-573`，接线 `:1311-1316`，逐窗重置 `:1384-1386` |
| 阶段 1 批 2 坑①：`price_source_signature` 会让日更缓存每天 100% miss | **已修**。签名可接 `(start,end)`，逐个 parquet 只读 `date` 列，与窗口不相交即 `continue` | `full_train_batched.py:192-225`，逐窗调用 `:1378`，注入 `:1382` |
| L4：`selection_score` 三个携带敞口的项未归一化 | **已修**。`avg_annual_return`/`worst_drawdown`/`avg_turnover` 三项均已除以 `gross` | `watchlist_grid.py:545-565` |
| §4 方法论债：`stability_decision` 月胜率跨 horizon 池化 | **已修**。改为 per-horizon 月胜率列表 + `all(rate >= 阈值)` | `watchlist_grid.py:680-754` |
| L5：`hold_rank_buffer` 在网格里是字面量常量 | **部分修**。short/swing 两支都已 `for buffer in (0,1,2)` 维度化；三处 `fixed_params` 改为"仅在显式请求时才放键"，不再无条件压回 `0` | `watchlist_grid.py:167-174`、`:175-186`、CLI `:932/:963-969`；`scheduled_workflow.py:154-174`、`:499-519` |
| §6.1 F：生产 job 一个严格开关都不传 | **已推翻（shell 层）**。`intraday-light`/`daily-light`/`weekly-full`/`monthly-factor` **四个 job 现在都传** `--train-target-mode tradable-label`、`--strict-execution-labels`、三个 strict panel flag、`--purge-horizon`、`--pit-index-code 000852.SH` | `scheduler_jobs.sh:138-144/170-176/199-205/231-237`；转发链 `scheduled_workflow.py:1051-1078`（`:1171` 唯一调用点） |
| §6.1 G：`tradable-label` 会让 `weight_shadow` 静默失效（列在但全 NA） | **已修，且不是静默**。新增 `TARGET_CANDIDATES = (target_ret_1d, tradable_ret_1d, open_ret_1d)` 逐列回退，三列都无实值时 `raise ValueError`；两个入口都走它 | `weight_shadow.py:29-46`，调用点 `:264`（`evaluate`）与 `:463`（`run_routine`） |

### 8.2 §6.1 I（purge 静默跳窗）——「已修」但要看清修在哪一层

`:1493` 那句只 `print` 不抛异常的语句**原样保留**，所以*单个*窗口被 purge 吃掉仍然只打印。真正的门是**事后总量门**：

```python
# full_train_batched.py:1852-1865
if purge_horizon:
    completion_ratio = completed / requested if requested else 1.0
    if completion_ratio < float(min_window_completion_ratio):   # 默认 0.90，:1210
        raise RuntimeError("purge window completion ratio below threshold: ...")
```

计数器同时写进 summary（`:1909-1914`：`purge_windows_requested` / `purge_windows_skipped` / `purge_window_completion_ratio`）。

→ **判定：静默失效已消除**（超过 10% 窗口被吃掉会 fail closed），但**跳窗仍是逐窗静默的**，≤10% 的损耗不会有任何告警，只能靠事后读 summary 的比率。另外 `min_window_completion_ratio` **只有函数参数、没有 CLI 开关**（全文件仅 4 处命中，无 `add_argument`），生产无法收紧到 1.0。

### 8.3 我先前的判断中，在新快照上依然成立的（8 条）

1. **`rebalance_stride` 仍完全进不了搜索**。`_base_combos` 里仍是 `"rebalance_stride": 1` 字面量；三处 `fixed_params` 对 stride 仍是**无条件**押回（`watchlist_grid.py:963-969`、`scheduled_workflow.py:154-174`、`:499-519` 都只对 buffer 做了条件化，stride 照旧）；`scheduler_jobs.sh` grep `rebalance-stride|hold-rank-buffer|short-rebalance|short-hold` → **零命中**。→ L3/L5 里 stride 那一支的结论一字不改。
2. **`fixed_params` 的整列赋标量机制未变**：`run_grid:845-846` 仍是 `for key, value in (fixed_params or {}).items(): combos[key] = value`。对方的修法是"少放键"，不是"改机制" —— 只要将来有人重新无条件放键，维度就会再次被压平。
3. **模板自喂污染未修**：`run_short_grid` 仍 `template=eval_file`（`scheduled_workflow.py:154-174`），而模板归一化 `watchlist_grid.py:137-150` 仍只有 `to_numeric/fillna/clip/astype(int)`，**没有取值白名单**。
4. **邻域剪枝 `limits` 仍不含 `hold_rank_buffer`**（`:850-868`），`gross_exposure` 仍是精确相等 `<= 1e-12` → 日更里 buffer 维度按冠军锚点严格 ×3，gross 维度仍不搜索。
5. **晋级门仍 `selection_best.head(20)`**（`scheduled_workflow.py:553`）→ buffer 扩维后 top-20 的有效多样性稀释问题原样存在。
6. **`_daily_short_neighbor` 仍零调用者**；`param_id` 仍是位置序号（`:874`）→ 跨轮次按 `param_id` 比历史结果仍会错位。
7. **调度仍全关**：`docker-compose.yml:60 SCHEDULER_DISABLED=1`、白名单 `scheduler_jobs.sh:49-57` 仍只放行 `intraday-1400-*|realtime`、crontab 仍无 `intraday-1400-*` 条目。→ **8.1 里那四个"现在传了严格开关"的 job，今天一个都不会执行**；`realtime-weight-shadow`（crontab 工作日 18:30）同样 SKIP。这是"fixed-but-not-enabled"的第五种变体：**参数传对了，但 job 本身被 SKIP**。
8. **`monthly-factor` 仍 `--refresh-months 1`**（`:239`）→ 若再补 `--enforce-c30-gates` 会立刻 `ValueError`（`full_train_batched.py:474-476`，校验点 `:2066`）；`daily-light` 仍 `--force-latest-price`（`:168`）。

### 8.4 仍未接通的三项（严格链的剩余缺口）

- `--require-selection-provenance`、`--enforce-c30-gates`、`--min-adv20`、`--rolling-factor-select`：转发代码都在（`scheduled_workflow.py:1052-1078`），但 `scheduler_jobs.sh` 里**四个 job 一个都没传**。→ §4 封锁项 1/2（`factor_selection_lh1000_cont` 无 provenance、provenance gate 默认关）**未清除**。
- **状态表/PIT 参照表的生产生产者仍无调度调用者**：`daily_update.py:560 refresh_pit_reference_data` 与 `:585 refresh_trading_status_reference` 在本仓库内**除测试外零调用者**，`daily_update.main()` 的 argparse 也没有对应开关（`:749-766`）。→ 四个 job 传的 `--strict-execution-labels` 依赖 `trading_status_history.parquet`（缺失即 `RuntimeError`，`tradability.py:119-121`），这张表现在只能靠**人工离线补数**产生，不是调度产物。这是 §6.1 F 的根因里唯一**没有**被修掉的一条。
- **active prediction 无新鲜度门**：`realtime/engine.py:48-53/211-215` 仍只用 mtime 判"是否变化"，`realtime/config.py:294-296` 指向 `active_quant_short_predictions.parquet`；全 `realtime/` 只有 quote 级 `*_quote_max_age_sec`（`config.py:235/250`），**没有对预测制品的 max-age/stale gate**。→ S15 的风险原样存在。

### 8.5 正式 24 窗结果的自主复算：口径混淆（**我提出 → 对方已修代码+改文档 → 实测复现我的反推**）

**结论先行**：`13:21` 快照上，对方文档报的「rolling 相对 fixed paired mean gain `-0.00103251`（`-10.33bp/day`）、p=`0.96101949`」**口径是错的** —— 那是「模型 vs 等权基准」的配对，不是 rolling−fixed。我在 `13:21` 快照上从代码判定了这一点，并用算术反推出真正的 rolling−fixed ≈ **+2.3bp/天**。`15:17` 快照里对方**补了代码、改了文档、跑出了实测值 `+2.32bp/天`** —— 与我的反推在两位有效数字上一致。

**第一步：我在 `13:21` 快照上的判定（代码证据）**

`quant/rolling_fixed_relative_comparison.py` 就是产出该结论的那份脚本（`research_only`/`production_publication`/`common_days`/`prediction_key_hash` 四个字段与文档引用逐字对应）。当时全脚本只有一处配对：

```python
# 059dd258… :129-139（此段在新快照里原样保留）
for baseline in ("market_equal_weight", "pit_industry_equal_weight"):
    for model in ("rolling", "fixed"):
        model_stats[model] = {str(block): _paired_block_bootstrap(
            result[f"{model}_absolute_return"], result[baseline], ...)}
```

配对的两条序列是 **模型 vs 等权基准**，`rolling` 与 `fixed` 各自独立对同一个基准做配对。**当时这份脚本里根本没有 rolling−fixed 的配对**，所以 `-10.33bp/day` 只能是「rolling 相对**全 PIT universe 等权**」。

**第二步：三重算术自证**（数字全部取自对方文档，运算由本机做）

- rolling 日均净收益 = `-0.04067015 / 477` = **−0.85bp/天**
- 若 `-10.33bp/天` 是"相对基准"，则基准日均 = `-0.85 + 10.33` = **+9.47bp/天**，477 天累计 **+45.2pp** —— 一个 2024-08~2026-08 小盘等权指数的合理涨幅。✔ 自洽
- 若它真是"相对 fixed"，则 fixed 日均须为 `+9.47bp/天`、两年累计 +45pp；但对方自己报的 fixed `total_return = -0.14416501`、`Sharpe = -1.19934474`。✘ 直接矛盾
- 由 `total_return -0.14416501` + `Sharpe -1.19934474` 反解 fixed 日均（解 `477(m − s²/2) = ln(1−0.14417)`、`m/s = −1.199/√252`）→ `s ≈ 0.42%`、`m ≈` **−3.17bp/天** → **rolling − fixed ≈ +2.3bp/天**

**第三步：`15:17` 快照对方的修正（已复核）**

- **代码**：新增 `:140-146`，第一次做了真正的 rolling−fixed 配对——

```python
comparisons["rolling_vs_fixed"] = {
    str(block): _paired_block_bootstrap(
        result["rolling_absolute_return"], result["fixed_absolute_return"],
        args.bootstrap_samples, block, args.seed,
    )
    for block in blocks
}
```

- **文档**：`下一轮调研记录_20260812.md:549` 给出明确口径更正，承认「此前标成 rolling 相对 fixed 属于指标命名错误」；`:550` 报实测：rolling 日均 `-0.00008526`、fixed 日均 `-0.00031749`、**rolling−fixed = `+0.00023222`（+2.32bp/天）**，block-5 CI95 `[-0.00020752, +0.00067449]` = `[-2.08bp, +6.74bp]`，one-sided p = `0.16091954`，「方向为正但统计不显著」。
- **与我的反推对照**：我算的 rolling −0.85bp/天 vs 实测 −0.853bp；我反解的 fixed −3.2bp/天 vs 实测 −3.17bp；我反推的差 +2.3bp/天 vs 实测 **+2.32bp/天**。✔ 三项全中。

→ **判定不变，且现在有对方自己的实测背书**：`+2.32bp/天` 与上一轮的 `+4.60bp/天`（`audit_20260812_rolling_fixed_paired_validation.json`）同号同阶，**"rolling 优于 fixed 的方向翻转了"这个读法从未成立** —— 翻转的只是"相对市场等权"这个被换掉的基准。而 `+2.32bp` 对应 t ≈ **1.03**（本机由 CI 反解 SE=2.25bp），477 天样本下 MDE80 ≈ 5.6bp/天 → **不可判定，不能晋级**。注意这个 p=0.1609 是**未进 Holm 家族的裸值**，见 §9.2 问题一。

**两点补充（前者仍未修，后者是新增代码的性质）**

1. **基准对比的两处口径不对称仍在**，所以 `-10.33bp/day` 即便改对了名字也不能当作"无 alpha"的判据：① **成本单边计提** —— `rolling_absolute_return` 取自 returns parquet 的 `ret`，而 `ret = gross_ret − cost`（`backtest.py:330`），等权基准直接是 `tradable_ret_1d` 日均值、**不含任何交易成本**，这一项就占 3.74bp/天；② **敞口不对称** —— 策略 `avg_holdings = 1.8763` 带现金，基准是全 universe 满仓等权，牛市里低敞口必然跑输，这部分不是"没有选股能力"。
2. **新增的 `rolling_vs_fixed` 块无测试、无消费者**：全仓 `--include=*.py`（排除 `research_runs/`）grep `rolling_fixed_relative_comparison|rolling_vs_fixed` **只命中定义处自身**；`evidence["schema_version"]` 仍是 `1`（新增字段未升版本）；这一块也**没有进 Holm 家族**（`_holm_adjust` 只作用于两个基准对比的 rolling p 值，`:137-139`），即 rolling−fixed 的 p 值是**未做多重比较校正的裸值**。→ 这是本仓库那个反复出现的模式的又一例：研究代码路径存在，但没接进任何东西。

### 8.6 24 窗结果对 §2 排序的影响

**先把成本-毛收益结构算清（本机复算，全部可自证）**：

| 量 | 24 窗正式结果 | 日均 |
|---|---|---|
| gross 累计 | +0.13792985 | **+2.89bp/天** |
| cost 累计 | 0.17860000 | **−3.74bp/天** |
| net 累计 | −0.04067015 | **−0.85bp/天** |

且 `avg_turnover × 20bp × 477 = 0.18721174 × 0.002 × 477 = 0.178600` —— **与 cost 累计逐位相等**，证明成本项就是"换手 × 20bp"，无其他成分（这也说明 C42 的加权换手口径已生效：换手从旧样本的 0.2551 降到 0.1872）。

**三条结论：**

1. **毛收益仍为正（+2.89bp/天）** → §0 「"模型比随机差"是被证伪的」这条纪律**在新的严格链上再次得到确认**，且这次是在 tradable-label + PIT + purge 的严格口径下、478 个交易日、311,520 行预测上。
2. **成本占毛收益的 129%** —— 这是这次跑成负的**唯一**原因。→ **L1（成本口径重定价）从"第一名"升级为"决定符号的那一项"**：按同一 turnover 0.1872，20bp→10bp 省 **1.87bp/天**（net 转 **+1.02bp/天**），20bp→7bp 省 **2.43bp/天**（net **+1.58bp/天**）。**改一个数就能让这次正式跑从负翻正。** 但必须同时诚实说：1~2bp 的翻正远低于该样本的配对 MDE80 ≈ 5.6bp/天，**这是记账正确性的胜利，不是 alpha 的胜利**（且 L1 本身是恒等式、不适用显著性检验，见 §9.1 ⑨）。
3. **L4 已修（8.1）、L5 的 buffer 支已半修（8.1）、stride 支仍全封（8.3-1）** → §2 的排序调整为：**L1（唯一算术确定且现在决定符号）> L2（严格链，shell 层已通，但状态表生产者仍是人工补数、四个 job 仍被 SKIP）> L3 的 stride 支（唯一未接通的降成本维度）> L6/L7（探索）**；**L4 从杠杆清单移出**（已修，剩下的只是"重跑一次网格看冠军是否变"）。

### 8.7 一句话（新快照版）

对方这一轮把我记的四条阻塞缺陷（G/H/I/J）**都修掉了**，把 `selection_score` 敞口归一化、`stability_decision` 池化、`price_source_signature` 每日失效三条也修掉了，并且**在四个生产 job 里真的补上了六个严格开关**，最后一次重拷里还补上了我指出缺失的 rolling−fixed 配对（`:140-146`）+ 文档口径更正（`:549-550`）—— 施工侧的进展是实的。但**收益侧一步没进**：24 窗正式严格跑净收益 −0.85bp/天，负值的**全部**来源是"换手 × 20bp"这一项（成本 = 毛收益的 129%）；`-10.33bp/day` 是相对市场等权、不是相对 fixed，这个口径错误**已被对方修正并实测复现**（`+0.00023222` = +2.32bp/天，p=`0.161`，与我先前反推的 +2.3bp/天一致，见 8.5）。**真正的 rolling−fixed 仍是 +2.3bp/天、仍不显著（t≈1.03，477 天下 MDE80≈5.6bp）。** 于是结论回到原点且更尖锐：**唯一能改变符号的动作是把 20bp 成本口径改成真实的 7~10bp；而在 `SCHEDULER_DISABLED=1` 被解除之前，这四个已经配好严格参数的 job 一次都不会跑。**

---

## 9. 「方向为正但不显著」的完整清单 + 显著性判断本身是否合理

本节两问分开答：**9.1 清点**（有哪些、效应量多大、样本多大）、**9.2 审计检验方法**（这些"不显著"的判定合理吗）。所有 SE / t / 所需样本量都由本机从各报告的 CI 反解重算，运算过程写在条目里。

### 9.1 清单：七条「方向为正、统计不显著」

反解公式：由报告的 bootstrap CI95 取 `SE = (hi − lo) / (2 × 1.96)`，`t = obs / SE`，单侧 α=0.05 / 功效 80% 的 `MDE = 2.486 × SE`。

| # | 对比 | 观测效应 | CI95 | 报告 p | 样本 | 本机反解 SE | t | MDE80 | 达 p<0.05 需要 |
|---|---|---|---|---|---|---|---|---|---|
| **①** | rolling − fixed_control（24 窗正式） | **+2.32bp/天** | `[-2.08, +6.74]`bp | 0.1609（单侧，裸值） | 477 天 | 2.25bp | **1.03** | 5.59bp | **≈1212 天 ≈ 4.8 年** |
| **②** | rolling_v2 − fixed_33 净（上一轮） | **+4.60bp/天** | `[-9.21, +18.97]`bp | 0.2534 → Holm **0.4112** | 176 天 | 7.19bp | **0.64** | 17.9bp | ≈1164 天 ≈ 4.6 年 |
| **③** | 同上，毛收益 | **+5.79bp/天** | `[-7.89, +19.65]`bp | 0.2056 → Holm 0.4112 | 176 天 | 7.03bp | 0.82 | 17.5bp | ≈776 天 ≈ 3.1 年 |
| **④** | 24 窗 rolling 自身**毛**收益 > 0 | **+2.89bp/天** | 未报 | 未报 | 477 天 | 2.36bp（本机由 Sharpe 反解 σ=51.6bp） | **1.22** | 5.9bp | ≈860 天 ≈ 3.4 年 |
| **⑤** | h3 相对 market（非重叠 stride=3） | **+73.47bp/周期** | `[-19.86, +172.47]`bp | Holm **0.9492** | 69 周期 | 49.1bp | **1.50** | 122bp | ≈83 周期 ≈ 250 天 ≈ **1.0 年**（最接近的一条） |
| **⑥** | h10 相对 market | +62.91bp/周期 | `[-94.99, +240.56]`bp | Holm 1.0 | 21 周期 | 85.6bp | 0.73 | 213bp | ≈105 周期 ≈ 1052 天 ≈ 4.2 年 |
| **⑦** | sentiment 三臂 holdout IC | `holdout_ic_1d` 0.0146~0.0150（**过 0.01 门**）、`ic_3d` 0.0275~0.0295 | 未报 | 未报 | n=23473 | — | 池化 t≈2.30 | — | 唯一未过的是 `direction_1d` 0.4942~0.4958 vs 阈值 0.50，**差 0.4~0.6pp** |

**另有三条"方向为正"但必须单独归类，不能与上面并列：**

- **⑧ `hold_rank_buffer` = 20/50**：`total_return` 0.5370 / 0.5298 vs buffer=0 的 0.5128（+4.03bp/天 / +2.82bp/天）。**但这份诊断根本没算 CI**（`audit_20260811_portfolio_hysteresis_validation.json` 的 `buffer_diagnostic` 只有 turnover/sharpe/total_return 三列），而且 `buffer=2` 与 `buffer=0` 三个指标**逐位相同**。它是 60 天样本内诊断，报告自己写 `interpretation_limit: these in-sample diagnostics do not select a production buffer or establish future alpha`。→ **不是"不显著"，是"没检验"。**
- **⑨ L1 成本口径重定价（20bp→7~10bp）**：+1.87~2.43bp/天（按 turnover 0.1872）。**这一条不该做显著性检验** —— 它是 `cost = turnover × roundtrip` 这个恒等式里改一个常数，无抽样误差。已在 §8.6 逐位验算（`0.18721174 × 0.002 × 477 = 0.178600`，与 cost 累计逐位相等）。
- **⑩ L9 尾部惩罚改真实出清（+12.21bp/天）**：账面最大的一条，但 §2 已判定几乎全是**记账假设变化**，不是真钱。同样不适用显著性检验。

**清单的整体形状（这比逐条数字更重要）**：①②③④ 是同一条链的四种切法（且 ② 的 176 天完全落在 ① 的 477 天之内，重叠 37%），t 值全部落在 **0.6~1.2**；⑤⑥ 是 horizon 支，t 值 0.7~1.5；⑦ 是唯一走 IC 口径的。**没有任何一条 t > 2。** 换句话说：**不是"有一批接近显著的信号被严格标准挡住了"，而是"所有已测方向的信噪比都在 1 附近"** —— 这是随机波动与真实小效应无法区分的区域。

### 9.2 显著性判断是否合理：五条做对了、五条有问题

**先给结论**：**检验机制本身是这个仓库里质量最高的部分之一，"不显著"的判定我认为是可信的、不是过度保守。** 但有五处方法问题，其中两处会让 p 值**偏乐观**（即真实情况比报告的更不显著），三处让判定**在不该严的地方偏严 / 在该严的地方偏松**。

#### 做对的五条（我逐行读了 `intraday_1400/evaluation.py:35-114`）

1. **循环移动块 bootstrap 保留了自相关**：`_circular_block_bootstrap_means:44-56` 抽 block 起点而非单点，`% length` 环绕，`block_count = ceil(n/block)` 后截断到 `n`。block 长度 3/5/10/20 四档敏感性都报了。对日收益序列这是正确做法。
2. **配对在日期上 inner join 后再 dropna**：`_paired_block_bootstrap:66-69` 先 `concat(axis=1, join="inner")` 再 `.dropna()`，然后对**差值序列**做 bootstrap。这是配对检验的正确形式，比"分别 bootstrap 两条序列再比均值"强得多。
3. **p 值用中心化重抽样，不是"CI 是否跨零"的换算**：`:75-77` 对 `values − observed` 单独跑一次 bootstrap（`seed+1`）作为零分布，`:94-96` 用 `(count(centered ≥ observed) + 1) / (B + 1)`。加 1 的偏保守修正是标准做法（避免 p=0）。
4. **Holm 校正实现正确**：`_holm_adjust:103-114` 按升序累积 `max(running, (m − rank) × p)` 并 clip 到 1，是标准 step-down。且 ② 的 family 只有 2（net + gross）时也照样校正，没有偷懒。
5. **晋级门用 CI 下界而非 p 值**：`daily_optimization_pipeline.py:329-337` 的 `passed` 要求 `ci95[0] > 0.0`，且与 `mean_filled_names ≥ 1.0`、`max_drawdown ≥ −0.60`、`positive_months ≥ 3` **同时** 成立。`choose_next_branch` 的签名现在**强制要求** `baseline_metrics`，`None` 直接 `ValueError`（`:319-321`）—— 这修掉了此前"控制器在代码层面无法表达优于在位"的缺陷。

#### 问题一（偏乐观）：`p_value_one_sided` 是**裸值**，未进任何 Holm 家族

新增的 `rolling_vs_fixed` 块（`rolling_fixed_relative_comparison.py:140-146`）算出 p=0.1609，但 `_holm_adjust` 在这份脚本里**只作用于两个基准对比的 rolling p 值**（`:137-139` 的 `valid_p` 只从 `model_stats["rolling"]` 取）。→ **①的 0.1609 没有做多重比较校正**。同一次跑里实际做了 `2 基准 × 2 模型 × 1 block + 1 对 rolling-fixed = 5` 个检验，只有 2 个进了家族。诚实的读法：**①的真实校正后 p 只会比 0.1609 更大。**

#### 问题二（偏乐观，且这条最重要）：**整个体系对"搜索了多少个候选"零校正**

`watchlist_grid.py:152-174` 本机重算：short 网格 `5 ic × 5 top_n × 3 gross × 6 rq × 6 pq × 3 naive × 3 buffer = 24300` 行，swing `4 × 4 × 4 × 3 × 3 = 576` 行。冠军是从这两万多个组合里挑出来的，然后**只对冠军**做一次配对检验。Holm 家族大小是 2 或 9，**不是 24300**。全仓 grep `reality_check|spa_test|superior_predictive|deflated|white_test|hansen` → **零命中**：没有 White's Reality Check、没有 Hansen SPA、没有 Deflated Sharpe。

→ **这意味着：即便某个候选真的做到了 p=0.03，那个 p 也不能按 0.03 读。** 反过来说，这一条**加强**了当前的"不显著"结论（本来就不显著，校正后更不显著），所以它不影响今天的判定；但它意味着**将来一旦有东西"显著了"，那个显著性是不可信的**。这是我认为整个统计体系里最该补的一块，且它不需要更多数据，只需要在冠军选择环节记下搜索基数。

#### 问题三（该严处偏松）：`min_window_completion_ratio` 与 `MIN_FILLED` 是软门

- `full_train_batched.py:1210` 的 `min_window_completion_ratio: float = 0.90` **只有函数参数、无 `add_argument`**（全文件 4 处命中）→ 生产无法收紧到 1.0，最多 10% 的窗口可以被 purge 静默吃掉而不告警（§8.2）。
- `daily_optimization_pipeline.py:33` 的 `MIN_FILLED = 1.0` —— "平均填满 1 只股票"作为组合有效性门槛过低。24 窗正式跑的 `avg_holdings = 1.8763`，即**平均不到 2 只股票**。在 Top2 上做统计推断，单日收益 σ 高达 51.6bp（本机由 Sharpe −0.2623 反解），这是 ① 的 SE 只能做到 2.25bp 的根因。**想提高功效，减小 σ 比延长样本便宜得多**（σ 减半 = 样本量翻 4 倍的效果），而减小 σ 的直接手段就是提高持仓只数。

#### 问题四（口径不当）：⑦ 的两个门槛混用了两套统计逻辑

`stock_analyzer/sentiment_signal.py:407`：

```python
enabled = bool(holdout["n"] >= 100 and holdout_ic >= 0.01 and (holdout["direction_1d"] or 0) >= 0.50)
```

三个门里 `holdout_ic ≥ 0.01` 是**效应量门**（无检验），`direction_1d ≥ 0.50` 是**方向胜率门**（也无检验），`n ≥ 100` 是样本量门。问题在于：

- `_metrics:344-346` 的 IC 是**池化 Spearman**（把 23473 个 (股票, 日) 观测混在一起算一个相关系数），不是"逐日 IC 的均值"。本机估算：23473 / 60 个留出日 ≈ 391 只/日（与冻结清单 389 行吻合）→ 池化把**选日能力**和**选股能力**混成一个数，且把 60 个日期的聚类结构当成 23473 个独立样本。池化 t ≈ 2.30 看着像显著，但**按日聚类后有效样本是 60 不是 23473**。
- `direction_1d = 0.4942` 相对 0.50 的偏离，按 n=23473 的朴素二项 SE（0.00326）算 z = **−1.77**（看似"显著为负"）；但按 60 个留出日聚类，SE 上界 0.0646，z = **−0.09**（完全是噪声）。→ **"差 0.4~0.6pp 就过门"这个描述本身是伪精度**：这个差值在正确的聚类口径下连符号都不可信。
- 更根本的是**选型偏差**：`:383-401` 在 `3 signal_source × 4 half_life × 3 weight_sets = 36` 个候选里按 `fit["score"]` 选 best，然后只在 holdout 上报 best 的指标。36 选 1 的选型偏差**未做任何校正**，而 holdout 只有 60 天。

→ ⑦ 的正确表述：**它连"效应量为正"都还没稳稳建立，谈"差一点就显著"是过度解读。** §0 的纪律「sentiment 是未测出、不是无用」在这里再次成立，但方向要补一句：**也不能说它"接近有用"。**

#### 问题五（结论层）：⑤ 的 Holm p=0.9492 **不能当作"h3 无效"的证据**，但也不能当作"值得马上做"

⑤ 是清单里 t 值最高的一条（1.50），也是唯一"再跑 1 年就可能出结论"的一条。但它有三个口径问题（`解决清单_供Opus复核.md:454` 自己列了）：`h3_strict: false`（严格列是**事后 join** 上去的，不是逐股 purge 重训 —— 只有 h5 是）、现金占比 **45.51%**（h5 6.83% / h10 0.95%，三条腿口径完全不同）、缺 `positive_only/pred_quantile/max_weight/no_refill/universe hash`。→ **它的 t=1.50 不可直接采信，因为分子（效应量）本身来自一条不严格的流水线。** 正确动作是**在 h5 那条严格流水线上重跑 h3**，而不是拿现有 h3 的数字去申请功效预算。

#### 一个反方向的问题：**"不显著"有没有被滥用为不作为的理由？**

我认为**没有**，但有一处值得指出。对方的判决措辞是精确的（`rolling_is_directionally_better_but_not_confirmed_alpha_or_promotion_ready`、`keep_disabled_under_this_protocol`、`prohibited_claim: sentiment_is_generally_useless`），`解决清单_供Opus复核.md:454` 明确写「没有 horizon 通过 Holm 0.05 …… **但这不等于"无 alpha"**」。这是正确的科学表述。

但**把"不显著"和"不该做"混为一谈是错的**，而 §12 的收尾结论「停止继续扩大因子/模型搜索」有这个风险。正确的分解是：

- **不显著 → 不能晋级生产**：✅ 完全正确，应坚持。
- **不显著 → 不该再搜索**：⚠️ 对"搜索更多因子/模型"成立（因为问题二：搜索越多，将来的显著性越不可信），但对**降低 σ 的结构性动作**不成立。⑧ 的 buffer=20（+4.03bp/天、无 CI）、L3 的 stride 支、提高持仓只数——这些是**改变检验精度本身**的动作，不是"再抽一次奖"。在 MIN_FILLED=1.0 / avg_holdings=1.88 的现状下，**先把 σ 从 51.6bp 降下来，再谈功效**，比延长样本到 4.8 年现实得多。
- **不显著 → 不能改成本口径**：❌ 错误。L1 是恒等式，不需要显著性。

### 9.3 一句话

**清单上七条"方向为正但不显著"里，t 值最高的一条是 1.50，其余全在 0.6~1.2 —— 这不是"一批信号被严格标准挡住"，而是"整个已测空间的信噪比都在 1 附近"。** 检验方法本身（循环块 bootstrap + 差值配对 + 中心化零分布 + Holm + CI 下界作门）是**正确且不过度保守的**，所以这些"不显著"的判定我认为可信；但有两处让 p 值**偏乐观**（新增 `rolling_vs_fixed` 的 p 是未校正裸值；两万多个网格候选选出的冠军零选择偏差校正，全仓无 Reality Check / SPA / Deflated Sharpe），一处让研究**在错误的方向上求解**（`MIN_FILLED = 1.0`、`avg_holdings = 1.88` 使日 σ 高达 51.6bp，SE 被顶到 2.25bp —— **降 σ 比等 4.8 年样本便宜得多**），一处口径根本不对（⑦ sentiment 的池化 IC 把 60 个日期当 23473 个独立样本，`direction_1d` 按日聚类后 z 从 −1.77 变成 −0.09，"差 0.4pp 就过门"是伪精度）。**唯一不该做显著性检验的两条（L1 成本恒等式、L9 记账假设）恰好是账面数字最大的两条 —— 别把它们混进这张表。**

---

## 10. 对 `2026-08-13 17:26` 第三次快照的复核（**本文档最新权威结论，优先级高于 §8/§9 中冲突处**）

### 10.1 变更面：438 个文件里只有 2 个动了

`find . -type f -newermt "2026-08-13 15:20"` 精确返回两个文件，mtime 均为 `2026-08-13 17:26`：

```
38846ecdaf9185d203721058774470a5ac25f0c6b55b14051fcebdd0a251a60d  quant/watchlist_grid.py      ← 原 1e34c0ce…，行数不变（980）
bcedf1fb4f78d1ad441889b727d2ac06e5c14e0f3f88e039b0426eeaaa40afed  下一轮调研记录_20260812.md   ← 560 → 586 行（新增 §13 / §13.1）
21a3ebf25ff4e4a396222d43612361a54305a5694fdaf7185746b801fac0f8a3  解决清单_供Opus复核.md       ← 未变（558 行，mtime 仍 15:17）
```

**`watchlist_grid.py` 我此前记录的每一个锚点都逐一在 `file:line` 上复验过，全部依旧成立**：`:168`/`:184` `"rebalance_stride": 1` 字面量 + 两支都是 `for buffer in (0,1,2)`；`:845-846` `combos[key] = value` 整列标量压回；`:851-857` 邻域 `limits` 仍不含 `hold_rank_buffer`、gross 仍是 `<= 1e-12` 精确相等；`:874` `param_id` 仍按位置生成；`:931-933` `--hold-rank-buffer` 默认 `None`；`:962-969` `rebalance_stride` 无条件传、`hold_rank_buffer` 条件传；`:529-566` `selection_score` 的三项仍全部 `/ gross`；`:680` `stability_decision` 在。→ **§8.3 的 8 条「依然成立」在第三次快照上继续成立，一条不减。**

（口径声明：磁盘上唯一的历史副本是 `research_runs/audit_20260810_v1/source/quant/watchlist_grid.py`（951 行、0810），所以 diff 出来的是 **0810→17:26 的累计变更**，不是 15:17→17:26 的净增量；`15:17` 的副本不存在，无法做精确单点 diff。下面第 10.2 条是这份累计 diff 里**我此前未记录过的唯一一项**。）

### 10.2 新增 `--workers` 并行度旋钮 —— 第六种「fixed-but-not-enabled」

`run_grid` 签名新增 `workers: int | None = None`（`:833`），实现：

```python
worker_count = max(int(workers or min(8, os.cpu_count() or 1)), 1)      # :885
if worker_count > (os.cpu_count() or worker_count):                      # :886
    raise ValueError(f"workers={worker_count} exceeds available CPUs={os.cpu_count()}")
chunksize = max(1, min(16, len(items) // max(worker_count * 8, 1)))      # :899
```

CLI `:941-942` `--workers type=int default=8`，`:970` 透传。**三个问题：**

1. **两个生产调用点都没传**：`scheduled_workflow.py:154`（短线网格）与 `:520`（选参网格）的 `run_grid(...)` 参数列表里没有 `workers`；`grep -n "workers" quant/scheduled_workflow.py` 只命中 `--update-workers`（`:1256` 默认 12，数据更新用）。`docker/scheduler_jobs.sh` 的 `--workers` 三处命中（`:372` qfq、`:385` daily 更新、`:409` all_a_meta）**全部与网格无关**。→ 生产等效并行度 = `min(8, cpu_count)` = **8**。
2. **默认值比原来更低**：原硬编码上限是 `min(12, cpu_count)`，新默认 8。本机 `nproc` = 96，**这一改把网格并行度降了 33%**。若意图是"让并行度可调"，那生产侧没接；若意图是"降低资源占用"，则应说明为何 96 核机器上用 8。
3. **无测试覆盖**：网格 `workers` 路径没有任何测试。

→ 已回改 §6.1 E 与阶段 2 的墙钟估算：24300 组合的全网格从「@12 workers 21.4 min」修正为「**@8 workers ≈32 min**」。结论不变 —— 算力仍不是瓶颈，且这 32 min 只落在 `monthly-factor`。

### 10.3 L1 被对方的新记录**部分推翻**（这是本轮最重要的一条）

`下一轮调研记录_20260812.md:583` 记下了用户补充的真实费率：**佣金万5 = 单边 `0.0005`、双边 `0.0010`**。按「万5 + 卖出印花税 0.05%」算，研究 roundtrip ≈ **`0.0020`（20bp）**，**恰好等于当前默认值**，对方据此明确**不修改**成本默认值，并声明 `QUANT_BT_COST_ROUNDTRIP` / `REALTIME_PAPER_COST` / 硬编码成本在拿到账单或成交回单之前一律不动。

**我接受这个更正。** §2 L1 的量级前提是「万2.5 → 10bp」，对这个账户不成立：

| 我原先的 L1 | 更正后 |
|---|---|
| 20bp → 7~10bp，**+9.5~12.4bp/天**，「唯一改一个数就落袋」 | 20bp 对该账户**已是正确口径**；7/10bp 属敏感性场景 |
| 优先级第 1 | 降级为「**待真实账单核实**」，不再作为收益杠杆排序第一 |

**仍然成立的残余部分**（不要连带否掉）：
- `scheduler_jobs.sh:14-19` 注释里「印花税卖出千1」这个**分项**确实过时（2023-08 已减半为 0.05%）；对方 `:565` 也承认「税率与当前制度不一致」。合计之所以仍是 20bp，是因为原注释高估税、同时漏掉过户费/最低佣金，**误差互相抵消** —— 这是巧合，不是注释正确。
- **研究与生产的 env 名分叉未修**：`intraday_1400/` 与 `realtime/` 下 8 处硬编码 `0.002` 不读 env，另有 `INTRADAY_1400_ROUNDTRIP_COST` 这个不同名 env（§2 L1 末段）。将来若真要动成本，仍必须一次对齐三组，否则再次分叉。
- **break-even = 15.45bp**（`:562`，`0.13792985 / 89.3 = 0.00154457`）：当前 20bp **高出盈亏平衡点 4.55bp**。这是 24 窗 `net sum = −0.04067015` 为负的**直接算术原因**，也是比"找更多因子"更硬的一个事实 —— 在 15.45bp 的毛收益/换手比之下，**这个策略要靠降成本或降换手才能过线，而降成本这条路已经堵死（费率就是 20bp）**，所以真正剩下的只有**降换手**（L3 的 stride、L5 的 buffer=20）。

→ **§2 排序更新：L1 出列，L3（降换手 / 拉长持有期）上升为第一顺位。** 理由不是 L3 的证据更强（它的证据仍被污染，见 §2 L3），而是 break-even 恒等式指向的唯一可动变量就是换手。

### 10.4 §13.1 直接证明：成本**不是**不显著的原因（这是对 §9 的确认与延伸）

对方在同一批 24 窗 returns 上（不重训、不改 PIT/label/持仓，只按 `gross_ret - turnover * cost_roundtrip` 重算）扫了成本对 rolling−fixed 的影响：

| roundtrip | rolling | fixed | rolling−fixed | CI95 | 单侧 p |
|---|---|---|---|---|---|
| 0bp | +2.8916 | +0.7916 | **+2.1000** | [−2.2257, +6.5352] | 0.1787 |
| 7bp | +1.5811 | −0.5967 | **+2.1778** | [−2.1448, +6.6224] | 0.1684 |
| 10bp | +1.0195 | −1.1916 | **+2.2111** | [−2.1071, +6.6531] | 0.1659 |
| 15.4457bp（break-even） | ≈0 | −2.2716 | **+2.2716** | [−2.0449, +6.7092] | 0.1593 |
| 20bp（现默认） | −0.8526 | −3.1749 | **+2.3222** | [−1.9894, +6.7543] | 0.1541 |

（单位 bp/天。）**关键读法：差异随成本上升而略微 _扩大_（rolling 换手更高，降成本反而更利于 fixed），0→20bp 全程只移动 ~0.22bp/天，而 CI 半宽约 4.4bp** —— 相差一个数量级。所以：

- **成本校准能决定 rolling 自身净收益的符号（7/10bp 转正、20bp 为负），但完全不能把 rolling−fixed 推过显著门。** 这与我 §9 的判断一致，并且是更强的形式：不是"成本不是主因"，而是"成本在这条差异上几乎无杠杆"。
- 也就是说 §9.1 那七条"方向为正但不显著"里，**没有一条能靠调成本口径救回来**。σ 才是约束（§9.2 问题三：`MIN_FILLED = 1.0` + `avg_holdings = 1.8763` → 日 σ ≈ 51.6bp）。

**一处需要纠正对方的数字**：`:560` 写「复核估算 MDE 约 28bp」。那个 28bp 是我在 **176~225 天**样本、旧口径下算出的数；这里样本是 **477 个共同交易日**，按对方自己报的 CI95 反解 `SE = (6.7543 − (−1.9894)) / 3.92 ≈ 2.23bp`，单侧 α=0.05 / 80% 功效的 **MDE80 ≈ 2.486 × 2.23 ≈ 5.6bp/天**，不是 28bp。差 5 倍。这个错误方向上是**过度悲观**：真实需要检出的门槛比他们以为的低得多（观测到的 +2.32bp 距 5.6bp 只差 2.4 倍，而不是差 12 倍），所以「当前实验对小于该量级的优势没有检验能力」这句话应该改成「**需要约 2.4× 现有样本量或约 √2.4 ≈ 1.55× 的 σ 削减**」—— 这是可达的，不该按"无能力"结案。

### 10.5 一句话（第三次快照版）

**代码侧这一轮只多了一个没接到生产的并行度旋钮（默认还从 12 降到 8），此前的 8 条缺陷判定一条没被推翻；记录侧则贡献了两个真正改变结论的事实 —— ①用户的实际费率就是万5+0.05%印花税 ≈ 20bp，我的 L1「改成本口径落袋 +9.5~12.4bp/天」对这个账户不成立，L1 降级、L3（降换手）接替第一顺位；②成本从 0 扫到 20bp 只让 rolling−fixed 移动 0.22bp/天，而 CI 半宽 4.4bp，所以成本这条路对显著性零杠杆。合起来指向同一个出口：break-even 是 15.45bp、实际成本 20bp，唯一还能动的变量是换手，而不是成本、也不是再多搜因子。**（对方 §13 引用的「MDE 约 28bp」是旧 176 天样本的数，477 天口径下应为 ≈5.6bp，见 §10.4。）

---

## 11. 「这些结论是在相同条件下和基线对比的吗」—— 可比性与可信度分级审计（2026-08-13 追加）

问题问得对：**前面 §8/§9/§10 所有数字的可信度不是一个档，差别极大，而差别的来源正是"和什么比、在什么条件下比"。** 本节把每条结论按可比性分成 A~E 五级，并给出我能独立验证到哪一步。

### 11.1 可比性阶梯（一览）

| 级 | 对比 | 两侧条件是否一致 | 可信度 | 致命问题 |
|---|---|---|---|---|
| **A** | rolling vs `fixed_control_derived_v1`（24 窗，+2.32bp/天） | **配对机制是严格的**（代码强制） | **配对可信，基线不可信** | 基线由处理组自己反推、退化成 **1 因子 vs 30 因子**、且含全样本后视 |
| **B** | rolling vs 市场等权 / PIT 行业等权（−10.33bp/天） | ❌ **敞口 18.8% vs 100%** | **不成立，应弃用** | 拿一个 19% 仓位的组合去比 100% 满仓基准 |
| **C** | stride 1 vs 2 vs 3（+49.8% / +31.4% / +13.2%） | ✅ 同日历、同参数、同标签 | **方向可信，量级不可信** | 零 CI、59~176 期、单次运行 |
| **D** | `hold_rank_buffer` 0/2/20/50（+4.03bp/天） | ❌ **口径断裂 + 样本 30 期** | **不可用作证据** | 该探针关掉了可交易过滤；且收益差 **95% 不是省成本来的** |
| **E** | h3 / h5 / h10（+73.47 / +21.18 / +62.91bp/周期） | ❌ 三条不同流水线 | **h5 之外不可用** | h3/h10 `strict=false`、现金 45.5%/6.8%/1.0%、周期 69/41/21 |

### 11.2 A 级：24 窗 rolling−fixed —— **配对是真严格的，但基线站不住**

**先说做对的部分**（这是全仓库配对质量最高的一处，`quant/rolling_fixed_relative_comparison.py`）：

```python
if r_keys != f_keys:                                    # :107
    raise ValueError("rolling/fixed prediction key hashes differ")
if not rolling[LABEL].equals(fixed[LABEL]):             # :109
    raise ValueError("rolling/fixed strict labels differ")
if not rolling[BUYABLE].equals(fixed[BUYABLE]):         # :111
    raise ValueError("rolling/fixed buyability masks differ")
```

→ **`(code, date)` 键集、`tradable_ret_1d` 标签、`buyable_close` 可买掩码三者不一致就直接抛异常**，不是"事后检查"而是**前置门**。加上 `:121` 的 `join(how="inner")` 得到 477 个共同交易日、`下一轮调研记录_20260812.md:537` 记明两臂**复用同一份 v8 prepared panel/cache**、同一 `000852.SH` PIT、同一 `batch-size=25`、同一 20bp 成本、同一持仓机制。**这是教科书式的配对设计**，`intraday_1400/evaluation.py:38` 再对**差值序列**做循环块 bootstrap，自相关也保留了。

**我能独立验证到哪一步**：本机没有 `quant_data/`、也没有那两份 returns parquet，**无法从原始数据复算**。我能做的是**内部一致性反解**，做了两处，都通过：
1. 由对方 §13.1 的成本曲线反解隐含换手：`(2.8916 − (−0.8526)) × 1e−4 / 0.002 = 0.18721` —— 与他们独立报告的 `avg_turnover = 0.18721174` **吻合到 5 位小数**。
2. `0.18721174 × 0.002 × 477 = 0.178600` —— 与 `cost sum = 0.17860000` **逐位相等**。
→ 所以这批数字**内部是自洽的、不是手填的**。但"自洽"不等于"可复现"：源数据在研究主机上，本机不可达。

**基线的三个问题（这才是 A 级降级的原因）：**

1. **基线是从处理组自己反推出来的。** `:544`：`fixed_control_derived_v1` 的规则是"在正式 rolling 的 24 个窗口中入选频率 ≥ 12 次"。→ **这不是独立对照，是 rolling 自己选择结果的一个收缩版本。** 一个合格的 fixed 对照必须在**看不到 rolling 结果**的前提下确定（例如预注册一份因子清单，或用样本外第一个窗口定死）。
2. **它退化成了「1 因子 vs 30 因子」。** `:544` 明说：只有 `intraday_range` 达标（24/24），`volatility_10`/`volatility_5` 各 9/24、`volatility_20` 仅 5/24，**都没进**。而 rolling 每窗 `selected_count = 30`（`full_train_batched.py:2026` `--rolling-top-factors` 默认 30，`:1503` 传入 `_rolling_factor_selection(top_n=…)`）。→ **实际测的是"30 个因子 vs 1 个因子"，不是"自适应选择 vs 固定选择"。** `+2.32bp/天` 里有多少来自"会换因子"、多少来自"因子多"，这个设计**无法分离**。想测前者，对照组必须也是 30 个因子（例如全期第一个窗口选出的 30 个冻结不动）。
3. **基线里含全样本后视。** "24 窗中入选 ≥12 次"这个条件用到了**全部 24 个窗口**（含未来），所以 `intraday_range` 是**带后视挑出的最强单因子**。方向上这让基线**偏强** → `+2.32bp/天` 是**保守**的（真实差异可能更大）。这是唯一一个方向对 rolling 有利的偏差，但代价是这个对照**不能解释成"2024 年当时能做的选择"**。
4. 顺带一个反直觉事实：由 §13.1 反解，**fixed 臂的换手 0.19833 反而比 rolling 的 0.18721 更高**。1 个因子的组合换手更高 —— 说明这个 control 的行为并不是"更稳定的固定策略"，而是"信号更弱因此排名更抖"。这进一步说明它不是一个好基准。

**A 级结论**：配对机制我给满分，**但"rolling 比 fixed 好 2.32bp/天"这句话的可信度受限于基线，不受限于统计**。就算样本再长 2.4 倍把它做显著，能得到的结论也只是"30 因子优于 1 因子"，**不足以支撑"滚动选因子有价值"这个原本要测的命题**。

### 11.3 B 级：vs 市场等权 / 行业等权 —— **这个对比根本不成立，应当弃用**

`:123-126` 的四个 excess 列是**直接相减**：

```python
result["rolling_market_excess"] = result["rolling_absolute_return"] - result["market_equal_weight"]   # :123
```

而 `:81` 的基准是 `data.groupby("date")[LABEL].mean()` —— **全池等权、100% 满仓**。策略这一侧呢？`下一轮调研记录_20260812.md:542` 记 `平均持仓数 = 1.8763`，`backtest.py:314` 的 `w = min(1/top_n, max_weight)`，`cost_ablation.parameters` 是 `top_n=3, gross_exposure=0.3` → **平均投入敞口 ≈ 1.8763 × 0.1 = 18.8%，其余 81% 是现金**。

由报告的 `-0.00103251` 反解：**市场等权在这 477 天是 +9.47bp/天（累计 +57%）**。→ 所谓"rolling 跑输市场 10.33bp/天"，**绝大部分就是"没满仓"**，跟选股能力无关。

**按单位投入资金归一化后符号会翻**：rolling 毛收益 `2.89 / 0.1876 = 15.41bp/天`，市场 `9.47bp/天` → **单位敞口毛 alpha 是 +5.94bp/天，正的**。（但同一归一化会把成本从 3.74 放大到 19.9bp/天，净仍为负 —— 所以这不是好消息，只是说明**原始那个 −10.33bp 数字既高估了坏消息、也测错了东西**。）

→ **B 级的两条 excess 列（以及基于它们的 Holm 家族）应当从证据体系里撤下**，除非改成敞口匹配的基准（等权池 × 18.8% + 81.2% 现金），或者改用信息比率/回归 alpha。**注意这两条恰好是 `:137-139` 里唯一进 Holm 家族的对比** —— 也就是说，被校正的是两个不该比的对比，而唯一该比的 `rolling_vs_fixed`（`:140-146`）**没进家族**（§9.2 问题一）。

### 11.4 C 级：stride 1/2/3 —— **同条件，可比；但只有一次运行、零 CI**

这一组反而是条件最干净的：`audit_20260812_rolling_fixed_paired_validation.json` 的 `cost_ablation` 三档共用 `top_n=3 / gross 0.3 / ridge_quantile 0.4`、同一份预测、同一 20bp、**同一日历区间**（`_apply_rebalance_stride:161` 是 `calendar_dates[::stride]`，从同一权威日历抽取，不重新起相位），只是周期数 176 / 88 / 59。

| stride | 周期 | 同期总收益 | 每周期净 | 每周期成本 |
|---|---|---|---|---|
| 1 | 176 | **+0.49844** | 23.01bp | 5.10bp |
| 2 | 88 | +0.31420 | **31.10bp** | 5.55bp |
| 3 | 59 | +0.13177 | 21.00bp | 5.76bp |

**两个读法都要给**：按**同一日历区间总收益**（这才是可交易口径），stride 越长越差，L3「拉长持有期」在信号侧确实有害 —— §2 L3 的判断成立。但按**每周期**看，stride2 的 31.10bp 反而**高于** stride1 的 23.01bp，说明单次调仓的信号质量没变差，输的是**调仓次数**。→ 正确的施工含义不是"别拉长持有期"，而是**"拉长持有期必须同时提高单次仓位或提高持仓只数，否则等于自愿减少下注次数"**。

**但这三个数字都没有 CI**（`cost_ablation` 只有 4 个标量字段），报告自己的判决是 `do_not_change_production_stride_or_hold_buffer`。所以 C 级是"方向可信、量级不可信"，不能拿 `+49.8%` 对 `+13.2%` 去算什么。另外一个反常需要记：**stride 越长每周期换手反而越高**（0.2551 → 0.2773 → 0.2881），即"少调仓省成本"在这份数据上**每周期并没省**，省的是次数。

### 11.5 D 级：`hold_rank_buffer` 诊断 —— **口径断裂，且收益差 95% 不是省成本来的（本轮新算）**

`audit_20260811_portfolio_hysteresis_validation.json` 有两处硬伤：

1. **口径断裂**：`real_prediction_validation.execution_filter_for_parity_probe` 自己写明 `QUANT_BT_FILTER_UNTRADABLE=0 because the selected prediction artifact lacks buyability columns`。→ **这一组是在关掉可交易过滤的乐观口径下跑的**，而 A 级的 24 窗跑是严格 `tradable-label` + `buyable_close`。**两者不在同一口径，数字不可互引。**
2. **样本 30 期、`top_n=2`**：`input_dates=60`、`rebalance_stride=2` → `evaluated_dates=30`；`sharpe: 34.40` 这种数值本身就说明年化被 30 个 stride-2 日期放大到失去意义。

**本轮新做的分解（这条足以否掉 ⑧）**：buffer 20 vs 0，换手从 `0.27931` 降到 `0.25862`，降幅 `0.02069`。→ 30 个评估日省下的成本 = `0.02069 × 0.002 × 30 = 0.00124`，即 **12.4bp**。而 `total_return` 差是 `0.5369971 − 0.5128259 = 0.02417`，即 **241.7bp**。

→ **省成本只解释了收益差的 5.1%**（buffer=50 是 11.0%）。剩下 **95%** 来自"因为迟滞而持有了不同的股票"。在 `top_n=2`、30 个评估日的样本上，这**纯粹是样本内运气**，不是"降换手带来的收益"。报告自己的 `interpretation_limit` 也写了 `these in-sample diagnostics do not select a production buffer or establish future alpha`。

→ **§9.1 ⑧「buffer=20 = +4.03bp/天」必须撤销作为"降换手有收益"的证据。** 它唯一证明的是 `_select_with_rank_hysteresis` 状态机能降换手（12.4bp 那部分），**这部分是恒等式，且量级只有 0.4bp/天**。同时 `buffer=2` 与 `buffer=0` 三个指标逐位相同 —— 说明在 `top_n=2` 下 buffer 2 根本不可能触发。

### 11.6 E 级：h3/h5/h10 —— 三条不同流水线；**而且基线选择本身就移动 1/4 效应量**

`research_runs/audit_20260810_v1/quant_data_h5/full_a_2018_wide/audit_20260810_horizon_controls.json`：三个 horizon 共用 205 个共同日期、`top_n=5`、20bp、200 seeds、5000 bootstrap、9 项 Holm family —— **控制设计本身是这批证据里最完整的一份**（它同时有 market / random / shuffle 三个基线）。但：

- **三条流水线不同**：`解决清单_供Opus复核.md:453` 明写 h3/h10 `strict=false`、严格列是**事后 join**，只有 h5 是逐股 purge + 3 个月 validation 的严格重训。
- **现金比例差 45 倍**：`cash_mean` h3 = 0.4551、h5 = 0.0683、h10 = 0.0095。h3 有 45.5% 现金却去比 100% 满仓的 market —— **和 §11.3 同一个病**。
- **周期数 69 / 41 / 21**，效应量不能横比。

**但这份报告贡献了一个关键事实：换基线就换答案。** 同一 h3、同一 69 个周期：

| 基线 | 基线自身均值 | 观测差 | CI95 | 本机反解 t |
|---|---|---|---|---|
| market 等权 | −7.56bp/周期 | +73.47bp | `[−19.86, +172.47]` | **1.497** |
| random（200 seeds） | −24.80bp/周期 | +90.71bp | `[−0.25, +188.54]` | **1.862** |
| shuffle | −26.87bp/周期 | +92.78bp | `[−0.48, +190.88]` | 1.855 |

→ **market 与 random 两个"零假设"本身就差 17.24bp/周期**（约等于 1/4 个效应量）。换成 random 基线，t 从 1.50 升到 1.86、CI 下界从 −19.86 升到 −0.25（**几乎贴到 0**）、`p_raw` 从 0.1356 降到 0.0588。→ **§9.1 ⑤「t=1.50」这个数字本身是基线选择的产物**；换个同样合理的基线就变 1.86。这不是造假，而是说明**在这个信噪比下，基线定义的自由度足以移动结论**，所以任何单一 t 值都不该被当成硬事实。（也提醒：`p_raw = 0.0588` 这种"差一点就过 0.05"的数，正是 §9.2 问题二说的、在两万多网格行的选择偏差下最不可信的区间。）

### 11.7 成本敏感性的内部有效性：**是"重定价"，不是"重优化"**

§13.1 明写「不重训、不改变 PIT、label、provenance 或持仓，仅按 `gross_ret - turnover * cost_roundtrip` 重算」。这在**算术上完全正确**（`backtest.py:329-330` 就是 `cost = turnover × cost_roundtrip`、`period_ret = gross_ret − cost`，我已逐位对上），但要明确它答的是哪个问题：

- ✅ 它答：**"同样这套持仓，在 7bp 世界里账面是多少"** → `+1.58bp/天`。
- ❌ 它不答：**"如果一开始就按 7bp 优化，能做到多少"**。7bp 世界里最优解会**主动提高换手**去吃更多短期信号，所以真实的 7bp 策略应当**优于** `+1.58bp/天`。→ 这个方向是**保守**的，可以放心引用为下界。

同理，`turnover` 的定义要记清：`_weight_turnover:107` 是 `0.5 × (Σ|Δw_stock| + |Δcash|)`，即**含现金腿的 NAV 级单边换手**。所以 `0.1872` 已经是 NAV 口径，不需要再乘敞口 —— §8.6 那条逐位相等的验算之所以成立，正是因为这一点。

### 11.8 回答：可信度够不够

**分三层答。**

**① 统计机器：够。** 循环块 bootstrap + 差值配对 + 中心化零分布 + `(count+1)/(B+1)` + Holm + 用 `ci95[0] > 0` 作门（`daily_optimization_pipeline.py:329-337`），这套是正确的、不过度保守的（§9.2）。**所以"不显著"这个判定本身可信。**

**② 配对条件：A 级够，其余不够。** 只有 24 窗 rolling−fixed 是代码强制的严格同条件配对（键/标签/可买掩码三重前置门 + inner join）。B 级（vs 市场）敞口 19% 比 100%，**根本不可比，应撤下**；D 级（buffer）关掉了可交易过滤，**口径与主线不一致**；E 级（horizon）三条流水线两条 `strict=false`；C 级（stride）条件干净但零 CI。→ **§9.1 那张七行表里，只有 ① 一条的"条件相同"是站得住的；②③ 是它的旧样本切法，④ 是自比，⑤⑥ 是 E 级，⑦ 是 IC 口径。**

**③ 基线本身：最弱的一环，也是此前被低估的一环。** 唯一站得住的 A 级对比，**基线是从处理组反推的、退化成 1 因子、且含全样本后视**。所以哪怕将来样本够长把 `+2.32bp/天` 做显著，得到的命题也只是"30 因子优于 1 因子"。**要测原本想测的"滚动选因子 vs 固定选因子"，必须补一个 30 因子的冻结对照组** —— 这是本节唯一的施工建议，而且它不需要新代码：把第一个窗口选出的 30 个因子冻结成一份 `factor_selection_*` 清单、用 `--require-selection-provenance` 跑同一条链即可，成本约等于再跑一次 24 窗。

**一句话**：**统计方法可信，唯一严格同条件的配对也可信，但那个配对的基线不可信 —— 而不可信的方向恰好让 rolling 显得保守。所以当前证据的正确结论是"尚未测出"而非"没有效果"；而在补上 30 因子冻结对照之前，`+2.32bp/天` 不该被解释成"滚动选因子的价值"。** 另外两条要立刻从证据体系里撤下：**vs 市场等权的 −10.33bp/天（敞口不匹配）**、**buffer=20 的 +4.03bp/天（95% 不是省成本来的，且口径断裂）**。

---

## 12. 对 `2026-08-14 10:34` 第四次快照的收益方向复核（**本文档最新权威结论，优先级高于 §1~§11 全部冲突处**）

> 本节回答的是「最新代码已更新，重新复核，看收益方向」。结论先行：**代码侧本轮零收益改动；收益侧唯一的实质进展是一条负面结论 —— §11.8 我要求的 30 因子冻结对照被执行了，它把 `+2.32bp/天` 清掉了。** 这是好事：账变干净了。

### 12.0 快照锚点与变更面

438 个文件 mtime 全部为 `2026-08-14 10:34`（整体重拷，`git rev-parse` → `not a git repository`），**mtime 不携带任何信号，SHA256 是唯一版本锚**。对 15 个关键文件做 SHA256 对账，只有 5 个相对 `2026-08-13 17:26` 快照发生变化：

| 文件 | 状态 | 变更实质 |
|---|---|---|
| `quant/full_train_batched.py`（2154 行 `77dd8ced49830f58`） | **CHANGED** | 相对 0810 归档 diff 115 行：窗口化 `_price_source_signature(start, end)`（`:192-224`）、`DailyICCache(recipe_signature=…)`（`:548-584`）、新增 purge 窗完成率门 |
| `quant/scheduled_workflow.py`（1565 行 `c13779205d2f784d`） | **CHANGED** | stride/buffer 传参改成「stride 无条件 + buffer 仅显式时」；`validate_publish_dry_run` 的 source-missing 分支 |
| `quant/model.py`（1078 行 `ba0ad9f18bd948c3`） | **CHANGED** | **diff 仅 LGBMRanker 线程管线**（`:653 model_threads`、`:679-680 ranker_threads`） |
| `docker/scheduler_jobs.sh`（502 行 `5fa091d14e7b454d`） | **CHANGED** | 严格 flag 已补齐，但降换手 flag 仍零传参（见 §12.3） |
| `下一轮调研记录_20260812.md`（707 行 `ceefd306cbceedd0`） | **CHANGED** | 586 → 707 行，新增 §14/§15/§16/§17 |
| `quant/watchlist_grid.py`（980 行 `38846ecdaf9185d2`） | **UNCHANGED** | 与 8/13 17:26 逐字节相同 |
| `quant/rolling_fixed_relative_comparison.py`、`quant/backtest.py`、`quant/daily_optimization_pipeline.py`、`intraday_1400/evaluation.py`、`quant/sentiment_feature_experiment.py`、`解决清单_供Opus复核.md` | **UNCHANGED** | 同上 |

**收益相关的代码变更是零。** 三个 CHANGED 的 Python 文件全是工程正确性：线程数、缓存键、跳窗保护。特别地 —— **`train_ridge:371` 的 `alpha: float = 10.0` 未变，`train_all:1003` 的 `ridge_alpha=10.0` / `:1029 kwargs["alpha"]=ridge_alpha` 也未变** → 支撑「分数特征无用」这一结论的 **ridge≈OLS 的同向族 ~1/k 摊薄机制原封不动**。§0 的表述纪律（「未测出」不是「无用」）在本快照上继续成立，一字不改。

### 12.1 §11.8 的施工建议已被执行 —— 结果是负面的，`+2.32bp/天` 正式作废

§11.8 我写的唯一施工建议是：「**要测原本想测的『滚动选因子 vs 固定选因子』，必须补一个 30 因子的冻结对照组**……把第一个窗口选出的 30 个因子冻结成一份清单、用 `--require-selection-provenance` 跑同一条链即可」。`下一轮调研记录_20260812.md` §17.1 做的正是这件事：

- 对照组标识 `frozen_first_window_top30_v1` —— **用第一个窗口选出的 30 因子冻结**，因子数与处理组相同（30 vs 30），不再是 1 vs 30
- 两侧各 **229,524 行**预测，**340 个共同收益日**
- rolling **+11.0967bp/天** vs frozen **+16.9530bp/天** → **rolling − frozen = −5.8564bp/天**
- block 长度 3 / 5 / 10 / 20 四档 CI **全部跨零**；裸单侧 p ≈ 0.92~0.93；**Holm 校正后 = 1.0**

**这条结果同时结算了三笔账：**

1. **`+2.32bp/天` 作废。** §11.2 的三个基线问题（基线由处理组反推 / 退化成 1 因子 vs 30 因子 / 含全样本后视）现在有了直接的实验证据：换成同因子数的公平冻结对照后，**符号翻转、量级从 +2.32 变成 −5.86**。这坐实了「原先测的是『30 因子 vs 1 因子』而不是『自适应 vs 固定』」这个批评 —— A 级对比的可信度评级由「配对可信、基线不可信」正式落定为「**基线已被替换，原结论撤销**」。
2. **但不能反过来说 rolling 有害。** 点估计 −5.8564bp/天 偏负，然而四档 block 的 CI 全部跨零、Holm 后 p=1.0。**正确表述是「测不出差别」**，与 §0 的表述纪律一致：这是 statistical non-detection，不是 negative effect。
3. **§11.2 里那条「全样本后视让基线偏强 → `+2.32bp` 是保守估计」的推理，方向判断对了但幅度完全不够。** 我当时说这个偏差「唯一一个方向对 rolling 有利」，实际它不只是让结论保守，而是**大到足以吃掉整个效应并翻转符号**。这是本轮我自己需要记下的一条推理教训：**当基线含后视时，不要用「所以真实效应更大」来给结论加保险 —— 后视基线的偏差量级不可先验估计。**

**可审计性声明（重要）**：`frozen_first_window_top30_v1`、`229524`、`340 个共同收益日期` 这些串在全仓库 grep **只出现在 `下一轮调研记录_20260812.md`（`:684`、`:685`）内部**，§17 的实验脚本**不在仓库里**，`research_runs/` 仍只有 `audit_20260810_v1`。所以上述结论我**只做了算术一致性检查，无法独立复现**。这与 §11.2 的处境相同（源数据在研究主机上，本机不可达）。

### 12.2 收益方向的唯一存活杠杆：降换手 —— 机制真实，显著性不足

24 窗算术未变且自洽：毛 **+2.89bp/天**、成本 **−3.74bp/天**、净 **−0.85bp/天**；`0.18721174 × 0.002 × 477 = 0.178600` 与 cost sum 逐位相等 → **成本严格等于 换手 × 20bp**。**break-even = 15.45bp，实际费率 = 20bp，缺口 4.55bp** —— §10.3 的恒等式在本快照继续成立，而 §10.5 由它推出的「唯一还能动的变量是换手」现在有了直接实验。

`下一轮调研记录_20260812.md` §14.4 的**同条件配对**换手探针（n=477、block=5、samples=10000、seed=42），这是**修好了 §11.5 D 级两处硬伤之后**的重跑（不再关掉可交易过滤、不再是 30 期 `top_n=2`）：

| 臂 | gross | turnover | Δ @20bp | 单侧 p |
|---|---|---|---|---|
| **rolling** buffer=50 vs 0 | 3.0211 vs 3.1067 | 0.2277 vs 0.2851 | **+1.0632bp/天** | 0.29577 |
| **fixed** buffer=50 vs 0 | — | — | **−1.8874bp/天** | 0.79932 |

（rolling 侧 CI95 `[−2.8144, +4.9317]`。）

**为什么这条我认为是「机制真实」而不是又一次样本内运气** —— 三点交叉验证：
- **毛收益几乎不变**（3.0211 vs 3.1067，−2.8%）而**换手降 20.1%**（0.2851 → 0.2277）。§11.5 里 buffer=20 的致命问题是「收益差 95% 不是省成本来的」，这次不是：增益的来源是换手项，结构上对得上。
- **量级与恒等式吻合**：`(0.2851 − 0.2277) × 0.002 = 1.148bp/天` 的理论成本节省 vs 观测 `+1.0632bp/天` —— 差额 0.08bp 由毛收益的轻微下降解释。**这是一个可以用恒等式独立核对的数，不是拟合出来的。**
- 方向与 break-even 缺口一致：策略缺 4.55bp 过线，降换手是恒等式允许的唯一入口（成本已被 §10.3 堵死）。

**为什么仍然不能改生产** —— 三点：
- **p = 0.29577，CI 跨零。** 按 §10.4 的口径，这批数据的 MDE80 ≈ 5.6bp/天，而 +1.06bp 只有它的 1/5 —— **需要约 (5.6/1.06)² ≈ 28 倍样本量或等效的 σ 削减**才能把这个量级做显著。这不是「差 2.4 倍」那一档，是真的远。
- **fixed 侧符号相反（−1.8874bp/天）** → 不普适。同一个 buffer=50 在两条因子路径上一正一负，说明它与信号结构耦合，不是一个纯粹的成本旋钮。
- **`stride` 路线已死**：stride=2 在 §11.4 已判定同日历总收益下降；stride=3 只剩 159 个调仓日，样本不足。

→ **§2 的排序更新：L3 的「拉长持有期」这一支出列（stride 已死），L3 收缩为「降换手 = `hold_rank_buffer` 迟滞」这一支，仍是第一顺位，但状态从「值得做实验」降为「实验已做、方向对、功效差 28 倍」。**

**而且这条杠杆今天即使搜出来也用不上** —— 见 §12.3 最后一行：`hold_rank_buffer` 生产选股链**根本不读**。

### 12.3 对我自己三条旧判定的更正（`HANDOFF_20260812_1600.md` §3.1/§3.2/§3.3 已过时）

本轮逐行复验 `watchlist_grid.py`（该文件本身 UNCHANGED，所以这三条是**我先前读错/读漏**，不是代码变了）：

**① §3.1 错了 —— 短线网格是 24300 组，不是 8100；`hold_rank_buffer` 是真正的搜索维度。**

`_base_combos:126-190` 的短线分支是 7 层嵌套：`5 ic × 5 top_n × 3 gross × 6 rq × 6 pq × 3 naive × 3 buffer = 24300`（本机纯 Python 复算：短线 24300 行 / 24300 唯一；swing 576/576 → ×3 naive = 1728）。关键在 `:168-169` 两行**性质不同**：

```python
"rebalance_stride": 1,      # :168 字面量常量 —— 我先前的判定对
"hold_rank_buffer": buffer, # :169 循环变量 for buffer in (0,1,2) —— 我先前判定错
```

→ `hold_rank_buffer ∈ {0,1,2}` **只在显式传 CLI flag 时**才被 `run_grid:826-846` 的 `fixed_params` 整列压回常量（`combos[key] = value`，且执行在邻域过滤之前）。而 `scheduled_workflow.py` 的三个调用点（`:154-174 run_short_grid`、`:499-532 _promotion_gate`、`watchlist_grid.py:952-971` CLI main）都是「**stride 无条件传、buffer 仅在 `*_explicit` 为真时传**」，`*_explicit` 由 `:1390-1391` 从 `sys.argv[1:]` 计算。**四个生产 job（`scheduler_jobs.sh:132/164/194/228`）对 `hold-rank-buffer` 的传参次数是 0** → 生产等效行为是「buffer 参与搜索 {0,1,2}」，不是「常量 0」。

**修正后的净判断没有变好，只是变准了**：值得试的 buffer 量级是 **50**（§12.2），而网格只搜到 **2**；§11.5 已实测 `buffer=2` 与 `buffer=0` 在 `top_n=2` 下三个指标逐位相同（根本不触发）。所以**「buffer 在网格里」和「buffer 的有效取值在网格里」是两件事，后者仍不成立**。这是「fixed-but-not-enabled」的第七种变体：**参数进了搜索空间，但搜索范围不覆盖有效区间。**

**② §3.3 已修 —— `stability_decision` 的月胜率池化。**

`:679-754` 现在是逐 horizon 计算 + `all(...)` 门：

```python
        monthly_win_rate = wins / months if months else 0.0
        monthly_win_rates.append(monthly_win_rate)          # 逐 horizon
...
    passed = (significant >= 1 and acceptable == common and common > 0
              and monthly_win_rates
              and all(rate >= float(min_monthly_win_rate) for rate in monthly_win_rates))
```

旧的池化版本可在 `research_runs/audit_20260810_v1/source/quant/watchlist_grid.py:717-731` 比对。**残留两种口径共存需要记一笔**：标量 `monthly_win_rate` 是逐 horizon 率的等权均值（`:736-738`），而 `monthly_wins` / `months` 仍是跨 horizon 求和（`:753-754`）—— 下游若读后两个字段，拿到的仍是池化口径。

**门确实变严了，但被网格增大抵消。** 本机二项分布测算（7 个日历月、阈值 2/3 → 需 ≥5 胜）：

| 真实月胜率 | 单 horizon 通过率 | 旧池化（21 样本需 ≥14） | 新版 `all(3 horizons)` |
|---|---|---|---|
| 0.50（无 alpha） | 22.66% | 9.46% | **1.16%** |
| 0.60 | 41.99% | 34.95% | 7.40% |

→ 无 alpha 时通过率从 9.46% 降到 1.16%（严了 8 倍），**但放进 24300 组网格，期望假阳仍有约 283 组**。门从 8100 组的时代变严 8 倍、搜索空间同时涨 3 倍 —— **净下来搜索偏差只改善 ~2.7 倍，而且绝对水位仍是「几百个纯噪声组合能过门」**。§9.2 问题二（网格选择偏差无控制）不因此缓解。

**③ §3.2 已修 —— `selection_score` 敞口归一化。**

`_selection_summary:529-566` 三个带敞口项已全部除以 `gross`（`gross = gross.fillna(max_weight * top_n)` 后 `.where(gross > 0)`），「反转 → 恒选下界 0.18 → 旧冠军全部失效」的担忧不成立。这与 §8.1 / §10.1 已记录的一致，此处只是把 `HANDOFF` 里的过时措辞一并作废。

### 12.4 文档声称与代码不符的四处（决定记录里结论的可信度）

这四条必须点明，因为它们决定了 §12.1 / §12.2 的数字能被信到什么程度：

1. **§14/§17 的实验脚本不在仓库。** `turnover_structure` / `paired_candidates` / `matched_buffer` / `frozen_first_window_top30_v1` / `229524` 全仓 grep 只命中 `下一轮调研记录_20260812.md`（`:595`、`:628`、`:684`、`:685`）。→ §12.1 §12.2 的所有数字我**只做了算术一致性核对**（§12.2 的换手恒等式核对通过），无法独立复现。
2. **§15.1 声称的统计口径修复不在这份代码里。** `intraday_1400/pipeline.py:692-717`（`_select_features`）与 `:734-773`（`_select_minute_features_grouped`）**仍是池化 correlation**：`target = train.groupby("date")[label_col].rank(pct=True) - 0.5` 之后直接 `values[valid].corr(target[valid])` 全样本一次。**没有逐日 Spearman IC、没有跨日等权、全仓找不到「至少 5 股票 / 20 日期」这个参数**。`intraday_1400/` 里 `spearman` 只出现在 `pipeline.py:799` 的 `_realized_leg_metrics`，与特征筛选无关。→ 「分钟特征的统计口径已修」这个前提**不能从用户给我的代码里确认**；§0 对分钟特征的「未测出」表述必须保留。
3. **`INTRADAY_1400_NONPRICE_PREPARED_DIR` 全仓零命中** —— 29 个 `INTRADAY_1400_*` 环境变量清单里没有它。
4. **§17.2 的「预测文件不存在也拒绝」不成立。** `realtime/reference.py:169-197` 的 `assert_prediction_fresh` 首个分支是**静默放行**：

```python
    path = cfg.predictions_file
    if not path.exists():
        return None                     # ← 不抛，直接放行
```

只有两个调用点（`reference.py:212` 在 `_load_expected_return` 内、`realtime/engine.py:264` 在 `run()` 开头），而绕过路径有六条：`realtime/diag.py:28/41`、`reference._build_calibration:414`、`engine._maybe_reload:236`、`stock_analyzer/quant_signal.py:565-579`、`realtime/weight_shadow.py:259/458`。**freshness 无任何单测**（`sim_streams.py` 的假 `Cfg:63-126` 连 `prediction_max_age_days` 属性都没有；`realtime/config.py:117-118` 默认 3 天）。

**另记：§15 的分钟特征仍是「未定论」而非「无效」。** 两次 runner `exit=137` OOM（12GiB@threads=8、14GiB@threads=2、无报告产出）是**工程失败**，记录自己也明确写了这不构成无效证据。§16 我先前的两条建议仍被缺失的 `buyable_close` / `tradable_ret_1d` sidecar 阻塞。

### 12.5 搜索偏差仍然零控制（这是收益方向上最大的系统性风险，且本轮未动）

- **24300 组的网格里没有任何多重性控制**：`watchlist_grid.py` 全文不含 `bootstrap` / `p_value` / `holm`。配 §12.3 ② 的测算 —— **约 283 组纯噪声组合能过稳定性门**，而选出的冠军是这 283 组里 `selection_score` 最高的那个。
- **全仓 Holm 只有两处**：`intraday_1400/evaluation.py:103-114`（被 `:437-441` 消费）、`rolling_fixed_relative_comparison.py:139`。后者的三个问题（本轮复验，文件 UNCHANGED）：`--block-lengths` 默认 `[5]`（`:178`）→ **家族大小 1 → Holm 退化成恒等映射**；Holm 只施于 rolling 侧的 per-baseline p 值（`:137-139`），**fixed 侧与 `rolling_vs_fixed`（`:140-146`）完全无多重性控制**（§9.2 问题一未修）；且该脚本**零引用者**（无测试、无 pipeline、无 docker），是一座 CLI 孤岛（`:151-153` 自标 `schema_version: 1` / `research_only: True` / `production_publication: False`）。
- **`reality_check` / `spa_test` / `deflated` / `hansen` / `white_test` / `bonferroni` / `newey` / `benjamini` / `fdr` → 全仓零实现。** `daily_optimization_pipeline.py:322` 会 bootstrap，但只用 `ci95[0] > 0` 单条判据（`:330-338`）。
- **`embargo` 仍是全仓 0 文件。**

### 12.6 本轮新增的「fixed-but-not-enabled」实例（累计已达第 7~11 种）

| # | 实例 | 证据 |
|---|---|---|
| ⑦ | **参数进了搜索空间但范围不覆盖有效区间** —— `hold_rank_buffer` 网格搜 {0,1,2}，而有效量级是 50 | §12.3 ① |
| ⑧ | `min_window_completion_ratio = 0.90` | `full_train_batched.py:1210`，全仓仅 4 处命中，**无 CLI flag、无 scheduler 接线**（默认值是生效的，属「静默生效」而非「未启用」） |
| ⑨ | `rolling_fixed_relative_comparison.py` 零引用者 + Holm 家族大小 1 | §12.5 |
| ⑩ | `--validation-months` **无任何脚本传递**；`--require-selection-provenance` / `--enforce-c30-gates` / `--min-adv20` / `--min-listing-sessions` 从生产不可达 | `scheduler_jobs.sh` grep 全 0（`rebalance-stride` 0、`hold-rank-buffer` 0、`rolling-factor-select` 0、`require-selection-provenance` 0、`enforce-c30-gates` 0、`min-adv20` 0、`min-listing-sessions` 0、`validation-months` 0） |
| ⑪ | **`hold_rank_buffer` 生产选股链根本不读** —— 网格搜得出、生产用不上 | `stock_analyzer/`、`app.py`、`realtime/` 三处 grep 全 0。→ 即使网格选出 buffer=2，落到生产等效值仍是 0 |

⑪ 是本轮最需要落地的一条：**§12.2 认定的唯一存活杠杆（降换手），其参数在生产链上是断开的。** 所以「验证 buffer」和「用上 buffer」是两个独立工程，前者要把搜索范围扩到 {0,5,10,20,50}，后者要把参数接进选股链 —— §6.1 L 的完整施工清单仍然适用，一条没少。

### 12.7 生产门禁状态（未变，且是正确的）

`docker-compose.yml:60 SCHEDULER_DISABLED=1`、`:78 RUN_ON_START=0`；SKIP 白名单（`scheduler_jobs.sh:47-57`）只豁免 `intraday-1400-*|realtime`（所以 `realtime-weight-shadow` 也在被 SKIP，§6.1 K 成立）。四个 `quant.scheduled_workflow` job（`:132/164/194/228`）已补齐严格 flag —— 三个 light job 传 `--train-target-mode tradable-label --strict-execution-labels --strict-calendar-factors --strict-announcement-lag --strict-pit-min-price-rows --purge-horizon --pit-index-code 000852.SH --skip-swing-grid`，`monthly-factor` 另加 `--strategy-mode candidate-upgrade --short-only-upgrade --rolling-top-factors 30 --max-factor-ic-corr 0.85 --promotion-*`。**在净收益为 −0.85bp/天、且唯一杠杆功效差 28 倍的当下，门禁关闭是正确的**，不建议恢复调度。

### 12.8 一句话（第四次快照版）

**这一轮的代码改动全部是工程加固（线程数、缓存键、跳窗门），收益方向零推进；真正的进展来自记录侧那个我要求的 30 因子公平冻结对照 —— 它是负面的，把长期被当作主力收益来源的 `+2.32bp/天` 清掉了（rolling−frozen −5.8564bp/天、Holm 后 p=1.0，正确表述是「测不出差别」而非「rolling 有害」）。清完之后账面上唯一站得住的杠杆只剩降换手：break-even 15.45bp vs 实际 20bp 这 4.55bp 的缺口是硬算术，同条件 rolling buffer=50 vs 0 的 +1.0632bp/天 能被换手恒等式独立核对（省成本理论值 1.148bp），但 p=0.29577、fixed 侧反号 −1.8874bp/天、且要做显著需约 28 倍样本 —— 更要紧的是这个参数在生产选股链上根本不被读取，网格也只搜到 buffer=2（实测在 top_n=2 下不触发）。所以下一步的正确投入不是再找因子、也不是再调成本，而是：① 把 `hold_rank_buffer` 接进生产选股链并把搜索范围扩到 {0,5,10,20,50}；② 给 24300 组网格加上多重性控制（当前约 283 组纯噪声能过稳定性门）。同时我对自己三条旧判定做了更正（网格 24300 而非 8100、buffer 是真搜索维度、月胜率池化已修），并点出记录里四处声称与代码不符 —— 其中 `intraday_1400/pipeline.py:692-773` 仍是池化 correlation，意味着「分钟特征统计口径已修」这个前提无法从代码确认，分钟特征、分数特征、sentiment 三者的「未测出」表述必须原样保留。**
