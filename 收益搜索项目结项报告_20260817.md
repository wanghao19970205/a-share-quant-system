# 收益搜索项目结项报告

> 结项日期：2026-08-17
> 项目周期：2026-08-12 → 2026-08-17
> 范围：日频轮动/固定 watchlist 选股链（生产分支） + `intraday_1400` 分钟特征分支（研究分支）
> 纪律执行情况：全程 `SCHEDULER_DISABLED=1`、`RUN_ON_START=0`，realtime 保持旧模型，未修改任何 active manifest，未执行任何生产发布，研究产物一律未发布。
> 详细过程与逐项证据见 `下一轮调研记录_20260812.md` 第 0 ~ 42 节；本报告只做收敛，不重复推导。

---

## 1. 结论

**本项目没有找到任何可作为 alpha 的增量收益来源。收益侧全部方向已关闭。**

判负不是"力量不足"或"门禁太严"，而是有因果解释的（见第 3 节）。项目的真实产出在方法侧和正确性侧（第 4、5 节），不在收益侧。

两条必须同时成立、不能只讲一半的结论（依据见第 3.5 节，调研记录 §44）：

- **现任基线确有真实的横截面排序能力**：在成交率逐位匹配的零假设下 0/200 次抽样达到其 holdout 表现，经验单侧 p `0.005`。它不是噪声，也不是"涨停买不进转现金"的副产物。
- **但"现任是最优版本"不成立**：同一个选择窗内 `selection_score` 更高的参数组在 holdout 上更差，说明生产用来挑候选的判据在样本外没有鉴别力。现任只是"目前手上最好的、且确实有效的一个配置"。

因此本项目的准确结论是：**在现有 universe、breadth 与 6 个月 holdout 分辨率下，既找不到可证明的增量收益，也无法证明现任已达最优。**

按既定标准，以下一律**不得**被引述为收益证据：方向性但不显著的差异、被 cap 的运行、`gate-only` 结果、选择窗内的改善、以及仅在 Sharpe 上为正但配对 p 值不显著的结果。本报告严格遵守这一点。

---

## 2. 搜过什么，结论是什么

按方向逐条收口（括号内为调研记录节号）：

- **方向 A / A2 / D：组合构造参数网格**（阶段 A/A2/D/M/P）。24300 组合 × horizon 1/2/3 全量重跑，候选在 holdout 上全部输给现任或不可分辨。**判负**。
- **`hold_rank_buffer`**（阶段 E/F/O/35）。改判为"非劣 + 成本算术"，族校正后 p `0.078 ~ 0.090`，只是方向性；且机制被证明是 alpha 而非成本节省，且语义在线上不可兑现。**不得作为收益证据**。
- **rolling vs fixed-33 选因子链**（0.3 节）。rolling 净 `+23.56bp/日`、fixed `+18.96bp/日`，方向性净增益约 `+4.60bp/日`，但配对 CI 跨零、Holm block-5 `p = 0.4111588841`，且 fixed-33 来源不可审计。**不是 confirmed alpha**。
- **`intraday_1400` 分钟特征**（阶段 15~21）。正式四折 `formal_wf_100k_v4`：`no_enhancement_passed`，7/7 项 Holm `p = 1.0000`；可执行性归零。**形式判负，分支暂停不删除**。
- **方向 E：库内未被消费的信息源**（阶段 Q/R/S/T/U，37 节）。`margin_detail` 的 `rz_buy_intensity` 是**真信号**——去掉 `pred`/`turnover`/`volatility_10`/余额水平后残差 IC `-0.025`、`t = -6.54`，衰减平滑（lag1 `-0.025` → lag5 `-0.012`）、分年稳定、十分位单调下降 `10.5bp`。但组合层面无收益：holdout 三档否决结果完全相同（`11.02bp`、Sharpe `4.34`、原始 `p = 0.237`、月胜率 `0.14`）。决定性原因是**作用面不相交**：holdout 里 `94.9%` 的实际选股根本不是两融标的。`dividend` 表 8 个特征增量 IC 全在 `-0.009 ~ +0.006`，Holm 后存活集合为空。**判负；仓库内已无未筛的逐股源**。
- **成本口径修正**（0.4 节）。20bp→7bp 约改善 `11.8bp/日`，但盘中链仍为负。**是记账修正，不是新增 alpha**。
- **breadth：唯一剩下的方向**（阶段 Y，42 节）。预登记后测 `top_n ∈ {6,8,10,12}`（生产网格只搜到 5），固定 `gross_exposure=0.30`、`slot_weight=gross/top_n`、族大小 4。轴确实咬得住（`avg_holdings` 0.33 → 2.23），holdout 日均收益随 breadth **单调下降**（h1：10.66 → 7.43 → 8.25 → 6.36 → 5.54bp），四个候选受控闸门全部 `passed=False`，单侧原始 p `0.746 ~ 0.968`，Holm 后全为 `1.0`，**连不做多重比较校正的旧闸门也全部不通过**。**判负**，方向 E 与 `hold_rank_buffer` 的最后一个前置条件随之消失。

---

## 3. 为什么没有收益：三层因果解释

这是本项目最有价值的结论——它把"搜不到"从运气问题变成了结构问题：

1. **被搜索的对象里没有新信息**（阶段 P）。现任参数在 `active` 与 `monthly_factor_candidate` 两个不同 artifact（sha256 不同、`pred` 相关 0.991、最大绝对差 0.77）上的收益序列**逐位相同**：483 个观测、`max_abs_diff = 0.0`、持仓 `mean_jaccard = 1.0`。原因是 `top_n = 2` 下前两名几乎从不改变。所以阶段 A/A2/D 的全部"候选 vs 现任"差异来自组合构造参数，不来自新信息。
2. **真实存在的新信息与策略作用面不相交**（阶段 T）。`0.025` 量级的横截面 IC 需要一个宽的作用面才能兑现，而 `top_n = 2` 只使用极值尾部。
3. **breadth 本身无效，而且方向是反的**（阶段 Y）。把名额往下摊必然摊薄——说明模型排序是有信息的，第 3~12 名的期望收益本就低于前 2 名。更关键的是，真正的持仓上限不是 `top_n` 而是筛选条件（`positive_only` + `pred_quantile=0.55` + `ridge_quantile=0.70`，`top_n=12` 时 holdout 也只持 2.23 只），而这两个 quantile 本来就在搜索网格里、搜索自己选中了最紧的一端。

再叠加一层检出能力约束：当前 6 个月 holdout 的可检出下限是 `20bp/日`（跨分支合并后约 `13bp/日`，阶段 N），**本身就高于所有候选改动的量级**。

### 3.5 复核：对比公平性与基线有效性（阶段 Z1/Z2/Z3，§44）

结项后针对"是不是没和基线公平对比、基线是否恰好最优"做的复核，查出两处真实的不对称，其中一处证伪了我自己的检验设计：

- **调优不对称（已修，结论不变）**：阶段 Y 只换 `top_n`、其余沿用调过优的现任值。阶段 Z1 给 `top_n ∈ {2,6,8,10,12}` 每档各自搜完整 540 组并取 argmax，族大小 5。结果四档仍全部不通过（原始 p `0.787 ~ 0.921`，Holm 后 `1.0`，旧闸门亦否），收益随 breadth 单调下降依旧成立。**不对称不是判负的原因。**
- **选择判据在样本外无鉴别力（新发现）**：Z1 的对照组 `top_n=2` 重调优后与现任不同，选择窗上更好（h1 `12.80` vs `11.62bp`）、holdout 上更差（`8.52` / Sharpe `3.38` vs `10.66` / `4.11`，月胜率 `0.0`）。这是 §33 过拟合结论的更强表述，也意味着"没有候选打得过现任"部分源于**选择机制识别不出更好的候选**。
- **基线首次被拿去和零假设比（Z2 设计有缺陷，Z3 修正）**：Z2 只置换打分列，零假设持仓 `1.96` 只 vs 实际 `0.33` 只，把"排序能力"与"83% 交易日空仓"混在一起，不可采信。Z3 把可买标记与分数用同一置换搬走，持仓逐位匹配（`0.3305…` vs `0.3305…`，空仓 88 天 vs 88.0 天），结果 h1 实际 `+10.66bp` vs 零假设均值 `-2.96bp`、最大 `+2.84bp`，**0/200**，p `0.005`；h2 同向。**基线的排序能力成立。**
- **一条量化闭环**：Z3 给出的基线总排序价值约 `13.6bp/日`（h1），而 §30.1 测得 6 个月 holdout 的可检出增量下限是 `20bp/日`。任何增量改动都只是这 `13.6bp` 的一部分，而门禁要 `20bp` 才看得见——这就是"为什么什么都测不出来"的定量答案。

---

## 4. 项目的真实产出（非收益）

- **门禁可信度**：旧门禁实测假阳性 `3.5% ~ 3.75%` → 受控门禁 `0.0%`（阶段 C/G/G2/25 节）。多重性控制内部三个参数选择已单独审计（阶段 L）。
- **算力**：短分支 24300 → 8100 组合，摆动分支 2304 → 768，且不损失结论。
- **可检出下限工具**：跨分支合并把下限从 `20bp/日` 降到约 `13bp/日`（阶段 N），仅适用于同向作用的共享参数。
- **方法论留档**：`hold_rank_buffer` 在选择分数上单调，因此不可作为被搜索参数（`watchlist_grid.py:168-173` 已写入代码注释）；挂载必须走 `/research/` 而非 `/app/`，否则 `sys.path[0]` 会遮蔽 `/src`（32 节）；判断回归必须整仓同版本，新旧混装的失败不是回归（38.1 节）。
- **回归验证**：`quant.test_model_expansion_experiment` **156 tests OK**、`intraday_1400.test_intraday_1400` **152 tests OK**（整仓同版本挂载）。

---

## 5. 查出的正确性缺陷（本项目未落地，全部列为重启前置）

这五条是本项目的另一半产出。**全部刻意未落地**，原因是每一条都会改变特征域，会使本项目已完成的全部对比失去可比性；且生产在结项前不重启。

1. **`margin_buyable` / `margin_shortable` 是退化列**（39、40 节）。源表 `margin_underlying_szse` 只有 22 个日期（`daily_update.py:484-487` 每次只抓 `end` 一天、无回补），且 `当日可融资/可融券` 几乎恒为 `Y`，所以这两列在有观测处**恒等于 1.0**（`n_distinct=1`、`std=0.0`、完全共线）。危害不是缺历史，而是**常量经 neutralize→zscore 链被放大成单位方差浮点噪声**：探针面板实测 `std = 0.999576`、18890 行只有 80 个不同取值、该噪声的 `ic_mean = 0.0483`、`ICIR = 0.262`、`t = 1.08`，与真因子同量级，`min_weight=0.03` 拦不住。当前之所以没出事，只是因为存盘 prepared 文件建于 2026-07-09、彼时两融覆盖近乎为零。处置：摘掉这两列，或改从 `margin_detail`（722 个日期、4417 只、两市齐全）重建。
2. **存盘 prepared 面板不可由当前代码复现**（40.3 节）。`factor_panel_full_a_cont_parts` 的 `prepared_monthly` mtime 为 2026-07-09，早于修复 commit `2aedb38`（2026-08-12，`regressors = [n for n in neutral_cols if n != c]`，`engineering.py:786`），因此其 `log_mv_total` 是被自回归破坏的版本：789421 个观测只有 389 个不同取值、24.3% NaN；当前代码在同一原始数据上产出 distinct ratio 0.98、0% NaN。**重建代价已量测**（阶段 X）：30 个因子里换掉 1 个（`jaccard = 0.9355`，`log_mv_total` 出、`yjbb_gross_margin` 入），前 15 名逐位相同。既有配对对比仍然有效（损坏是共模），但现任数字重建后不可逐位复现。
3. **选因子器系统性偏爱噪声**（41.2 节）。`_rolling_factor_selection` 的去克隆过滤器只剔除"IC 序列与已入选因子相关 ≥ 0.85"的因子，**纯噪声列与任何因子都不相关，因此必然存活**；配合绝对阈值 `ic_count >= 20`（本例 71/343 = 21% 覆盖率即通过），任何"高缺失 + 无相关性"的退化列都能稳定占住名额。建议改为相对覆盖率阈值，但它会同时影响目前 stability 排前四的 `amplitude`/`change`/`turnover_chg_5`/`amount_chg_5`（96~98% 缺失），**必须单独预登记后再定**。
4. **重复特征**（40.1 节）。`income_net_profit` 与 `yjbb_net_profit` 相关 `0.9996`，且两者都在现役 34 个特征里。
5. **发布链改动**（38 节第 3 项）。持久化前一日选股 + `top_n` 裁剪 + 实时可买掩码。不是收益项，是重启生产的前置条件。

---

## 6. 重启生产的前置条件清单

按依赖顺序：

1. 第 5 节第 3 条的选因子器阈值改动**单独预登记**并定案（它决定重建后的因子域）。
2. 摘掉 `margin_buyable` / `margin_shortable`；合并 `income_net_profit` / `yjbb_net_profit`。
3. 整份重建 `factor_panel_full_a_cont_parts`，并**重跑现任基线**，接受数字变化（代价已量测：1/30 因子）。
4. 落地发布链改动（持久化前一日选股 + `top_n` 裁剪 + 实时可买掩码）。
5. 恢复调度前重新执行 38.1 的整仓回归。

---

## 7. 明确不做的事

- 不放宽 α、族大小、`min_significant_sharpe_gain`。
- 不做 `intraday_1400` 的参数/特征搜索（已形式判负）。
- 不加密日频网格（阶段 P 已证明无新信息可赢）。
- 不做 `hold_rank_buffer × top_n` 的二次搜索，也不做放宽 quantile 的复搜（前者前置条件已不成立，后者是重搜已搜维度）。
- 不把两融因子加进特征表（IC 好看但组合层面无收益）。

唯一有理论价值的后续方向不是继续搜参数，而是**提高检验分辨率或降低基线噪声**：§30.1 测得 holdout 要 18 个月才把下限从 `20bp/日` 降到 `10bp/日`；或者用阶段 Z3 那种成交率匹配的零假设检验替代 candidate-vs-incumbent 作为主判据。这是方法论方向，不是收益方向，需单独立项与预登记。

---

## 8. 未平项

- **commit 决策未定**：12 个修改文件，加上未跟踪的 `research_runs/grid_multiplicity_20260815_probes/`（`grid_gate_stage_a … y`）与两份 `复核意见*.md`。本项目全程未提交，等待指示。
- 上述五条重启前置条件均未落地，按设计如此。

---

## 9. 产物与可核验性

全部研究产物在远端 HB18-8241（`root@192.140.180.77`）`/www/A/research_runs/grid_multiplicity_20260815/out/`，容器一律 `--network none`、只读挂载 `source/`→`/src`、脚本挂 `/research/`、只有 `out/` 可写。每个阶段的容器状态、退出码、日志与产物 sha256 均已逐条记入调研记录。

最后一个阶段（breadth）的核验信息：脚本 `grid_gate_stage_y.py` sha256 `50fe391c…`，产物 `grid_gate_stage_y.json` sha256 `40a3fbe0…`、`grid-gate-stage-y.log` sha256 `ed32b2aa…`，容器 `grid-gate-stage-y` `exited exit=0`，挂载自检输出 `watchlist_grid=/src/quant/watchlist_grid.py`。

公平性复核三阶段（§44）的核验信息：脚本 `grid_gate_stage_z1.py` `5df5a2f6…`、`z2` `426e6e50…`、`z3` `4253cd05…`；产物 `grid_gate_stage_z1.json` `e4526960…`、`z2.json` `984ad5a7…`、`z3.json` `b6baa472…`；容器 `grid-gate-stage-z1/z2/z3` 均 `Exited (0)`，日志已归档，挂载自检均为 `/src/quant/watchlist_grid.py`。

---

## 10. 结项补录（2026-08-19：Z5/Z6、一处撤回、生产已恢复）

### 10.1 三条可以写死的结论

1. **识别端不可修**：45.4 的两个判据在 120 个池化单元上校准后都不可用——`D1_absolute` 结构性无效（"现役 + 无用列"继承现役技能），`D2_paired` 在 family=2700 下功率≈0（16.1bp/日 才到 0.25）。这是校准结果，不是推测。
2. **生成端从未被检验**：Z4 的 C 分片在第 16–18/30 抽被 `Exited 137` 中止且无产物，预登记的终止规则不追溯修改。所以"不存在更好的候选"这句话**本项目没有资格说**。
3. **口径问题的正确形态是部署风险，不是既存缺陷**（此条替换 08-17 版报告中的"口径分歧是已确认缺陷"）：部署中的生产在选参与验收两端都是补位、内部一致；不补位只存在于不发布的研究控制器 `daily_optimization_pipeline.py` 与仓库里尚未部署的新代码。**撤回**先前基于"活容器内有该文件"作出的"发布链是不补位"的判断——文件在容器里不等于被生产调用（`grep` 在 `/app/docker/` 与 `scheduled_workflow.py` 内零命中）。

### 10.2 Z5：长样本上重测现役（family=1，无晋升授权）

67 窗走查放开 `--recent-windows` 后得 1334 个有效交易日（**预测起点 2021-02-02**，非 2018：36+1 个月预热吃掉前三年）。硬控制门位对位复现 Z4a（`10.660575342126032bp`）。不补位口径下：全样本 10.78bp/日、SE 1.06、t=10.18、置换 z=22.3、经验 p=1/201；分段 8.43 → 13.44bp 无衰减；95% 可判定增量下限从 118 日的 14.86bp 降到 **4.15bp**。可以说"现役有真实且非偶然的选股能力"；不能说"它最优"。

### 10.3 Z6：换会计约定后冠军是否还是冠军

同面板同 8100 行网格，三条臂跑真实代码。生产（补位）臂的 argmax 与现役参数逐字段相同（rank=1，复刻忠实）；换成不补位后现役掉到 4032/8100（606 日）与 5443/8100（1334 日），排名 Spearman **-0.504**、top-500 重叠 0；只切换补位一件事的 `refill_emulated` 臂现役回到 rank=1，**证明补位单独解释全部翻转**。结论：部署仓库里的不补位代码而不重新选参，会让生产跑在新口径下排 5443 名的参数上。

### 10.4 项目状态与生产恢复

- 研究已停机。`research_runs` 清理后 118G → 82G，保留全部 JSON/脚本/日志与三臂 ranking parquet、长样本预测。
- `docker commit` 固化容器内的 `8a7bd68` 代码为 `stock-scheduler:deployed-20260819`，解除"recreate 即丢代码"的脆弱点。
- 生产已恢复且**完全回到历史行为**：`SCHEDULER_DISABLED=0`，日更含锚定现役的 54 组有界微调、月度 `monthly-factor` 的 8100 组 argmax 晋升**一并恢复**。我提出的两项收紧（关月度、日更加 `--skip-short-grid`）被否决，理由成立：无证据支撑的行为变更不属于"恢复"。留档反对意见见调研记录 §45.9。
- §6 的五条重启前置条件仍未落地；实盘下单链仍未审计；仓库 16 项脏改动未提交、未部署。

